# خطة تنفيذ: تسجيل الدخول بجوجل والمزامنة المستمرة عبر Cloudflare Workers و D1

## الهدف
إضافة ميزة المزامنة المستمرة التلقائية (Offline-first) لحفظ إعدادات المستخدم، ختماته، مواضع وقوفه، وعدّادات الأذكار والمسبحة، باستخدام Google Sign-In للهوية فقط، وCloudflare Workers + D1 Database لتخزين البيانات، مع التأكيد الصارم على الأمان وأن يتم التحقق من الهوية من الـ `idToken` حصراً.

## Open Questions (أسئلة للمالك)
- هل قمت بإضافة `CF_WORKERS_TOKEN` بالصلاحيات المطلوبة (D1:Edit و Workers Scripts:Edit) في ملف `scripts/.env`؟ (لقد تأكدت من وجوده برمجياً، لكن أرجو تأكيد أن الصلاحيات صحيحة وتم إنشاؤه من الحساب الصحيح `Int.vip00@gmail.com's Account`).
- هل هناك أي مفاتيح `SharedPreferences` محددة لا ترغب في مزامنتها (مثل إعدادات المظهر إذا كانت خاصة بالجهاز)، أم نقوم بمزامنة جميع مفاتيح الإعدادات والتقدم؟

## التغييرات المقترحة

### 1. البنية التحتية الخلفية (Cloudflare Worker + D1)
- **قاعدة البيانات D1:** 
  - جدول `user_state` (للاعدادات والختمة): الأعمدة `(sub, key, value, updated_at)`.
  - جدول `user_counters` (للأذكار والمسبحة): الأعمدة `(sub, key, event_id, increment_value)` لضمان عدم التكرار والجمع المتراكم.
- **Worker (API):**
  - مسار `POST /sync` لاستقبال التحديثات، ومسار `GET /sync` لجلب الحالة.
  - **الأمان:** التحقق من `idToken` عبر `https://oauth2.googleapis.com/tokeninfo` أو `certs`، والتأكد من تطابق `aud` و `iss` وعدم انتهاء `exp` واستخراج `sub` ليكون هو المعرّف الوحيد (`user_id`).
  - سيتم كتابة السكريبت في مجلد `sync_backend` ونشره باستخدام `wrangler` عبر الـ Token المتاح في `.env`.

### 2. تطبيق Flutter (rafeeq_app)

#### [NEW] `lib/core/services/sync_service.dart`
- خدمة تعمل بالخلفية لمراقبة التغييرات في `SharedPreferences` عبر `debounce` (ثانيتين).
- طابور محلي (Local Queue) باستخدام `sqflite` أو `shared_preferences` لتخزين التحديثات أثناء انقطاع الإنترنت وإعادة إرسالها فور عودة الاتصال.
- معالجة التعارض: 
  - الإعدادات والختمة: "الآخر يكسب" (بناءً على `updated_at`).
  - العدادات: إرسال "مقدار الزيادة" (Increment) مع `event_id` فريد (UUID) ليقوم السيرفر بجمعه.

#### [NEW] `lib/features/auth/`
- دمج إضافة `google_sign_in`.
- واجهة الدخول (Card في شاشة المزيد): يعرض رسالة دعوة قبل الدخول، وبعد الدخول "ينقلب" بـ Animation (Flip) ليعرض البريد الإلكتروني، آخر مزامنة، وتقدم الاستخدام (نسبة الختمة، مجموع الأذكار) بشكل حي، وزر خروج يمسح الحساب والبيانات.
- إضافة شاشة في أول تشغيل (Onboarding/Splash) للدخول "لمرة واحدة" مع خيار "تخطي" فعال.

#### [MODIFY] `lib/main.dart` & `pubspec.yaml`
- إضافة حزم `google_sign_in`, `uuid`, `connectivity_plus` (لمراقبة حالة الإنترنت).
- تهيئة `SyncService` عند بدء تشغيل التطبيق.

## خطة التحقق والقياس (Verification Plan)
### Automated Tests
- الحفاظ على الـ 212 اختبار الحاليين دون كسر أي منها.
- إضافة اختبارات وحدة (Unit Tests) لعملية دمج البيانات (Conflict Resolution) والتأكد من أن العدادات تجمع بشكل صحيح، والإعدادات تعتمد الأحدث.

### Manual Verification
1. **اختبارات الأمان (Security Tests):**
   - إرسال طلب لـ Worker بدون توكن للتأكد من إرجاع `401`.
   - إرسال طلب بتوكن مزيف أو منتهي للتأكد من إرجاع `401`.
   - محاولة استعلام بيانات `sub` آخر باستخدام توكن صالح لمستخدم مختلف للتأكد من أنه يقرأ بياناته هو فقط ويرفض التلاعب.
2. **اختبار المزامنة (End-to-End):**
   - تسجيل الدخول على المحاكي `emulator-5554`، قراءة قرآن والتسبيح لعدة مرات.
   - إطفاء الإنترنت، التسبيح مجدداً (محاكاة الأوفلاين)، ثم إعادة الإنترنت.
   - مسح بيانات التطبيق أو تثبيته على محاكي آخر، وتسجيل الدخول ورؤية استرجاع الأرقام الدقيقة.
   - توثيق وتسجيل هذه الأرقام في التقرير النهائي بدقة.
