# رفيق الدرب — الحالة عند التسليم (2026-09-24 ليلًا)

البرومبت الجاهز للنسخ في `NEXT_PROMPT.md`. هذا الملف هو الشرح الأطول، و`TASK_FOLLOWUP.md`
هو سجل الخطوات الحي (فيه مصفوفة الفحص كاملة والدليل على كل بند).

## الحالة
- **آخر إصدار منشور: v3.62.0** — التاج على `1f758e40`. حُذف v3.61.0 وتاجه؛ v3.51.0 ونسخ
  `content-*` باقية. الـ APK المنشور نُزّل ثانية وطابق البناء بايتًا ببايت (sha256 `9eacb8b4…`).
- **`master` متقدّم على الإصدار** بإصلاحات «الفحص الشامل — الجزء الثاني». كلها في الكود،
  و`flutter analyze` نظيف و**577 اختبارًا تنجح**، لكنها **لم تُبنَ ولم تُرَ على جهاز**.
  واحد منها **Kotlin لم يُترجَم أصلًا**.

## ما أُنجز اليوم (مرئي على المحاكي بنسخة 3.62.0)
الأذان يوقف قارئ الكتب مؤقتًا ويكمل بعده · الأذان يلغي تسجيل التسميع · حذف قارئ «آية بآية»
أو سورة أثناء تشغيلها يوقفها · «استمع» معطّل أثناء التسميع · أزرار التحميل تعرض حالتها
في كل التطبيق · إعادة محاولة على النت الضعيف · كارت القارئ مقروء في الوضع الفاتح ·
الأذان فوق وضع التركيز · الثيم واللغة والخلفية أثناء التلاوة · القبلة ٢٥٨° مطابقة للحساب ·
الثيمات الثلاثة · إعادة التشغيل تعيد جدولة الأذان · التطبيق يعمل بلا نت.

## نصف منتهٍ — أعده ولا تثق به (بناء واحد ثم تحقق على المحاكي)
1. **Kotlin — `MainActivity.onResume`**: إذا الإشعارات مرفوضة والأذان يعمل في الخلفية لم
   يكن هناك زر إيقاف أصلًا (BAL_BLOCK). الإصلاح يفتح شاشة الأذان عند فتح التطبيق. **لم
   يُترجَم.** التحقق: ارفض الإشعارات، اضغط «تجربة» في إعدادات كل صلاة، ثم HOME فورًا،
   ثم افتح التطبيق → يجب أن تظهر شاشة الأذان بزر «إيقاف».
2. **طلب إذن الإشعارات كان يظهر مرتين** متتاليتين (`alarm_permissions_service.dart`) —
   صار طلبًا واحدًا. التحقق: امسح البيانات، اضغط «سماح بها» → حوار واحد فقط.
3. **انقطاع النت أثناء تنزيل WorkManager** كان يُعامَل كإلغاء (والتنزيل يكمل في الخلفية
   بينما الشاشة تقول «غير مثبّت»). `DownloadManager._userCanceled`. التحقق: نزّل صوت القارئ
   المحسّن، شغّل وضع الطيران ١٥ ث ثم أطفئه → يبقى «جارٍ التحميل» ويكمل ويصبح «مثبّت».
4. **رسائل الخطأ الخام** (`DioException … github.com` بالإنجليزية) صارت «لا يوجد اتصال
   بالإنترنت»: `core/utils/user_error.dart` في التسميع والتحفيظ وتسجيل الدخول، وخطأ
   `DownloadManager`. التحقق: وضع الطيران أثناء تنزيل نموذج التسميع.
5. **تذكيرات الأذكار/الختمة/التسبيح** تفتح شاشتها عند الضغط (كانت تفتح الرئيسية فقط):
   `app/notification_open.dart` + `open:` payloads. التحقق: اضبط تذكير أذكار الصباح بعد
   دقيقتين، اضغط الإشعار → أذكار الصباح. (الموجودة على الهاتف تأخذ الـ payload عند إعادة الجدولة.)
6. **معاينة فيديو البداية** كانت تترك التلاوة موقوفة → تُستأنف عند الإغلاق.
7. **شاشة البداية على نت بطيء** كانت ترشّح العفاسي ١٫٧ جيجا (السيرفرات البطيئة كانت تُستبعد)
   → البطيء يبقى في القائمة «بطيء على هذا الاتصال» (مفتاح `onboarding.response_slow` × ٧ لغات).
   التحقق: `adb emu network speed umts` + مسح البيانات → الترشيح البنا ٣٨٣٫٦ م.ب.
8. **اسم المدينة بعد تغيير اللغة** يُقرأ من الإحداثيات المحفوظة فورًا (`LocationService.savedIn`)
   بدل انتظار GPS حتى ١٥ ث.

## بقية الفحص الشامل (لم يُجرَ)
- الوضع الأفقي على الشاشات الرئيسية (الخط الكبير ١٫٣ جُرّب: لا قصّ ولا تداخل).
- الأردية (يمين لليسار، أرقام لاتينية) مرور سريع.
- الرقية/التجويد/ملفات الجهاز مقابل قارئ الكتب والأذان: كلها على المشغّل المشترك نفسه
  الذي جُرّب مع السور — مغطاة بالتصميم، لم تُجرَّب منفردة.

## بعد التحقق
- إصدار **3.63.0** فقط إذا طلب المالك: ارفع `pubspec` و`AboutScreen.appVersion`، ابنِ بـ
  `build_github_release.bat` (المحاكي مطفأ، trap 24)، احذف v3.62.0 وتاجه، أبقِ v3.51.0 و`content-*`،
  تأكد أن التاج = HEAD.

## محجوب على المالك
- صوت الهاتف في قارئ الكتب يتوقف مع الأذان: مختبَر آليًا فقط (المحاكي بلا صوت عربي) — يُجرَّب على هاتفه.
- تدوير مفتاح R2 (مسجّل في رأس HANDOVER.md).
