# رفيق الدرب — ملخّص الجلسة التالية (2026-09-22)

اقرأ `CLAUDE.md` أولًا، ثم أعلى `HANDOVER.md` (جدول الحالة + كتلة «SESSION
2026-09-22»).

## الحالة
- `master` مدفوع. **لا إصدار منذ v3.55.0** (`bb476739`). كل ما بعده رُئي على
  emulator-5554 فقط، **لم يصل لهاتف المالك** (فُصل عن adb).
- 491 اختبارًا ناجحة، `flutter analyze` نظيف، المحتوى المستضاف 10/10 = 206.

## ما أُنجز ولم يُصدَر
الختمة (أوراد/تراجع/قوائم)، سور مختارة، الأكورديون + زر الرجوع، تلاوات
الآيات (جدول عدد الآيات)، إخفاء الكتب، تنظيف الإعراب، التنوين المتتابع، RGB،
صيغة النسبة الموحدة، شرح الجزرية، شاشة الشرح/القبلة/صور الشرح، إشعار الصلاة
ثلاثة أسطر.

## التالي بالترتيب
1. **أكمل بناء المرحلة ١** (19 كتابًا، 16 مبنيّة على القرص؛ 3 ناقصة).
2. **أخرج** `tawdih_al_maqasid_sharh_al_nuniyyah` واقرأ بطاقة
   `al_rawd_al_murbi` (حاشية العثيمين؟).
3. `py -3 scripts/library_phase1.py --report` ثم بدونها → رفع + تحقق + كتالوج،
   ثم رؤية المكتبة على المحاكي.
4. **تنظيف حواشي المحققين** عن الأسماء الخمسة (إلا التخريج)، بقاعدة المالك:
   ما لا يضر حذفه يُحذف. لا تمس نص المؤلف ولا الأسماء المتشابهة.
5. قس كتابًا كبيرًا على هاتف قبل موسوعات المرحلة ٢.
6. إصدار: رفع `pubspec`، `build_github_release.bat`، حذف الإصدار السابق ووسمه
   (عدا v3.51.0 نقطة الرجوع)، ملاحظات عربية صادقة.
