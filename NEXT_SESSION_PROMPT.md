# NEXT SESSION — رفيق الدرب

هذا هو الملخّص الطويل الذي يشير إليه `NEXT_PROMPT.md` (وهو النص نفسه الذي
يلصقه المالك في بداية الجلسة). حُرِّر في 2026-09-20 بعد تحقّق مباشر:
`flutter analyze lib test` نظيف، `flutter test` 433 ناجحًا و2 متخطَّين،
والمحتوى المستضاف 10/10.

---

اقرأ `CLAUDE.md` ثم `HANDOVER.md` (كتلة الحالة في أوله مكتوبة اليوم 2026-09-20).
وابدأ من حيث وقفنا بالضبط.

الحالة: آخر إصدار **v3.42.0** والتاج عند `6e56cc2b`. الفرع `master` سابقه بكوميتين
غير منشورين: حذف المصحف الليلي، وأدوات خريطة التظليل. الشجرة نظيفة،
`flutter analyze lib test` نظيف، و`flutter test` ‏**433 ناجحًا و2 متخطَّين**
(اختبارا التصدير الموسومان `export`)، وكل مسارات المحتوى المستضافة **10/10**
ردّت على طلب مدى — كل هذا مقيس اليوم.

## 1) التظليل في المصاحف — أهم طلب عند المالك، وهو **غير منتهٍ**

قال حرفيًا: «اتأكد مليون المية إن كل مصحف صفحة صفحة آية آية التظليل فيها مظبوط
بالمللي… ده مطلب أهم من التطبيق نفسه».

ما ثبت بالصور في الجلسة الماضية: المصاحف الأربعة الممسوحة لا تملك إحداثيات
خاصة بها، بل تستعير إحداثيات مصحف المدينة وتُمطّها على كل صفحة، والطبعات لا
تكسر سطورها عند الكلمات نفسها ولا تضع تَرويسات السور على الأسطر نفسها (مصحف
قطر يضع ترويسة النساء أسفل صفحة 76 بينما تفتح المدينة صفحة 77 بها). لذلك
يقف التظليل أحيانًا كلمة أو سطرًا كاملًا بعيدًا عن علامة الآية.

الأدوات جاهزة في `scripts/` ومُسلَّمة، لكنها **غير مركّبة في التطبيق**:

1. صدّر الطبقة المستعارة التي تبدأ منها الأداة تخمين الشبكة:
   `cd rafeeq_app && git show 6c9f10f9:rafeeq_app/assets/data/mushaf/editions.json > build/hl/editions_borrowed.json`
   ثم `EDITIONS=build/hl/editions_borrowed.json OUT=build/hl/rects.json flutter test --tags export --run-skipped test/export_highlight_rects_test.dart`
2. حمّل صفحات المصاحف الأربعة إلى `rafeeq_app/build/hl/pages/<tajweed|madinah_gold|qatar|kuwait>/NNN.jpg`
   (الصفحات ليست في المستودع؛ التحميل من R2 استغرق دقائق).
3. **أعد التشغيل الكامل من الصفر** — الأرقام القديمة (قطر 591، التجويد 556،
   المذهّب 524، الكويت 336) **سابقة** لآخر ثلاثة إصلاحات (إزالة خطوط المسطرة
   المطبوعة، وفحص «قلب السطر داخل الشريط»، وقصر أسطر الترويسة على بدايات السور
   الحقيقية). بعد الإصلاحات لم تُجرَّب إلا الصفحات 1–60: **قطر 56/58، المذهّب
   55/58، التجويد 47/58**. لا تثق بأي رقم لم تُخرجه بنفسك:
   `py -3 scripts/build_printing_map.py <edition>`
4. افتح الصفحات المرفوضة ومثلها من المقبولة بالرسم `--draw` **وانظر إلى الصور**.
   كل استنتاج صحيح في الجلسة الماضية جاء من النظر إلى صورة، لا من رقم.
5. ثم `py -3 scripts/install_printing_maps.py`، وأعد كتابة اختبار
   `test/mushaf_polygon_fit_test.dart` ليقفل التصميم الجديد (كان فيه اختباران
   جديدان: «كل مصحف يحمل خريطته» و«صفحة 404 من قطر: الم وحدها»، وقد أُعيدت
   الملفات إلى حالة ما قبل التركيب فاختفيا).
6. جرّب على `emulator-5554`: اضغط مطولًا على آية في كل مصحف، في **الوضع العادي
   والدافئ والليلي**، وفي **الوضعين الرأسي والأفقي**. المالك نبّه على هذا تحديدًا.
7. الصفحتان 1 و2 لا خريطة لهما بعد (الفاتحة وأول البقرة تُرسمان في كتلة نص
   مختلفة) — تحتاجان معالجة خاصة.

**بديل يستحق الفحص أولًا:** مكتبة QUL (`qul.tarteel.ai`) تنشر بيانات تخطيط
جاهزة لـ 12 مصحفًا، منها مصحف المدينة بثلاث طبعات و**مصحف قطر**، وتُرسم
الصفحة بخطوط المجمّع (خط لكل صفحة، كل رمز كلمة كاملة) فيكون التظليل مضبوطًا
بلا قياس. قِيس اليوم: الـ 604 خط ttf = **198.5 ميجا**، و woff2 أصغر بكثير
(40–190 كيلو للصفحة في عينة من 5 صفحات). **العائق: الرخصة.** لا QUL ولا
ملفات الخط نفسها تذكر رخصة (اسم الخط الداخلي «Test Font, KFGQPC»)، وموقع
خطوط المجمّع `fonts.qurancomplex.gov.sa` لم يستجب من هذه الشبكة (timeout
مرتين)، وأرشيف الإنترنت ردّ 429. المالك يحاول فتحه من جهازه. **لا تبنِ شيئًا
على هذه الخطوط قبل قراءة شروط المجمّع.**

## 2) بعدها، بالترتيب الذي طلبه

1. **المصحف النصي والبسملة:** أخرج البسملة من أول آية، واجعلها سطرًا مستقلًا في
   منتصف الصفحة فوق أول آية من كل سورة، وعُدّها آية في الفاتحة فقط، ولا تكتبها
   أصلًا في التوبة. السبب: عند تشغيل التلاوة يظلَّل الآن البسملة والآية الأولى
   ككتلة واحدة.
2. **تكبير المصحف بإصبعين**، والرجوع للحجم الطبيعي بضغطة.
3. **شريط الخيارات في المصحف:** اسم السورة يمينًا، والجزء شمالًا، ورقم الصفحة
   أسفل الشاشة، وبلا تكرار لرقم تطبعه الصفحة نفسها. ويشمل ذلك الوضع النصي.
4. **تبويبات المكتبة:** عناوينها مقصوصة، ويجب أن يصغر الخط داخل الكبسولة عند
   اختيار خط كبير حتى لا يخرج عنها.
5. **«المزيد»:** الكارت الذي تحته كروت يأخذ لونًا مميزًا، والكروت التي تحته
   تأخذ لونه وتكون أقصر عرضًا، وبشكل كروت «المصادر والمراجع».
6. **سياسة الخصوصية:** عبارة «التطبيق لا يطلب حسابًا» غير دقيقة — الصحيح أن
   الاستخدام لا يحتاج حسابًا وأن تسجيل الدخول اختياري للمزامنة فقط. عدّل
   `legal/privacy.html` وارفعها بـ `py -3 scripts/r2_upload_privacy.py`.
7. **موقع المراجعة** https://tito423.github.io/rafeeq-review/ — المرحلة الثانية:
   الكتب التسعة بحكم كل حديث ومحققه (67,153 حديثًا، 45,219 منها بحكم ومحقق
   مسمّى)، ثم كتب المكتبة. المولّد `scripts/build_review_site.py`.

## 3) أشياء لم تُجرَّب على جهاز، أعِد تجربتها ولا تصدّقها

* البحث عن «ابن القيم» من شاشة البحث نفسها (اتُّبِع باختبار على الكتالوج فقط —
  المحاكي لا يكتب عربيًا؛ الطريقة: انسخ نصًا عربيًا من داخل التطبيق والصقه).
* تقليب صفحات المصحف المصوّر بسحبة سريعة جدًا لم ينجح على المحاكي (500 مللي
  ثانية نجحت، 120 لم تنجح). غير مؤكَّد بالإصبع على جهاز حقيقي.

## قواعد لا تُكسر

* بلغ عن الكوتة الحية قبل أي مرحلة طويلة، ولا تبدأ مرحلة لا تكفيها.
* لا تنشر إصدارًا إلا بأمره، وابنِه بـ `build_github_release.bat` وحده.
* لا تُركّب خريطة تظليل لم ترها بعينك على صورة الصفحة.
* «لا تظليل» أفضل من «تظليل غلط» — الصفحة التي تفشل تبقى بلا تظليل.
