# NEXT SESSION — رفيق الدرب

الملخّص الطويل الذي يشير إليه `NEXT_PROMPT.md` (وهو النصّ الذي يلصقه المالك
في بداية الجلسة؛ هذا الملف هو الخلفية، وذاك هو المُلصَق).

حُرِّر **2026-09-21** بعد تحقّق مباشر — لا من الذاكرة:

| | |
|---|---|
| `flutter analyze lib test` | **No issues found** (24.4 ث) |
| `flutter test` | **456 نجحت، 2 تخطّي** |
| المحتوى المستضاف | **10/10** ردّت 206 بنوع المحتوى الصحيح، بـ User-Agent (مصيدة #19) |
| اللغات | 7 × **1,684** مفتاحًا، متطابقة تمامًا |
| `hadith.db` | 109,731,840 B · **67,153** حديثًا · **45,219** مخرَّجًا |
| `quran_local.db` | 5,640,192 B · **6,236** آية |
| `azkar.db` | 49,152 B |
| المكتبة | **214** كتابًا في `book_catalog.dart` |
| المصاحف المصوّرة | طبعة واحدة: `madinah_qc` |
| الـ APK | **260,277,487** B، ثلاث معماريات، `minSdk` 24 |
| النسخة | `3.52.0+54` |

الحالة: `master` عند `f6b71416`، الشجرة نظيفة ومدفوعة. **إصداران منشوران**:
`v3.52.0` (الأحدث) و`v3.51.0` (نقطة الرجوع، بطلب صريح ألّا تُحذف).

---

## ما طلبه المالك في آخر رسالة — ثلاثة أشياء

النصّ الكامل، والخطوات، والقياسات التي أغنته عن إعادتها، كلها في
**`NEXT_PROMPT.md`**. هنا الخلفية التي لا تتّسع له:

### ١) العمرة قسم مستقلّ

`hajj_guide.dart` يبني المسارين من كتاب واحد — «الإيضاح في مناسك الحج
والعمرة» للنووي (الشاملة 96232)، معرّفه `al_idah_fi_manasik_al_hajj_wal_umrah`.
`HajjStep` شريحةُ صفحات/فقرات في الكتاب، و`tracks` تقرّر في أي تبويب تظهر،
و`rite` هو ما يضع الخريطة أو العدّاد عليها (`null` = لا رسم).

الجلسة السابقة غيّرت ثلاثة أسطر فقط: `umrah` صار `{HajjTrack.umrah}`،
و`child` صار حجًّا فقط، و`TawafCounter` صار يدور في الوضع المصغَّر. أما
الفصول التسعة `_both` فهي متن الحج نفسه — ولهذا يفتح «المواقيت» في تبويب
العمرة على «في ميقات الحج». المطلوب قصٌّ على مستوى الفقرة، أو مصدر ثانٍ
موثَّق، أو الاعتراف بأن الكتاب لا يكفي. **لا اختراع نصّ.**

وفي `HANDOVER.md` كان مكتوبًا أن المالك «اختار إبقاء وصايا للحاج على
المسارين» — **هذا غير صحيح وقد صُحِّح**؛ لم يُسأل، وهو قال إن التبويبين
الأخيرين غير مناسبين.

### ٢) تدقيق المصحف النصّي

القياسات كاملة في `NEXT_PROMPT.md`. خلاصتها: **النصّ سليم والخطّ سليم**،
والمشكلة أن علامة الوقف مكتوبة كلمةً مستقلّة بين مسافتين في نصّ تنزيل
العثماني — **4,578 علامة في 2,719 آية من 6,236** — فيوزّعها ضبطُ السطور
كأنها كلمة. الإصلاح طباعي لا نصّي. `text_uthmani` لا يُمسّ (§1.2).

الملفات: `mushaf_text_page.dart` (981 سطرًا)، `mushaf_ayah_row.dart`،
`data/text_layout_provider.dart`. الخط `assets/fonts/AmiriQuran-Regular.ttf`
= Amiri Quran 1.003، 1,446 محرفًا، GPOS وGSUB سليمان، وكل علامات
`U+06D6`–`U+06ED` في الـ cmap.

### ٣) شرح للمقدمة الجزرية

مرشّحان في الفهرس المحلي: **21580** «فتح رب البرية شرح المقدمة الجزرية»
و**17065** «الروضة الندية شرح متن الجزرية». **لم يُتحقَّق من مؤلِّفيهما ولا
حقوقهما** — تلك أول خطوة، و`CONTENT-LICENSES.md` هو السجلّ الذي تُقيَّد فيه.

---

## ما زال مفتوحًا من جولات سابقة

* **الأذان**: لا يتوقّف من الإشعار، ويتكرّر بعد إطفائه. **لم يُشخَّص
  إطلاقًا** — أُبلِغ عنه مرارًا ولم يُفتح له ملف بعد. هذا أقدم عيب مفتوح
  في القائمة. `NotificationRouter` (مصيدة #31) وخدمات `lib/core/services/`
  هي المدخل، و`adb shell cmd alarm set-time` (مصيدة #28) يُعيد إنتاجه في
  دقيقتين بدل انتظار الفجر.
* **التوتوريال** خارج سلسلة التشغيل الأول؛ مكانه «المزيد» ← «الأدوات».
* **«وصايا للحاج»** على مسار العمرة — قرار المالك.

## نقطة الرجوع

| | |
|---|---|
| الفرع | `known-good/v3.51.0` — لا يتحرّك |
| الوسوم | `backup-2026-09-21c`، `v3.51.0` |
| الحزمة | `../Rafeeq-Backups/Rafeeq-Al-Darb-2026-09-21.bundle` — 693,194,105 B، تاريخ كامل، مُتحقَّق منها |
| الشرح | `RESTORE.md` |

**مفتاح التوقيع في `../Rafeeq-Keys/` خارج المستودع ولا تغطيه أي نسخة
احتياطية.** إن ضاع فلا تحديث يُثبَّت فوق التطبيق إلا بإلغاء تثبيته — وهو
يعني ضياع مئات الميجابايت من المحتوى المنزَّل على جهاز المالك.

## الكوتة

عند كتابة هذا: **97٪ من الحد الأسبوعي مستهلك**، يتجدّد 2026-09-21 الساعة
22:00 UTC؛ الحد الخمس-ساعي 12٪. اقرأ الرقم حيًّا في أول ردّ (§2.0) ولا تبدأ
مرحلة لا يكفيها ما تبقّى.
