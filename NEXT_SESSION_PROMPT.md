# برومبت الجلسة الجاية — رفيق الدرب (Rafiq Al-Darb)

انسخ ده كله وابعته للـ agent اللي هيكمّل.

---

مشروع **Rafiq Al-Darb** — تطبيق إسلامي Flutter في `E:\My Projects\Rafiq-Al-Darb`.
متفترضش أي حاجة عن الحالة من الرسالة دي — استنتج كل حاجة من الريبو.

## ORIENT الأول (من PowerShell/cmd، مش Git Bash)

```
git -C "E:\My Projects\Rafiq-Al-Darb" log --oneline -25
git -C "E:\My Projects\Rafiq-Al-Darb" status --short
.\cp.bat /s
```

بعدين اقرأ كامل بالترتيب:

1. **PHASE3.md** — **ابدأ هنا، ده أهم ملف دلوقتي وأكبرهم.** فيه جدولين
   حالة (P3‑1..P3‑19 فوق، وP3‑20..P3‑37 في آخر الملف تحت "PHASE 3, ROUND
   2"). علّم أي حاجة تخلصها `✅` واكتب تفاصيلها في القسم بتاعها.
2. **PHASE3_FEEDBACK.md** — النص الحرفي الأصلي لأول رسالة فيدباك.
3. `design_refs/` — صور مرجعية (`ref_tasbeeh.jpg`, `ref_azkar_hub.jpg`,
   `ref_home.jpg`, `ref_icon_installed.jpg`) + **فيديو حقيقي لنسخة قديمة
   شغالة من التطبيق نفسه** (`old_app_video.mp4` + 14 فريم مستخرج في
   `old_app_frames/`) — استخدمه كمرجع دقيق لتصميم شاشة splash والمصحف
   الأولي وكارت الصلاة (P3‑20/21/22). **مهم:** اقرأ التحذير في PHASE3.md
   عن شاشة اختيار المصحف في الفيديو (17 مصحف) — أدلة قوية إنها من QuranFlash
   الممنوع، **ممنوع تعيدها أو تجيب صور أغلفة منها**.
4. **HANDOVER.md** — بلوك "Current work in progress" (بيتحدّث لوحده من
   `cp.bat`). §3 قواعد ملزمة، §5 قرارات ممنوع تلغيها.
5. **PHASE2.md** — Phase 2 خلصت بالكامل تقريبًا، اقراه للسياق التاريخي بس.
6. **HOSTING.md** — استضافة R2. §7 فيها bucket قديم ملوّث لسه محتاج قرار
   الأونر (`rafeeq-aldarb-data`، 10.66 جيجا) + توكن لازم rotate.
7. **ARCHITECTURE.md** — خريطة الكود.

## الحالة (اتأكد من git)

- **Phase 1 + Phase 2** ✅ خلصت كلها **إلا** P2‑7 (محتاج موبايل حقيقي —
  الأونر بيستخدم Claude Code Remote من موبايله، مش موصّل USB بالجهاز اللي
  بيشتغل عليه الـagent) وP2‑9 (محتاج keystore من الأونر).
- **Phase 3** 🔶 **شغالة دلوقتي، ضخمة، مقسّمة لـ37+ بند في PHASE3.md.**
  الجدول في PHASE3.md هو المرجع الوحيد الصحيح لحالة كل بند — ملخص عام
  وقت كتابة البرومبت ده (بعد جلسات كتير كملت فيها ~20 بند، كلها اتعملت
  لايف-فيريفاي على المحاكي مش مجرد بناء):
  - **⚠️ ملحوظة مهمة:** PHASE3.md كان اتقطع/فقد جزء منه (P3‑1..19) بسبب
    باج في سكريبت بايثون في جلسة سابقة — **اتصلح بالكامل** من git history
    (commit `5048241`) وموثّق في أول الملف. لو لقيت حاجة غريبة في الفورمات
    قارنها بالـgit log، مش المفروض يكون فيه فقدان بيانات تاني.
  - ✅ **خلص ومتأكد منه على المحاكي فعليًا** (مش مجرد analyze): الأيقونة
    (الأونر أكّدها — P3‑23)، كارت الختمة، تاب المسبحة، الأذكار (شبكة
    P3‑11، الأونر أكّد الشكل الجديد وصله — P3‑30)، باجين بحث عربي
    حقيقيين، +3 كتب نص جديدة، كارت الرئيسية العلوي، كارت الصلاة التفاعلي
    (الجزء اللي مش محتاج GPS — الجزء اللي محتاج GPS **مش متأكد منه بصريًا**
    بسبب مشكلة محاكي، شوف P3‑22)، **قارئ الكتب النصي كامل — تمرير + شريط
    قفز سريع (نص P3‑29) + التصميم البصري بستايل الشاملة بألوان تطبيقنا
    (بصري P3‑29، آخر حاجة اتعملت — توولبار ٦ أيقونات بكابشن: حجم الخط/لون
    الخط[جديد]/التشكيل[جديد]/بحث/الفهرس/إشارات مرجعية، خلفية ورقية بلون
    التطبيق، الستة أزرار كلهم متجربين لايف على المحاكي)**، محاذاة رقم
    الآية P3‑32، زرار تشغيل/إيقاف واحد للآية P3‑35، حديث اليوم P3‑36،
    زرار إلغاء تحميل P3‑24، تنقّل قسم التنزيلات P3‑25، اسم التطبيق/لغة
    الجهاز P3‑37، تحميل التلاوة كاملة P3‑27، قايمة تفسير واحدة بدل
    المقارنة P3‑33، **تاب "الصلاة" جديد بالكامل + بوصلة قبلة حقيقية
    P3‑16**، شريط قفز السور في المصحف P3‑8، تصميم توولبار المصحف P3‑34
    (ودلوقتي الويدجت بتاعه `ToolbarAction` بقى مشترك مع قارئ الكتب في
    `core/widgets/toolbar_action.dart`)، **splash screen جديد بالكامل
    P3‑20** (لاتيس هندسي متحرك + badge دائري متوهج بأيقونتنا الحقيقية، مش
    الأيقونة القديمة)، **onboarding أول تشغيل P3‑21** (اختيار من الـ٥
    مصاحف الحقيقيين + تحميل حقيقي، وتحميل تلاوة كاملة "أساسي" زي ما طلب
    الأونر — من غير كتالوج الـ١٧ مصحف الممنوع)، **ثيم RGB معاد تصميمه
    P3‑3** (أهدأ وأغمق قريب من `ref_tasbeeh.jpg`، حلقات توهج بدل الكتل
    الملونة الكبيرة، نقط نجوم متناثرة بدل الشبكة الكثيفة)، **كارت الحديث
    المزخرف + كارت "متابعة القراءة" الجديد P3‑4** (تفاصيل في البند 5 تحت).
    + **باج خطير في التفسير اتصلح بالكامل P3‑9** (تفاصيل تحت).
  - 🔶 **اتحقق منه بدون كود جديد** (كان شغال أصلاً أو مش قابل للتكرار):
    pinch-zoom في المصحف النصي وقارئ الكتب (P3‑8/P3‑15)، سرعة قارئ الكتب
    (P3‑15 — مترجعش مع كتاب حقيقي 15.8MB)، كاش صفحات المصحف (P3‑8).
  - ❌ **محتاج جهاز حقيقي حصريًا:** زراير كتم/إيقاف الأذان، الشاشة الكاملة
    فعليًا، الإشعار الثابت لوقت الصلاة (كل التحقيقات الكودية خلصت،
    اتشال كود ميت، اتضاف كارت إذن Android 14 — التفاصيل في P3‑7/13/19).
  - ⏳ **لسه ما اتلمستش خالص:** تصميم كارت الختمة
    (P3‑6 — مفيش صورة مرجعية للتطبيق "ختمة" اللي طلبه الأونر، لكن فيه
    تصميم بديل قريب في `ref_home.jpg`)، قسم تفسير ٢٠ مصدر (P3‑31 — محتاج بحث تراخيص أول)، صور أغلفة
    المصاحف (P3‑28 — محتاج بحث تراخيص أول) — كلها مفصّلة في PHASE3.md.

## ⚠️ أهم حاجة حصلت آخر جلستين

1. **باج خطير في التفسير اتصلح بالكامل (P3‑9)** — مش مجرد باج تاني في
   القايمة — كان فعليًا **معظم القرآن (~٨٣٪) بيعرض تفسير آية غلط تمامًا**
   (آية سابقة، مش الآية اللي فاتحها المستخدم)، وكمان مصدر "تفسير الجلالين"
   مكنش جلالين أصلاً — كان تفسير ابن كثير الحقيقي بس بلابل غلط. اتصلح
   بإعادة بناء قاعدة بيانات `quran_sciences.db` كاملة (سكريبت جديد
   `fetch_tafsirs_complete.py` + `build_sciences_db.py` اتعدّل)، واتأكد
   منه لايف على المحاكي (آية الكرسي 2:255 بترجع صح دلوقتي). التفاصيل
   الكاملة في PHASE3.md قسم P3‑9 — لو حسيت في حاجة غريبة تانية في التفسير،
   ده أول مكان تدور فيه.
2. **قارئ الكتب النصي خلص بالكامل (P3‑29) — التصميم البصري كمان** —
   الأونر بعت مرجع الشاملة (`design_refs/ref_shamela_reader.jpg`) مع طلب
   "shamela style but follow our theme coloring". اتبنى توولبار ٦ أيقونات
   بكابشن (زي توولبار المصحف P3‑34، دلوقتي مشترك بينهم في
   `core/widgets/toolbar_action.dart`)، كل أيقونة وارها فيتشر حقيقي —
   بما فيهم لون الخط (جديد، ٣ اختيارات) والتشكيل (جديد، `stripTashkeelForDisplay`
   في `arabic_normalize.dart`، آيات القرآن المقتبسة داخل الكتب بتفضل
   مشكّلة كاملة دايمًا). خلفية القراءة بقت بلون التطبيق الورقي
   (`AppColors.paper`/`nightSurface`) بدل الرمادي القديم. "الجزء" اتشالت
   عمدًا — مفيش بيانات حقيقية للجزء في نموذج الكتاب، ومفيش داعي نخترعها.
   **باج فلاتر حقيقي اتلقط لايف مش بالتحليل الساكن:** `actions: const []`
   في الـ `AppBar` مبيمنعش زرار الـ endDrawer التلقائي (شرط فلاتر الفعلي
   هو `isNotEmpty` مش `!= null`) — الحل الصح `automaticallyImplyActions: false`،
   اتأكد منه لايف بعد كده. كل الستة أزرار توولبار اتجربوا لايف على
   المحاكي واحد واحد.
3. **Splash + Onboarding خلصوا بالكامل (P3‑20 + P3‑21)، لايف-فيريفاي كامل
   الدورة** — splash جديد (لاتيس هندسي متحرك + badge دائري متوهج
   بأيقونتنا مش الأيقونة القديمة)، بعده onboarding أول تشغيل (اختيار من
   الـ٥ مصاحف الحقيقيين + تحميل حقيقي بيستخدم نفس آلية `MushafPageService`
   اللي في شاشة التنزيلات، + تحميل تلاوة كاملة "أساسي" بيستخدم نفس آلية
   P3‑27 — الاتنين اتسحبوا لويدجتس مشتركة عامة). **كتالوج الـ١٧ مصحف
   الممنوع (QuranFlash) اتعمّد عدم بناؤه.** باجين حقيقيين اتلقطوا لايف:
   (1) تعليق XML فيه "--" كسّر بناء Gradle بالكامل (AAPT2 مبيقبلش double-
   hyphen جوه تعليقات XML)، (2) `Directionality(rtl)` متثبّت غلط في شاشة
   الـonboarding كسّر ترتيب النص مع اللغة الإنجليزية (اتأكد لايف: نقطة
   آخر الجملة بتتقفز لأول السطر، و"0 / 604 pages saved" بيطلع معكوس) —
   الحل: تشيل الـ`Directionality` المتثبّت تمامًا وسيبها تتبع لغة الجهاز
   زي كل شاشة تانية في التطبيق. اتأكد لايف كمان إن الـonboarding بيتخطى
   نفسه صح لما تعمل force-stop وتفتح التطبيق تاني (مش هيظهر تاني).
4. **⚠️ باج Crash خطير في batch10 اتصلح في batch11 (لقّطته أثناء شغل P3‑3)**
   — أي تبديل ثيم (Settings > Theme، أي خيار) بعد ما P3‑20/21 اتشحنوا كان
   بيعمل crash فوري برسالة "Looking up a deactivated widget's ancestor is
   unsafe". السبب: `SplashScreen._proceed()` و`OnboardingScreen._finish()`
   الاتنين كانوا بيقروا `context.locale.languageCode` **جوه** الـ`builder:`
   بتاع `Navigator.pushReplacement` بدل قبله — والـclosure ده بيتنفّذ
   متأخر، بعد ما الشاشة القديمة ممكن تكون بدأت تتفكك، فالقراءة تفشل. اتلقط
   عن طريق `flutter run` المباشر (مش adb logcat العادي، ده مكنش بيلقط
   الـstack trace) — لو حصل crash تاني غريب، جرب نفس الطريقة دي أول حاجة.
   الحل: اقرأ الـlocale في متغيّر محلي **قبل** `pushReplacement`، مش جوه
   الـ`builder:`. اتأكد لايف إن التبديل بقى شغال تمام بعد الإصلاح.
5. **P3‑4 خلص بالكامل (كارت الحديث المزخرف + كارت "متابعة القراءة")** —
   الأونر بعت مرجعين جداد (`design_refs/round2_2026-09-04/`) مع "use pics
   in new folder and proceed all". `DailyHadithCard` بقى جوه إطار مزخرف
   (حواف ذهبية + زوايا بأقواس مزخرفة + نجوم صغيرة حوالين العنوان) بألوان
   تطبيقنا (نيفي/ذهبي) مش لون المرجع الأسود-الأخضر. كارت جديد
   `ContinueReadingCard` على الرئيسية — بيانات حقيقية بس (آخر صفحة اتقرت
   فعليًا، لو محدش قرا حاجة لسه الكارت مبيظهرش خالص، مفيش بيانات وهمية).
   **باج reactivity حقيقي اتلقط لايف:** قراءة `SharedPreferences` مباشرة
   في كل build مبتحدّثش لما ترجع للرئيسية من تاب تاني، لأن `AppShell`
   بيستخدم `IndexedStack` وبيحافظ على كل التابات "mounted" من غير rebuild
   — الحل: `StateNotifierProvider` حقيقي (`quranLastPageProvider`) بدل
   القراءة المباشرة. اتأكد لايف كامل (قرا صفحة، رجع للرئيسية، الكارت ظهر
   فورًا من غير إعادة تشغيل التطبيق، ودوس عليه رجع لنفس الصفحة بالظبط).
   **ملحوظة تقنية:** المحاكي كان اتعطّل (13.5 ساعة تشغيل متواصل، load
   average 15+) في نص الاختبار — اتقتل وأتعاد تشغيله من جديد زي الإجراء
   المعروف قبل كده، مش باج في الكود.
6. **P3‑38 — راوند فيدباك تالت خلص بالكامل** — الأونر بعت صور/فيديو جديدة
   (`design_refs/round2_2026-09-04/`) + صورة أيقونة نضيفة بعد ما اتسأل.
   **أيقونة التطبيق اتعاد تصميمها** — هلال بلمعة "cosmic swirl" زرقاء-
   دهبية وكتاب أزرق (مش ذهبي زي قبل) — بنفس الـpipeline اللي بنى بيها
   P3‑1 الأول (SVG + headless Chrome + `flutter_launcher_icons`)، مش قص
   بيكسل من الصورة المرجعية (كانت صغيرة أوي 173×228). **باج XML حقيقي
   اتلقط لايف مرتين** — `icon_fg.svg` فيه تعليق قديم فيه "--" (double-
   hyphen) وده ممنوع جوه تعليقات XML، فـ Chrome كان بيكتب صفحة خطأ HTML
   كـ PNG من غير أي error واضح — لازم تتأكد بعينك من أي أيقونة اتولدت
   بالـpipeline ده. **الأذكار والمسبحة بقوا تابين منفصلين في الشريط
   السفلي (٧ تابات بدل ٦)** — زي الصور الحقيقية اللي بعتها بالظبط
   (`ref_azkar_hub_v2.jpg`)، مش تاب واحد فيه sub-tabs. `TasbeehScreen`
   جديد، `AzkarScreen` اتقلّم، `AzkarSettingsButton` مشترك اتستخرج.
   اتأكد لايف كامل: الأيقونة الجديدة ظاهرة في الـsplash الأصلي وشاشتنا
   المتحركة والـlauncher، والسبعة تابات شغالين صح من غير overflow.

## APK للتجربة (آخر نسخة، فيها كل حاجة اتعملت)

`https://github.com/tito423/rafeeq-api/releases/download/phase3-batch13-2026-09-04/app-debug.apk`

(batch 13 فيها P3‑38 كامل (أيقونة جديدة + فصل الأذكار/المسبحة) + كل حاجة
في batch12/11/10/9/8 — تفاصيل batch11 (إصلاح crash تبديل الثيم + P3‑3 ثيم
RGB) لسه سارية، batch10 لسه فيه الـcrash القديم فاتجنّبه. **P3‑8 شريط قفز
السور في المصحف** — شريط أفقي فوق أزرار الصفحات فيه كل الـ114 سورة،
السورة الحالية متعلّمة وبتتحرك لوحدها مع القراءة، دوس على أي سورة تروح
لها فورًا. وتحقيق P3‑15 (بدون كود) إن سرعة قارئ الكتب متبتش تتكرر مع كتاب
حقيقي، وإن السكرول والـpinch-zoom موجودين أصلاً في المكتبة المستخدمة.
التفاصيل الكاملة في PHASE3.md.)

## قواعد ملزمة إضافية اتعلمناها هذه الجلسة

- **لو لقيت credentials في رسالة من الأونر — خزّنها فورًا في
  `scripts/.env` (متجاهَل من git) ومتطبعهاش تاني.** حصل مرتين قبل كده،
  موثّق في `HOSTING.md` §7.
- **ممنوع تستخدم أي محتوى (تصاميم/صور أغلفة/كاتالوجات) من فيديو أو مرجع
  قديم من غير ما تتأكد مصدره** — الفيديو اللي في `design_refs/` فيه جزء
  (كاتالوج 17 مصحف) طلع دليل قوي إنه من QuranFlash الممنوع، رغم إن باقي
  الفيديو (splash, home) نظيف تمامًا. راجع كل مرجع بصري بنفس الحذر.
- المحاكي الحالي عنده مشاكل معروفة: (1) مساحة تخزين ضيقة (~600MB) —
  محتاج `adb uninstall com.tito.rafeeq_aldarb` بدل `install -r` أحيانًا،
  (2) `adb emu geo fix` مش بيوصل فعليًا للـ Fused Location Provider على
  الـAVD ده (Play Store image) — لو محتاج GPS حقيقي للتحقق، جرّب Extended
  Controls من الـGUI مش الـcommand line، أو سيبها للجهاز الحقيقي، (3) بعد
  شغل طويل (شفت `adb shell uptime` بيقول 12+ ساعة تشغيل متواصل، load
  average ~6-8) المحاكي ممكن يعمل ANR حقيقي ("isn't responding") — مش باج
  في الكود، مجرد المحاكي مرهق. لو حصل، جرّب تعيد تشغيل المحاكي نفسه أول
  حاجة قبل ما تفترض إن فيه باج.

## Checkpoint

بعد كل تعديل مهم: `.\cp.bat "اللي عملته"`. لو الكوتة قربت تخلص: حدّث
`PHASE3.md` (الجدول + التفاصيل) و`NEXT_SESSION_PROMPT.md` ده يدويًا
(**مش بيتحدّث أوتوماتيك**)، وابني وارفع APK جديد لو فيه تغييرات حقيقية.

## التحقق

`emulator-5554` غالباً شغّال. package = `com.tito.rafeeq_aldarb`.
`gh` مسجَّل دخول كـ`tito423`. R2 credentials في `scripts/.env`.
