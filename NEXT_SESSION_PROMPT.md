# رفيق الدرب — الحالة عند التسليم (2026-09-22، ليلاً)

## أين نحن
`master` = `c3b6b51e` مدفوع. **لا إصدار منذ v3.55.0**، والوسم يشير إلى
`bb476739` وليس إلى `HEAD`. كل شغل اليوم على المحاكي فقط ولم يصل هاتف المالك.

## ما أُنجز اليوم وتم التحقق منه على `emulator-5554`
1. **المكتبة — المرحلة ١ اكتملت.** 18 كتابًا مبنيًا من الشاملة رُفعت وتُحقق من
   كل واحد على الرابط العام (200، حجم = الكتالوج، بصمة gzip، بلا
   `Content-Encoding`)، وخرج متنا «الورقات» و«الآجرومية» لصالح شرحيهما →
   **229 كتابًا**. «الروض المربع» من طبعة ركائز 147658 بعد رفض طبعة 1679
   (بطاقتها تنص على حاشية العثيمين وتعليقات السعدي). «توضيح المقاصد» أُسقط.
   «إحكام الأحكام» و«تنوير الحوالك» كتب حديث → أصول مدمجة في التطبيق.
2. **فحص المكتبة كاملة كتابًا كتابًا:** الـ229 نزلت من الرابط العام وفُتحت
   بمحلّل التطبيق نفسه (`BookText.fromBytes`) → 229/229 تفتح، بلا صفحات
   فارغة، وكل مدخل فهرس ضمن النطاق. المتروك عمدًا: «الإيضاح» فيه 38 صفحة
   سطرها الوحيد نقاط (الورقة في المطبوع كلها حاشية) — مرتبط بشاشة الحج
   بأرقام صفحاته فلم يُمس.
3. **قاعدة الحواشي المسمّاة** طُبّقت بعد قراءة كل ذكر بسياقه في الـ213 كتابًا:
   111 حكمًا للألباني بقيت (تخريج)، وكل «بن عبد الوهاب» رواة قدامى، و«العثيمين»
   في غير موضعه هو المؤرخ عبد الرحمن بن سليمان. حُذف كلام محققين فقط في ثلاثة
   كتب — `scripts/strip_named_notes.py`.
4. **الختمة**: «تراجع» لم يعد SnackBar، بل سطر داخل البطاقة يختفي بعد 6 ثوانٍ.
5. **الحج**: قسم مطوي «في المذاهب الأربعة» من الجزيري تحت كل خطوة —
   `scripts/build_hajj_madhahib.py`، 49 حاشية، صفر غير مربوطة، 147 قولًا.
6. **`lib/features/hifz`** — قسم التحفيظ والتسميع (المرحلة الأولى، بلا تعرّف
   صوتي): سلّم مراجعة 0/3/7/16/35 يومًا، إخفاء الكلمات من آخر الآية، تكرار
   التلاوة 1/3/5/10.
7. **اللقطات** أُعيد تصويرها وبُني منها معرض المميزات داخل التطبيق؛ لقطة
   التحفيظ حلّت محل لقطة الأذكار في الثمانية.
8. **التسميع الآلي — قياس فقط، لم يُبنَ**: `scripts/measure_quran_asr.py`
   (+`--hard`). النتائج في رأس السكربت وفي HANDOVER.

## ما لم يُنجز / غير متحقق منه
* **لا إصدار**، ولا شيء من هذا على هاتف المالك.
* **اللقطات الخام لثلاث شاشات لم يُعد تصويرها** (الأذان، الحديث، درس
  التجويد) — ما زالت من جلسة 2026-09-21.
* **التسميع على الجهاز لم يُجرَّب**: الخطر المفتوح أن `sherpa_onnx` يتوقع
  تصدير whisper بسكربته الخاص، والملفات المتاحة مصدَّرة بـoptimum.
* لم يُقس: معالج الهاتف، صوت متعلّم مبتدئ، والتجويد (خطأ المد ليس كلمة خطأ).
