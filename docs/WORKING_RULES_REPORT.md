# تقرير قواعد العمل — رفيق الدرب (2026-09-24)

المصدران: `CLAUDE.md` (العقد الملزم، الأقسام 0–7 و49 مصيدة) و37 ملف ذاكرة.
عمود «الرأي»: **يبقى** / **يُعدَّل** (مع السبب). القرار للمالك.

## أ. المبادئ الأساسية (CLAUDE.md §1)
| القاعدة | الرأي |
|---|---|
| لا شيء وهمي أبدًا — لا بيانات تجريبية ولا روابط مؤقتة، وكل عنصر في كتالوج يُفحص على الخادم قبل إضافته | يبقى |
| المحتوى الديني: كل حكم حديث باسم من حكم به، و«غير مُحكَم» جواب صادق، ولا تعديل لنص آية أو حديث، وكل مصدر في شاشة المصادر | يبقى |
| لا شيء «تم» قبل أن يُرى على المحاكي أو هاتف | يبقى — مع استثناء مكتوب لما لا يُجرَّب على المحاكي (تسجيل دخول Google، هاتفك): يُذكر صراحة «لم يُجرَّب» |
| اقرأ البيانات الحقيقية قبل كتابة الكود الذي يحللها | يبقى |
| الصدق في التقارير، وتصحيح الادعاء الخاطئ علنًا | يبقى |
| الأسرار في `scripts/.env` فقط | يبقى — ويُضاف: مفتاح R2 موجود في تاريخ الـ repo ويجب تغييره |
| **جديد §1.7:** كل معلومة تُفحص لحظة السؤال | يبقى (أُضيف اليوم) |
| **جديد §1.8:** المكتبات والبيئة على أحدث إصدار مستقر ثبت أنه يعمل | يبقى (أُضيف اليوم) |

## ب. طريقة العمل (§2 و§5 والذاكرة)
| القاعدة | الرأي |
|---|---|
| قراءة الكوتة حيّة أول الجلسة وقبل كل مرحلة طويلة | **يُعدَّل:** تتعارض مع ذاكرة «هو يراقب الكوتة لا أنا — استمر ما لم يقل توقف». اقتراح: أقرأها وأبلغك بالرقم فقط، ولا أوقف العمل بسببها إلا إن طلبت |
| فحص Remote Control أول رد | يبقى |
| checkpoint دائم بـ `cp.bat` | **يُعدَّل:** `cp.bat` يضيف كل الملفات غير المتتبَّعة (ضمّ ملفات سجلات مؤقتة اليوم وأُزيلت). اقتراح: يضيف الملفات المتتبَّعة فقط |
| تعريف «تم»: analyze + test + محاكي + HANDOVER + commit/push | يبقى — ويُضاف «التحقق من كود خروج الاختبارات» (مرّ commit باختبار فاشل اليوم وصُحح) |
| إصدار واحد فقط، يُحذف السابق ووسمه | **يُعدَّل:** يُستثنى صراحة v3.51.0 (نقطة الرجوع) وإصدارات `content-*` (النسخة الاحتياطية للمحتوى — حذفها يكسر الاحتياطي) |
| الإصدار قرارك (ذاكرة) مقابل «لا تنهِ جلسة بعمل منتهٍ غير مُصدَر» (ذاكرة أخرى) | **يُعدَّل:** متعارضتان. اقتراح: لا إصدار إلا بطلبك، وإن قاربت الجلسة نهايتها أسألك |
| رقم الإصدار: الوسم هو المرجع و«سطر v2.1.x» (ذاكرة `release-versioning`) | **يُعدَّل — قديمة:** الواقع اليوم رفع `pubspec` و`AboutScreen` مع كل إصدار (§2.3). تُحدَّث الذاكرة |
| رسائل commit بالإنجليزية تشرح السبب والدليل | يبقى |
| الرد بالعربية، والكود والتعليقات بالإنجليزية، ونصوص التطبيق فصحى | يبقى |
| الاختصار، إنهاء ما في اليد قبل فتح جديد، «امشي برايك»، استخدام أصولك كما هي | يبقى |
| الأوقات بتوقيت دبي؛ لا وصول USB لهاتفك وقت العمل؛ لا تقترح adb على هاتفك | يبقى |
| البرومبت التالي فقط عند «جهّز الدنيا»، في بلوك md وفي `NEXT_PROMPT.md` | يبقى |

## ج. قواعد المنتج (الذاكرة)
| القاعدة | الرأي |
|---|---|
| أي أندرويد 7+ وأي معمارية، لا تضحية بالتوافق مقابل الحجم | يبقى — اقتراح اختياري: نشر نسخ مقسّمة حسب المعمارية **بجانب** النسخة الكاملة لتقليل التنزيل (حوالي 90 ميجا أقل) |
| خمسة مصادر لكل ما يُنزَّل، ولا تخطٍّ لآية | يبقى — الواقع اليوم مصدران على الإنترنت لأغلب المحتوى؛ الوصول لخمسة يحتاج دومين/Hugging Face/archive.org |
| كل وسائط لها مشغّل ظاهر (عدا الافتتاحية) | يبقى |
| لا خيار نصف شغّال في الواجهة؛ لا تسليم ناقص | يبقى |
| الأذان يعرض نصًا فقط مع توقيت متزامن مُتحقق | يبقى |
| صوت قارئ الكتب رجل مع تفخيم لفظ الجلالة | يبقى |
| المصحف يُقلَّب من اليمين لليسار في كل اللغات | يبقى |
| «ادعم التطبيق» لا «تبرع»، ورابط PayPal في نسخة GitHub فقط | يبقى — نسخة Play ستحتاج Google Play Billing |
| سياسة المكتبة (لا متون مجردة، نسخة واحدة لكل كتاب، الخمسة المستبعدون) و`CONTENT-LICENSES.md` سجل المصدر | يبقى — وهو أساس قرار نسخة Play |
| المزامنة عبر الأجهزة والمنصات | يبقى |
| الجولة التعريفية تؤطر كل ميزة وحدها | يبقى |
| أمثلتك عيّنة: افحص البيانات كلها | يبقى |
| مصحف ورقي واحد بتظليل دقيق، وعمل التظليل الآلي مؤجّل | يبقى — ذاكرة `raster-mushaf-editions` («ست طبعات») **قديمة** وتُحدَّث |
| 45 لغة ترجمة | يبقى — فهرس الذاكرة يقول 47، والمقاس على R2 اليوم 45: يُصحَّح الفهرس |

## د. المصائد التقنية (§3، 49 مصيدة)
تبقى كلها — كل واحدة دفعنا ثمنها. **اقتراح:** نقلها من `CLAUDE.md` إلى ملف `TRAPS.md` مع سطر فهرس لكل واحدة، لأن `CLAUDE.md` يُحمَّل كاملًا في كل جلسة ويستهلك جزءًا كبيرًا من السياق؛ والمصائد الجديدة اليوم تُضاف: `errorStream` في just_audio 0.10، و`Accept-Encoding` عند نسخ ملفات R2 المضغوطة، وAGP 9 مع permission_handler 13.

## هـ. ما يحتاج قرارك
1. الكوتة: أبلغ فقط أم أوقف العمل؟
2. الإصدار آخر الجلسة: أسألك دائمًا؟
3. نقل المصائد إلى `TRAPS.md`؟
4. نسخ مقسّمة حسب المعمارية بجانب الكاملة؟
5. أحدّث الذاكرات القديمة (رقم الإصدار، عدد الطبعات، عدد اللغات) الآن؟

## القرارات (المالك: «اعمل الصح والافضل ليا وليك ولشغلنا» — 2026-09-24)
1. الكوتة: أُبلغ بالرقم ولا أوقف العمل إلا بطلبك — CLAUDE.md §2.0.
2. الإصدار: لا نشر إلا بطلبك، وأسألك إن انتهت الجلسة بعمل غير مُصدَر — §2.3، وذاكرتان متعارضتان دُمجتا.
3. استثناء دائم من حذف الإصدارات: v3.51.0 وإصدارات `content-*` — §2.3.
4. `cp.bat`: يضيف الملفات المتتبَّعة و`HANDOVER.md` فقط (`scripts/checkpoint.ps1`).
5. الذاكرات القديمة صُححت: رقم الإصدار، عدد الطبعات (واحدة)، عدد اللغات (45).
6. المصائد نُقلت إلى `TRAPS.md` (52 مع ثلاث جديدة)، وCLAUDE.md من 881 إلى 428 سطرًا مع فهرس سطر لكل مصيدة.
7. النسخ المقسّمة حسب المعمارية: في `PLAN.md` خيارًا غير مقرَّر، بلا رقم مزعوم حتى يُقاس.
