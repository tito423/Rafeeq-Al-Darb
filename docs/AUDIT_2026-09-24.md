# Audit — 2026-09-24 (report only; no code changed by this audit)

Scope asked by the owner: architecture, backend, frontend, database,
security. Done so far: security (secrets, manifest), backend (sync Worker),
downloads / request volume, library compatibility, every screen on
emulator-5554. NOT yet: database (migrations, indexes), architecture /
code structure, performance (memory, cold start, battery), accessibility.

## Findings, most serious first

| # | Area | Finding | Evidence | Fix (not done) |
|---|---|---|---|---|
| S1 | Security | R2 access key id + secret in the PUBLIC repo's git history | matched by value against scripts/.env; commits 3747f715, ecbb820d, eae5036d | **Owner: rotate the R2 token in Cloudflare**, update scripts/.env |
| D1 | Data loss | Signing out wipes ALL SharedPreferences, not just the account | `sync_service.dart:134` `await _prefs.clear(); // Optionally clear data on sign out?` | **FIXED dc271f8f** — clears only the synced keys/counters + queue |
| B1 | Backend | `/sync` has no size limit on `updates`/`counters` arrays or `value` length — one signed-in user can fill D1 | `sync_backend/src/index.ts` POST /sync | **FIXED + deployed** (version 48b3fa3a): body ≤ 1 MB (413), ≤ 100 updates / 1,000 counters, value ≤ 512 KB, only the app's synced keys (unknown ignored), integer increments ≤ 10⁷. Live: 401 without/with a fake token; caps NOT exercised live (needs a real Google token) |
| B2 | Backend | Token check calls Google `tokeninfo` on every request (a debugging endpoint, rate-limited) | `verifyGoogleToken` | verify the JWT signature locally against Google's JWKS, cache the keys |
| B3 | Backend | No rate limiting on any route | whole Worker | Cloudflare rate-limiting rule or a per-sub counter |
| B4 | Backend | `/review` accepts notes under any reviewer name, no auth, CORS `*` | review handler | a reviewer passphrase, or Cloudflare Access on the site |
| B5 | Backend | `CREATE TABLE IF NOT EXISTS review_notes` runs on every review request | review handler | **FIXED + deployed**: once per isolate. Live: reviewers 200, preflight 204, POST 200 |
| I1 | Infra | App content served from `r2.dev`: Cloudflare says rate-limited, development only, no cache | developers.cloudflare.com/r2/buckets/public-buckets; no CF-Cache-Status measured | custom domain (owner: «مش دلوقتي") |
| C1 | Content | Quote / new-Muslim backgrounds come from images.unsplash.com only | 13 URLs in lib/ | **FIXED**: 12 photos on R2 `images/backgrounds/` (sizes equal, 200) and GitHub content-mirror; chain R2 → GitHub → Unsplash. NOT seen on device yet |

## What checked out
- Worker checks `iss`, `aud` (the app's client id) and `exp`; every SQL is parameterised; a user can only write rows under the `sub` from his own token.
- Manifest: no cleartext traffic; exported components are only the launcher activity and audio_service's required media components.
- No secret in the published APKs (v3.59.0, v3.60.0 checked byte-wise).
- Downloads: local copy first; 8 concurrent / 4 per host; partial files detected (size / hash / temp+rename).
- 565 tests, analyze clean.

## Part 2 — database, architecture, performance, accessibility (measured 2026-09-24)

| # | Area | Finding | Evidence | Suggested fix |
|---|---|---|---|---|
| DB1 | Database | Of 5 on-device databases only the library DB has migrations (v3); the translation store, sync queue and the two opened by db_helper are `version: 1` with no `onUpgrade` — the first schema change there will need one written, or old installs break | `grep openDatabase/onUpgrade` | add an `onUpgrade` stub + a test when the next change comes |
| DB2 | Database | Keys are right: book_pages (book_id, page_index), verses (lang, ref), book_meta id are primary keys (implicit indexes). quran_local.db has no index, but 6,236 ayahs — scans are cheap | bundled DB census: hadith 4 idx, sciences 8, quran_local 0, azkar 0 | none needed now |
| AR1 | Architecture | 363 Dart files / 84,531 lines, 29 features, 136 test files (568 tests). Seven files sit at or over the 800-line ceiling (guarded by code_layout_test) | `find lib -name '*.dart'` | split quran_screen / book_text_reader / player_screen when next touched |
| AR2 | Architecture | `book_catalog.dart` is 4,791 lines of DATA in code (239 books); a JSON asset would be easier to audit and to generate | file size | move to assets/data/catalogs/books.json (the review site already derives it) |
| AR3 | Architecture | Two state styles side by side: Riverpod providers and hand-rolled singletons (`AyahAudioService.instance`, `QuranAudioPlayer.instance`, `DownloadManager.instance`) | code | acceptable; do not add a third |
| P1 | Performance | Cold start to first frame 2.05–2.49 s (3 runs, `am start -W`, emulator x86_64); home screen PSS 139.8 MB | measured | profile on the owner's phone before optimising |
| P2 | Performance | APK 265,144,900 B (three ABIs by the owner's rule) | release asset | content already moved to downloads; ABIs stay |
| A1 | Accessibility | Many icon-only buttons have no label for TalkBack (e.g. the ± buttons in «ضبط المواقيت والتاريخ», hifz icons, dedication counters) | uiautomator dumps during the screen walk showed clickable nodes with empty text/description | add `tooltip`/`Semantics(label:)` — a sweep, low risk |
