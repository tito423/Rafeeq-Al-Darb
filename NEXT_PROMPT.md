# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول (المنهج الملزم)، وبعده **`WORK_QUEUE.md`** — دي ملاحظات
صاحب التطبيق من استخدامه الفعلي ومنها بتشتغل — وبعدها `HANDOVER.md`.

**قبل ما تخطّط: اقرأ الكوتة (§2.0) وقول له الرقم.** لو الواجهة اللي انت فيها
مش بتوريه، اطلب منه يقراه من شاشته — ما تخمّنش. (اتطلب منه مرتين في السيشن
اللي فاتت وماردّش؛ اطلبه تاني مرة واحدة وكمّل.)

---

## الحالة عند التوقف — متحققة، مش من الذاكرة

- المُصدَر: **v3.16.0** على `32bc414`. التاج على `master` والـSHA بتاعه =
  `HEAD`، وإصدار واحد بس في المستودع.
- الـAPK المنشور **اتنزّل من جيت هب واتفحص**: `CN=Rafeeq Al-Darb` على أندرويد
  ٩ فما فوق، ومفتاح الديبج القديم تحتها (نَسَب التوقيع سليم).
  الحجم ٣٣٠٬١٩٤٬٤٩٤ بايت.
- `flutter analyze lib test` نضيف · `flutter test` **١٧٠ ناجحة**.
- تسعة مصاحف بقوا **ستة** — كل واحد فيهم عنده طبقة آيات.

**التوقيع:** جرادل بيوقّع ديبج بقصد. بعد `flutter build apk --release` لازم
`py -3 scripts/sign_release.py` — فخ ٤١.

---

## أول حاجة: تلاتة لسه محدّش شافها على جهاز

**ما تثقش فيهم. افتحهم بنفسك الأول.**

1. **التقفيلة الحقيقية.** إصلاح الطابور متحقق منه بـ٥ اختبارات ضد
   `MemoryTaskQueue` الحقيقي، وزرار «إصلاح التحميلات» اتشاف على الجهاز بيقول
   «تم استئناف 2 تحميل غير مكتمل» — **بس الحالة المتقفلة نفسها ماتعملتش على
   جهاز.** عشان تعملها: نزّل مصاحف كتير مع تلاوة كاملة لحد ما يقف، وبعدين
   اضغط إصلاح وشوف بيقول «حرّرت N مهمة عالقة» ولا لأ.
2. **الآية اللي بتتلى وهي أطول من الشاشة** (C4). محتاجة البقرة ٢٨٢ في وضع
   البطاقات والتلاوة شغالة. مفيش اختبار عليها كمان — اكتب واحد.
3. **الهاتف بتاعه.** كل اللي فوق اتشاف على إيموليتر x86.

---

## حاجات لازم تعرفها قبل ما تلمس حاجة

- **الـANR اللي ظهر مرة على نسخة الديبج** سببه من السجل:
  `executing service ...GeolocatorLocationService, waited 20030ms`. خدمة
  الموقع، مش شغل التنزيلات. لو ظهرت تاني على نسخة إصدار، دي بقت مسألة حقيقية.
- **فرع الوضع الأفقي في `MushafPageView._stage` بقى ميت** — `QuranScreen`
  بيقفل وضع المصحف على الطولي. متسايب بقصد ومكتوب عليه كده.
- **`purgeUnknownEditions` بيمسح فعلًا.** بيمسح مجلد صفحات أي مصحف مش موجود في
  الكتالوج. لو ضفت مصحف وشِلته بالغلط، صفحاته المنزّلة راحت.
- **الطابور بيحرق خانة لو النظام رفض مهمة** — ده عيب في المكتبة نفسها
  (`MemoryTaskQueue.advanceQueue`). التطبيق بيداويه بـ`enqueueErrors`
  و`unjamQueues`. **لو حدّثت `background_downloader` اتأكد إن العيب اتصلح قبل
  ما تشيل العلاج.**

---

## ما تعملهاش تاني

- `flutter build apk --release` بيقتل الإيموليتر (فخ ٢٤). ابنِ الأول وبعدين
  شغّل الجهاز.
- **الـheredoc بيبوّظ `\n` والـquotes (فخ ١١).** استخدم أداة Write.
- **فخ ٢٣:** الباك سلاش قبل `$` في دارت **escape** مش interpolation.
  `no_escaped_dollar_test.dart` بيكسر البناء على ده.
- `if (!snapshot.hasData)` بيرسم الفشل على إنه تحميل. استخدم `FutureView`.
- `X as int` على صف قاعدة بيانات ادعاء عن البيانات — شغّل
  `py -3 scripts/db_type_audit.py` بعد أي إعادة بناء.
- صفحة مدفوعة مش بتترسم تاني — لازم `Consumer` جوّاها.
- `adb shell input keyevent KEYCODE_BACK` بيطلّعك من التبويب كله.
- **ما تستنتجش سبب من قراءة كود من غير ما تقيسه على جهاز.** في السيشن دي
  اتقرا bytecode صح واتبنى عليه استنتاج غلط عن سبب توقف التلاوة، واتشال بعد
  ما القياس كذّبه. القراءة بتديك فرضية؛ الجهاز هو اللي بيقول.
