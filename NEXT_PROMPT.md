# كمّل رفيق الدرب من حيث وقفنا (2026-09-22، ليلاً)

اقرأ `CLAUDE.md` كاملًا (إلزامي)، ثم أعلى `HANDOVER.md`: سطر الحالة وكتل
«SESSION 2026-09-22». ردّ بالعربي. في أول رد: اقرأ الكوتة حيّة (§2.0) وتحقق من
Remote Control بـ `ListAgents` (§2.0b).

## أين وقفنا بالضبط
`master` = `c3b6b51e` مدفوع، `flutter analyze` نظيف، **499 اختبارًا تمر**.
**لا إصدار منذ v3.55.0** (الوسم = `bb476739` ≠ `HEAD`)، و`pubspec` ما زال
`3.55.0+57`. كل شغل أمس رُئي على `emulator-5554` فقط و**لم يصل هاتفي**.
أُنجز: المكتبة 229 كتابًا (المرحلة ١ + فحص كل كتاب)، قاعدة حواشي المحققين،
«تراجع» الختمة داخل البطاقة، «في المذاهب الأربعة» للجزيري تحت كل خطوة حج،
قسم `lib/features/hifz` للتحفيظ، واللقطات.

## افعل بالترتيب

1. **التسميع الآلي على الجهاز — القرار أولًا، قبل أي واجهة.**
   `scripts/measure_quran_asr.py` أثبت على الحاسوب: 73 كلمة، 98.6٪ مطابقة،
   ويمسك الوقف والآية الغلط ولا يتأثر بقارئ آخر ولا بميكروفون ضيق النطاق.
   **وحُسم في 2026-09-22 أن التصدير الجاهز لا يعمل مع `sherpa_onnx`:**
   سكربت sherpa يسمّي موتّرات المشفّر `mel` ← `n_layer_cross_k/v`، بينما
   `eventhorizon0/tarteel-ai-onnx-whisper-base-ar-quran` يحمل
   `input_features` ← `last_hidden_state` (نزّلتُ المشفّر 23 MB وقرأت
   الأسماء من الملف نفسه، لا من التوثيق). فالخيارات الثلاثة:
   **(أ)** نشغّل `scripts/whisper/export-onnx.py` من sherpa-onnx بأنفسنا على
   `tarteel-ai/whisper-base-ar-quran` (يحتاج torch + openai-whisper على هذا
   الجهاز)، **(ب)** `whisper_ggml` على pub.dev مع تحويل ggml، **(ج)**
   onnxruntime مباشرةً وكتابة حلقة فكّ الترميز في Dart (الأصعب والأخطر).
   **اختر (أ) أو (ب)، نفّذه، ثم قِس على المحاكي بملف تلاوة حقيقي ثم على
   هاتفي. لا تكتب شاشة تسميع قبل رقم من جهاز حقيقي.**

2. **إصدار يوصل هاتفي** (هذا أهم ما ينقص): ارفع `pubspec`،
   `build_github_release.bat` (لا `flutter build apk` المجرّد — مصيدة #41)،
   احذف الإصدار السابق ووسمه (عدا v3.51.0)، ملاحظات عربية صادقة تذكر:
   18 كتابًا جديدًا، تنظيف حواشي المحققين، «المذاهب الأربعة» في الحج، قسم
   التحفيظ، وتراجع الختمة. ثم تحقق أن الوسم = `HEAD` ومن توقيع APK
   (`apksigner verify` مرتين كما في السكربت).

3. **أعد تصوير اللقطات الثلاث الناقصة**: الأذان والحديث ودرس التجويد — ما
   زالت من 2026-09-21، والباقي أُعيد تصويره أمس.
   `py -3 scripts/build_store_screenshots.py` ثم `build_tutorial_shots.py`.

4. **المرحلة ٢ من المكتبة** (الموسوعات) لم تبدأ: قِس كتابًا كبيرًا على هاتف
   أولًا (مصيدة #4) قبل أي وعد.

## قواعد سارية
لا متون مجردة في المكتبة؛ كتب ابن القيم وابن الجوزي والحكيم الترمذي وابن أبي
الدنيا تبقى؛ لا كتب لابن باز وابن عثيمين وابن جبرين وابن عبد الوهاب والألباني
ولا كلامهم في الحواشي **إلا التخريج**؛ الأسماء المتشابهة لا تُمَس.

## ما لم يُحسم
* نسخة بلاي ستور بدون مكتبة (قرار مبدئي، لم يُبنَ).
* أرقام عدّاد إشعار الصلاة لاتينية (أندرويد يستخدم لغة النظام).
* «الإيضاح» فيه 38 صفحة سطرها نقاط (ورقة كلها حاشية في المطبوع) — متروكة
  عمدًا لأن شاشة الحج تعتمد على أرقام صفحاته.
