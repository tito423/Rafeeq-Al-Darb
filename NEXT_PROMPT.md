# البرومبت للسيشن الجاية — انسخه كله

> يتعاد كتابته كل سيشن. القاعدة في `CLAUDE.md` §7: يكمّل من عند ما وقفنا، وأي
> حاجة سايبينها في النص تتقال إنها في النص والسيشن الجاية تعملها تاني.
> آخر تحديث: 2026-09-10، آخر السيشن الثامنة.

اقرا `CLAUDE.md` و`NEXT_SESSION_PROMPT.md` و`HANDOVER.md` — فيهم كل حاجة.

**قبل ما تخطط: §2.0 — اتحقق من الكوته وقوللي الرقم.** لو انت في تطبيق
الديسكتوب (تبويب Code) الـ`/usage` مش هيشتغل عندك — قوللي وأنا هقراهولك من على
شاشتي.

## الحالة دلوقتي

الشجرة نضيفة على `master` ومدفوعة · `flutter analyze lib test` نضيف ·
**٨٠/٨٠ تست** · `py -3 scripts/i18n_audit.py` صفر ·
المحتوى المستضاف **٣٢ مسار، صفر فشل**.

**متصدَّر: `v3.10.0`** — والتاج على `master` وSHA بتاعه = `git rev-parse HEAD`.

---

## ١) أول حاجة: تذكير «سنن السور» بيضرب ومش بيطلع إشعار

**دي حاجة اتكشفت السيشن دي واتسابت من غير إصلاح — ابدأ بيها.**

ظبّطنا تذكير لسورة الملك يوم الجمعة الساعة ٢٠:٠٠ واتفرجنا عليه **مرتين**:

* المنبّه اختفى من قائمة الانتظار وأعاد جدولة نفسه للجمعة اللي بعدها — يعني
  **ضرب فعلًا**؛
* وما ظهرش أي إشعار على قناة `rafeeq_sunan_suwar_reminder`؛
* و`dumpsys` لسه بيقول إن `mLastNotificationUpdateTimeMs` للقناة دي = **٠**،
  يعني **عمره ما اتنشر عليها إشعار واحد**.

ده **مش** بسبب `NotificationRouter` اللي اتعمل السيشن دي: النشر بيتم أصلًا في
الكود الأصلي للمكتبة (`ScheduledNotificationReceiver`) ومش بيعدّي على
`initialize` خالص. ابدأ من `sunan_suwar_reminder_service.dart` وقارن
`AndroidNotificationDetails` بتاعته بالمقولات اللي بتشتغل.

طريقة التجربة (اتجربت وبتشتغل): ظبّط التذكير، `adb shell cmd alarm set-time`
لقبل الميعاد بشوية، واستنى — `adb shell date` مش شغال على إميوليتور Play.

---

## ٢) جودة فيديو الأذان

> «الفيديو بتاع الأذان لما بيشتغل بتبقى جودته سيئة جدًا — حل المشكلة دي»

لسه محدّش بصّ فيها. **ابدأ بقياس**: دقة الملف الأصلي وبِت‑ريته، وإيه اللي
بيتعرض فعلًا، والفيديو بيتشغّل إزاي (`adhan_video_catalog.dart` و
`AdhanActivity`). `ffmpeg` موجود في `C:\Program Files\ShareX\ffmpeg.exe` (فخ #14).

---

## ٣) الخلفيات المصوّرة للمقولات — الجزء اللي المالك طلبه ولسه ناقص

كارت المقولة شغال بخلفيات **مرسومة برمجيًا** (`IslamicPatternPainter`، نفس
زخارف كروت الأذكار) — صفر مشاكل رخص، وستة تدرّجات كل واحد اتقاس تباينه.

المالك طلب كمان **صور من النت، كمية كبيرة وجودة عالية**. ده لسه ما اتعملش،
وسببه مكتوب: **فخ #18** — صورة مجانية مش معناها إن رخصتها تسمح باستضافتها،
ولازم تقرا رخصة كل مصدر وتكتبها وتسيب أي حاجة رخصتها مش واضحة. السيشن دي عملت
كده مع hadeethenc.com وطلعت **بتسمح بشروط**، فالقراءة مش دايمًا بتنتهي بـ«لأ».

الكود جاهز يستقبلها: `QuoteCardScreen` بيختار خلفية عشوائية من قائمة، وإضافة
صور مستضافة مش محتاجة تغيير في أي حاجة تانية.

---

## ٤) حاجات ناقصة مقصودة ومكتوبة

1. **`words_meanings_ar` (معاني الكلمات) مش في حزم الموسوعة.** الـAPI بيرجّعها
   و`hadeethenc_crawl.py` مخزّنهاش، ومعاها `explanation_ar` و`hints_ar`.
   عايزة إعادة زحف (~٣٬٥٧٤ سجل عربي) وترقية `hadeethEncVersion` لـ`v2`.
2. **حلية الأولياء مش مصدر مقولات** — موجودة كـكتاب في المكتبة، ومستبعدة من
   `build_quotes.py` بسبب مكتوب فيه: عيّنة مقروءة بالإيد من ١٬٠٣٢ مرشّح فيها
   أحاديث بلا درجة وأنصاف أسانيد وملاحظات المؤلف عن الرواة.
3. **الأردو لسه بأرقام لاتينية** — عمدًا، لأن باقي نسخة الأردو كلها كده.
   أرقام الأردو الصح U+06F0-U+06F9 مش العربية.
4. **`QuranTranslationInfo.sizeLabel`** لسه بيبني `'… MB'` بنفسه بدل
   `formatBytes` — نسخة سادسة من الفخ #16 من غير الـisolate.

---

## حاجات محسومة — متفتحهاش تاني

* مفيش حاجة مجهولة المصدر بتتشحن. مجموعات `fawazahmed0` (fr/ur/ru) مالهاش
  مترجم مسمّى → برّه.
* ٣ طرق حساب مستبعدة بسبب مكتوب: Moonsighting وJafari وTehran.
* علامات التنصيص باج عرض مش تعديل نص، واتصلح على مرحلتين.
* الكروت بتتبع الثيم — بطلب المالك.
* **درجة الحديث في الموسوعة متتنسبش لعالِم بعينه.** hadeethenc مش بتسمّي
  عالِمًا لكل حديث، فالدرجة بتظهر «تصنيف موسوعة الأحاديث النبوية».
* **المقولات مش بتاخد آية ولا حديث.** الفلتر بيرمي أي فقرة فيها `{}` أو `(())`
  أو إسناد. متوسّعهاش عشان العدد يكبر.

---

## فخاخ ضربت في السيشن دي (كلها اتكتبت في `CLAUDE.md` §3 من #27 لـ#31)

* **الـzip بيتفك باسم الملف المضغوط مش باسم اللي جواه** — تبويب الموسوعة فضل
  ميت رغم إن التنزيل والفك نجحوا وكل الفحوص الآلية كانت نضيفة.
* **`adb shell date` مش شغال على إميوليتور Play**، بس
  `adb shell cmd alarm set-time <millis>` شغال.
* **نص الإشعار بيتجمّد وقت الجدولة مش وقت الظهور.**
* **`easy_localization` مش مصدّر `Localization`/`Translations`**، والتست لازم
  يمرّر `ignorePluralRules: false`.
* **إشعار واحد بس هو اللي بيملك الضغطات في التطبيق كله** — خمس خدمات كانت
  بتنادي `initialize` واتنين منهم بيركّبوا `(_) {}`.
* **أندرويد بيحدّ التطبيق بـ٢٥ إشعار منشور** ويرمي الزيادة.
