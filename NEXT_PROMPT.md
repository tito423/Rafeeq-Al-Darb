# البرومبت للسيشن الجاية — انسخه كله

> يتعاد كتابته كل سيشن. القاعدة في `CLAUDE.md` §7: يكمّل من عند ما وقفنا، وأي
> حاجة سايبينها في النص تتقال إنها في النص والسيشن الجاية تعملها تاني.
> آخر تحديث: 2026-09-10، آخر السيشن الثامنة.

اقرا `CLAUDE.md` و`NEXT_SESSION_PROMPT.md` و`HANDOVER.md` — فيهم كل حاجة.

**قبل ما تخطط: §2.0 — اتحقق من الكوته وقوللي الرقم.** لو انت في تطبيق
الديسكتوب (تبويب Code) الـ`/usage` مش هيشتغل عندك — قوللي وأنا هقراهولك من على
شاشتي.

## الحالة دلوقتي

الشجرة نضيفة على `master` ومدفوعة · `flutter analyze lib test` نضيف ·
**٧١/٧١ تست** · `py -3 scripts/i18n_audit.py` صفر ·
المحتوى المستضاف **٣٠ مسار، صفر فشل**.

**متصدَّر: `v3.9.0`** — تمن سيشنات شغل بقت في إيد المالك أخيرًا. `pubspec`
على `3.9.0+5`، والتاج على `master` وSHA بتاعه = `git rev-parse HEAD`.

## ابدأ بالترتيب ده

### ١) حاجة اتشحنت ومحدّش شافها على جهاز — لا يوجد

كل حاجة اتعملت السيشن دي اتشافت على `emulator-5554`:
التنبيه ضرب على الشاشة واتقرا بعينه (قبل الإصلاح وبعده)، وإشعار الإقامة
بالإنجليزي بعد تبديل اللغة، وتبويب الموسوعة كامل بالعربي والإنجليزي —
التنزيل، التصنيفات السبعة بأعدادها، الحديث بتخريجه ودرجته، والبحث.

**بس فيه ٣ حاجات مقصودة ومكتوبة إنها ناقصة — مش أعطال، شغل جاي:**

1. **`words_meanings_ar` (معاني الكلمات) مش في الحزم.** الـAPI بيرجّعها
   و`hadeethenc_crawl.py` مخزّنهاش، ومعاها `explanation_ar` و`hints_ar`.
   عايزة إعادة زحف لحوالي ٣٬٥٧٤ سجل عربي (١٥–٢٠ دقيقة بـ`curl`)، وبعدها
   `build_hadeethenc_packs.py` تاني ورفع تاني وترقية `hadeethEncVersion` لـ`v2`.
2. **الأردو لسه بأرقام لاتينية.** `localizeDigits` بتشكّل العربي بس، عشان باقي
   نسخة الأردو كلها لاتيني. أرقام الأردو الصح هي U+06F0-U+06F9 مش العربية.
3. **`QuranTranslationInfo.sizeLabel`** بيبني `'… MB'` بنفسه بدل `formatBytes`
   — نسخة سادسة من الفخ #16 من غير الـisolate. متلمستش.

### ٢) إشعارات المقولات الإسلامية — البند اللي المالك طلبه ومحدّش بدأ فيه

إشعار كل مدة يحددها المالك (نص ساعة أو أكتر أو أقل)، لما يضغط عليه يفتح كارت
جوّه التطبيق يغطي اللي وراه، بخلفية إسلامية تتغير عشوائي كل مرة، بديزاين حلو،
وزر dismiss بلغة الواجهة.

**المالك حسم المصدرين:**

* **المقولات من كتب المكتبة نفسها.** حدّد بالاسم: **«لا تحزن»** و**«صيد
  الخاطر»** (الاتنين موجودين في `book_catalog.dart` من ضمن ٢٢٧ كتاب)،
  و**«حلية الأولياء»** و**«روضة العقلاء ونزهة الفضلاء»** لابن حبان البستي —
  الاتنين دول **مش** في الكتالوج ولازم يتنزّلوا من الشاملة.

  | الكتاب | رقم الشاملة |
  |---|---|
  | روضة العقلاء ونزهة الفضلاء | **6944** |
  | حلية الأولياء وطبقات الأصفياء — ط السعادة | **10495** |

  `py -3 scripts/shamela_index.py find "<العنوان>"` لو احتجت غيرهم. فخ #17:
  بحث الشاملة بيدوّر **جوّه** الكتب مش في عناوينها.

  **كل مقولة لازم تحمل اسم كتابها** — §1.1 و§1.2.

* **الخلفيات من النت**، كمية كبيرة وجودة عالية، وفي بعضها زخارف زي اللي في
  كروت الأذكار (`IslamicPatternPainter` / `_CardBackground` في
  `azkar_section_screen.dart`). **فخ #18 ينطبق بالحرف**: صورة مجانية مش معناها
  إن رخصتها تسمح باستضافتها. اقرا رخصة كل مصدر واكتبها، ولو مصدر مالوش رخصة
  واضحة سيبه. (السيشن دي عملت كده مع hadeethenc: طلعت بتسمح بشروط، والشروط
  متنفَّذة ومكتوبة في `AppConfig.hadeethEncUrl`.)

### ٣) جودة فيديو الأذان

> «الفيديو بتاع الأذان لما بيشتغل بتبقى جودته سيئة جدًا — حل المشكلة دي»

لسه محدّش بصّ فيها. **ابدأ بقياس**: إيه دقة الملف الأصلي وبِت‑ريته، وإيه اللي
بيتعرض فعلًا، والفيديو بيتشغّل إزاي (`adhan_video_catalog.dart` و`AdhanActivity`).
`ffmpeg` موجود على الجهاز في `C:\Program Files\ShareX\ffmpeg.exe` (فخ #14).

## حاجات محسومة — متفتحهاش تاني

* مفيش حاجة مجهولة المصدر بتتشحن. مجموعات `fawazahmed0` (fr/ur/ru) مالهاش
  مترجم مسمّى → برّه.
* ٣ طرق حساب مستبعدة بسبب مكتوب: Moonsighting وJafari وTehran — السبب في
  `prayer_calculation_methods.dart`.
* علامات التنصيص باج عرض مش تعديل نص، واتصلح على مرحلتين: شيل علامات الاتجاه،
  **و** فرض RTL على الفقرة.
* الكروت بتتبع الثيم — عكس قرار P3‑4 القديم، وبطلب المالك.
* **درجة الحديث في الموسوعة متكتوبش من غير صاحبها.** hadeethenc مش بتسمّي
  عالِمًا لكل حديث، فالدرجة بتظهر دايمًا «تصنيف موسوعة الأحاديث النبوية»
  ومعاها مراجع الموسوعة نفسها. متحاولش تنسبها لعالِم بعينه.

## فخاخ ضربت في السيشن دي بالذات

* **الـzip بيتفك باسم الملف المضغوط مش باسم اللي جواه.**
  `DownloadManager._unzipToDatabases` بياخد `basename(zipPath) + '.db'`.
  `ar.zip` بقى `ar.db` والمستودع بيفتح `hadeethenc_ar.db` → التبويب فضل ميت
  رغم إن التنزيل والفك الاتنين نجحوا و`flutter analyze` نضيف و٦٦ تست عدّت.
  **دي §1.3 بالحرف: البايتات على الباكِت مش معناها إن الميزة شغالة.**
* **`adb shell date` مش شغال على إميوليتور Google Play** (مفيش root)، بس
  **`adb shell cmd alarm set-time <millis>` شغال** — وده اللي خلّى التنبيه
  يضرب في دقيقتين بدل ما نستنى للفجر.
* **`Localization` و`Translations` مش متصدَّرين من `easy_localization`** —
  التست بيستوردهم من `src/` عشان يقدر ينادي `plural()` الحقيقية،
  و**لازم يمرّر `ignorePluralRules: false`** وإلا الست حالات العربية بتتلخبط.
* **الكونسول cp1256 ومش بيطبع عربي** (فخ #10) — ضرب مرتين. اكتب في ملف UTF-8
  و`cat`.

## لو المالك قال «جهّز الدنيا»

الروتين إجباري وكامل في `CLAUDE.md` §6: تحقّق، قِس، حدّث `HANDOVER.md`، اكتب
`NEXT_SESSION_PROMPT.md`، ارفع كل حاجة، واتأكد إن التاج على `HEAD`.
