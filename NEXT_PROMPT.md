# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول (المنهج الملزم)، وبعده **`OWNER_FINDINGS.md`** — ده ملف
ملاحظات صاحب التطبيق من استخدامه الفعلي وهو اللي بتشتغل منه، وبعده
`HANDOVER.md`.

**قبل ما تخطّط: اقرأ الكوتة (§2.0) وقول لصاحب التطبيق الرقم.** لو الواجهة
مش بتوريه، اطلب منه يقراه من شاشته — ما تخمّنش.

## الحالة عند التوقف

- المُصدَر: **v3.13.0** على `5296993` — التاج على `master` والـ SHA بتاعه =
  `HEAD`، وإصدار واحد بس في المستودع، والـ APK المرفوع اتأكد إنه موقّع بمفتاح
  `CN=Rafeeq Al-Darb` (نزّلته من جيت هب وفحصته).
- `flutter analyze lib test` نضيف · `flutter test` **١١٩ ناجحة** · الشجرة نضيفة.
- **التوقيع:** جرادل بيوقّع ديبج بقصد. بعد `flutter build apk --release` لازم
  `py -3 scripts/sign_release.py` — فخ ٤١. المفتاح في `../Rafeeq-Keys/`.

## أول حاجة: الـ ANR — أهم حاجة مفتوحة

اتشاف مرتين على نسخة debug: `Waited 5007ms for MotionEvent` والتطبيق متجمّد
على الشاشة الرئيسية. **ما اتكرّرش على نسخة profile** — جولة تحقق كاملة عدّت
من غيره. اتشال منها لوپين إعادة رسم على الشاشة الرئيسية (setState كل ثانية
للشجرة كلها، وساعة بترسم ٢٥ مرة/ثانية لرقم بيتغيّر مرة) — ده هدر حقيقي بس
**مش مدّعى إنه السبب**.

اعمل: `flutter run --profile`، افتح DevTools، واقرا الـ timeline لأول ١٠ ثواني.
**ما تقولش إن التطبيق سلس قبل ما يبقى في رقم من هناك.**

## بعدها — مصلّح ومحدّش شافه على جهاز

الأربعة دول analyze-clean وعليهم اختبارات، بس **ما اتفتحوش على جهاز**. افتحهم
واتأكد بنفسك، ما تصدّقش الكوميت:

1. **إشعار المقولة والتطبيق مقفول** — اقفل التطبيق تمامًا، استنى إشعار مقولة،
   اضغط عليه: المفروض يفتح المقولة على طول من غير إسبلاش.
2. **زرار «ابحث عن شرحه»** في شاشة تفاصيل الحديث — نزّل حزمة موسوعة الأحاديث
   الأول، وبعدين افتح حديث فيه نص مقتبس واضغط الزرار.
3. **الشاشة اللي بتحمّل للأبد** — `FutureView` اتحطّت في ٤ شاشات. اتأكد إن
   شاشة أبواب أي كتاب حديث بتفتح عادي.
4. **«دبي» → الهيئة العامة** في اختيار طريقة الحساب.

## المفتوح، وليه

- **أسماء المؤذنين.** الملفات مطابقة بالبايت لمصدرها على archive.org، بس تسمية
  المصدر نفسها محدّش تحقق منها، وصاحب التطبيق **بيقول بأذنه إنها غلط**. أذنه
  دليل أقوى من اسم ملف. لو التسجيلات الجديدة لسه غلط عنده → **الأسماء تتشال**
  (§1.1 / §1.2). ما تخترعش تسمية.
- **تنبيهات قبل/بعد الصلاة على تليفونه هو** — شغالة على الإيموليتر بدليل
  `dumpsys alarm`. الفجوة بين «اتسجّل» و«ظهر»، غالبًا إدارة بطارية الشركة.
- **زر التجربة بيفتح الإعدادات عنده** — مش قابل للتكرار هنا؛ اشتغل بالكامل.
  الأرجح إذن ناقص (المنبّه الدقيق / ملء الشاشة) وكروت الأذونات هي اللي بتفتح
  الإعدادات.
- **شاشة سنن النسائي الفاضية** — القاعدة سليمة (٥٢ باب، ٥٧٦٨ حديث، والنسخة
  على الجهاز مطابقة بالحجم). لو رجعت هتطلع رسالة دلوقتي بدل الدوّامة — الرسالة
  هي الدليل الجاي.

## ما تعملهاش تاني

- `flutter build apk --release` بيقتل الإيموليتر (فخ ٢٤).
- الكونسول cp1256 مش بيطبع عربي (فخ ١٠)؛ **الـ heredoc بيبوّظ `\n` والـ quotes
  (فخ ١١) — اتلسعت منه تاني في السيشن دي؛ استخدم أداة Write لأي سكربت فيه نص.**
- `TextDirection` غامضة: `easy_localization` بيصدّر بتاعة `intl`. استخدم
  `ui.TextDirection`.
- صفحة مدفوعة (`Navigator.push`) **مش** بتترسم تاني لما الشاشة اللي تحتها
  تترسم — لازم `Consumer` جوّاها.
- `if (!snapshot.hasData)` بيرسم الفشل على إنه تحميل. استخدم `FutureView`.
- `adb shell input keyevent KEYCODE_BACK` بيطلّعك من الشاشة كلها في التطبيق —
  استخدم زرار الرجوع اللي في الشاشة نفسها لما تكون بتتنقّل جوّه تبويب.
