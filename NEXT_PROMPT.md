# كمّل رفيق الدرب من حيث وقفنا

اقرأ بالترتيب: `CLAUDE.md` كاملًا، ثم `TASK_FOLLOWUP.md` (قسم «Next step» — أعلاه أحدث)، ثم `NEXT_SESSION_PROMPT.md`،
ثم `TRAPS.md` للمنطقة التي ستعمل فيها. ردّ بالعربي. في أول رد: الكوتة حيّة (`get_usage`) فقط — لا تتكلم عن Remote Control.

## أين وقفنا (2026-09-26 ~11:30 دبي)
- المنشور ما زال **v3.63.7**. `master` متقدم عليه بكثير (العرض/الأفقي كله، إصلاح ترتيب أرقام الآيات، الإعراب، الأرقام العربية).
- المالك طلب: «لو خلصت كل حاجة اديني تقرير وانشر» — **النشر مشروط بإنهاء طلباته الجديدة أدناه**. عند الإنهاء: ارفع
  `pubspec` إلى 3.64.0 (والـ About)، `build_github_release.bat`، احذف v3.63.7 وتاجه (ليس v3.51.0 ولا content-*)، انشر بـ
  `--target master`، تحقق أن التاج = HEAD، وأعطه تقريرًا عربيًا.
- analyze نظيف، **595** اختبارًا ناجحًا، المستضاف 10/10 → 206، 7 لغات × 1,827 مفتاح.
- قاعدة العمل هذه الجلسة: كل شاشة تُرى بالعرض وبالطول على المحاكي؛ المحاكي يُدوَّر بـ `adb -s emulator-5554 emu rotate`
  (ليس settings — يتجاهلها)، وكثافته 320 (2 px/dp) لا 3 كالشاومي. مساعد الضغط/اللقطات كان سكربتًا مؤقتًا (e.ps1) — أعد كتابته.

## غير منتهٍ — يُعاد ولا يُصدَّق
1. **بناء 11:24 (HEAD `1bc93dc8`) لم يُثبَّت ولم يُرَ:** «المزيد» 3 في الصف بتمرير واحد (بعد صورة المالك «تحس انك بتديق
   على نفسك وربنا موسعها عليك»)، ومصحف النص في ملء الشاشة **بلا رأس اسم السورة الثابت** إلا عند اللمس وظهور الخيارات.
   ثبّته على المحاكي وشاهد الاثنين بالطول وبالعرض قبل أي شيء.
2. تظليل الآية المتلوّة (wash) بالعرض لم يُرَ؛ تظليل الضغط المطوّل رُئي. شغّل «تلاوة الآية» وشاهد.
3. الاستماع للكتاب أثناء الدوران (BookListenAction بـ GlobalKey) لم يُجرَّب — يحتاج كتابًا **مشكولًا** (الأربعون النووية 0%).
4. هاتف المالك (شاومي `BYKRKJPRC6O7FMHU`) عليه بناء 03:50:45 فقط؛ كل ما بعده رُئي على المحاكي فقط.

## طلبات المالك الجديدة (لم تبدأ) — بالترتيب
1. **المسبحة بالعرض** (صورته): «ارفع الحد بتاع الشاشة لفوق خلي الشاشة تتملى اكتر تفاصيل» — المحتوى يبدأ منخفضًا تحت
   العنوان وفراغ كبير؛ املأ الارتفاع.
2. **الشريط الجانبي**: «خلي النافيجيشن بار يروح شمال شوية لحد قبل الشريط اللي فيه النوتش … استغل حجم الشاشة كويس في كل
   شاشات التطبيق» — `SideTabs` يلتف بـ `SafeArea` كامل فيبتعد عن الحافة بعرض النتوء؛ قرّبه (اترك مساحة النتوء فقط حيث هو)،
   وراجع هوامش كل الشاشات بالعرض.
3. **الرئيسية على أي تابلت/شاشة ذكية**: «لو المستخدم اختار ساعة أنالوج قسّم الشاشة وصغّر العناصر الكبيرة بحيث الشاشة
   تستوعبهم ويبقوا واضحين … وممكن تصغير القسم الكبير اللي فيه الساعات وشرايح الصلاة … لازم التطبيق يبقى عنده المرونة
   ويشتغل في جميع الأوضاع وعلى جميع مقاسات التابات والسمارت اسكرين». الساعة الأنالوج 176dp ثابتة
   (`home_screen.dart` ~661) والرقمية 78؛ اجعلها تتبع الارتفاع المتاح. تحقّق على مقاسات: هاتف بالعرض (`wm size 1220x2712`
   + `wm density 480`)، تابلت (`wm size 1600x2560` + `wm density 320`)، شاشة ذكية (`wm size 1080x1920` + `wm density 240`)؛
   **أعد `wm size reset` و`wm density reset` بعدها**.
4. ثم التقرير والنشر (أعلاه).

## باقٍ من قبل
- الإحالات الـ222 غير المثبتة في الإعراب (`scripts/irab_daas_refs_for_owner.md`) تُلاحَق في الجدول/درويش/الإعراب الميسر.
- فواصل أسطر الشاملة داخل الجملة (مثل 2:23).
- منتقي الوقت المدمج في Flutter أرقامه لاتينية (ليس كودنا).
- Avast على الشاومي نبّه «Suspicious app» (Detection ID 3d1dc328d456) بعد تثبيت 03:09؛ المالك أغلق النافذة، ولم يُفحص السبب.
