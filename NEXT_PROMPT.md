# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول (المنهج الملزم)، وبعده **`WORK_QUEUE.md`** (أول قسم:
العاشرة J1–J26)، وبعدها `HANDOVER.md`.

**قبل ما تخطّط: اسأله عن الكوتة (§2.0).** مقدرتش أقراها من الواجهة. آخر مرة
قال إنها ٩٣.

---

## الحالة عند التوقف

- المُصدَر: **v3.19.0** على `master`. أول حاجة:
  `gh release view v3.19.0 --json tagName,targetCommitish,assets` وتأكد إن SHA
  التاج = `git rev-parse HEAD` وإن فيه إصدار واحد بس.
- `flutter analyze lib test` نضيف · `flutter test` **١٩٧ ناجحة**.
- **الإصدار ده اترفع من غير ما يتجرّب على الجهاز** — طلبه عشان الكوتة.

---

## أول حاجة: جرّب الدفعة العاشرة كلها على الـAPK الموقّع

كل ده اتكتب واتحلل واتختبر، بس **ماتشافش على الشاشة**. اعمله بالترتيب ده
وسجّل اللي شفته تحت كل بند في `WORK_QUEUE.md`:

1. **الضغط في المصحف (J1/J5):** ضغطة على الصفحة وعلى آية في النصي والمصوّر ←
   ملء الشاشة ودخول/خروج؛ ضغطة مطوّلة ← كارت الآية والتعليم. في العرضي مفيش
   خيارات. **اتأكد إن الضغطة الواحدة مش بتتأخر** (اتشال الـdouble-tap).
2. **«الانتقال إلى» (J2/J3):** الشيت بالتلات تابات، والصفحة مابتعملش فليكر
   لما تكتب في خانة الصفحة (المصوّر خصوصًا).
3. **شريط التمرير (J4/J17):** اسحبه في قائمة القرّاء (٢٤١ قارئ) وفي المزيد؛ الأسهم
   وضغطة الشريط؛ ومافيش فليكر لما تفتح قارئ.
4. **كارت الآية (J6/J7/J9):** «تلاوة الآية» يرجع بعد ما الآية تخلص؛ زرار التكبير؛
   التكرار بالشريط والمربع.
5. **البحث (J11/J12):** «الصلاة» لازم يطلع ٦٣ بكل المشتقات؛ الصبر ٩٠ في
   الموضوعات؛ جرّب «جزء من كلمة» و«كلمة مطابقة» و«بالتشكيل»، والتظليل على الكلمات.
6. **الرقية (J20–J22):** تشتغل في مشغّل التلاوة؛ إيقاف/استئناف/إلغاء التحميل؛ اسم
   الإشعار «تحميل ملفات الرقية الصوتية».
7. **كارت القارئ (J18):** يكتب كل السور اللي بتتنزل؛ **المشغّل الصغير (J19)** متحرك.
8. **الثيم النهاري (J24/J13):** معرض الساعات (الأسماء والأرقام)، وكارت القبلة.
9. **الأذان (J14):** الأذانين المحذوفين مش في القائمة، ولو كان مختار واحد منهم
   الإعدادات ماتقعش.
10. **ضبط المواقيت (J15)** مفيش خطوط العرض العليا؛ **المكتبة (J16)** زرار تحميل
    كتب المؤلف؛ **المزيد (J23)** عنوان «شاشة البداية».
11. **الأنيميشن (J25) وثيم RGB (J26)** في كل الشاشات — اتأكد إن مفيش شاشة
    بتغطي خلفية RGB بلون ثابت.
12. **«وضع المصاحف» (J8):** لسه ماتكررش — اطلب منه سكرين شوت أو فيديو.

أي حاجة تبان مكسورة: صلّحها، ابني، وقّع، وارفع إصدار جديد.

---

## حاجات لازم تعرفها

- **الضغطة الواحدة في المصحف = ملء الشاشة، والمطوّلة = الكارت** (طلبه صراحة).
- **البحث:** `lib/core/utils/quran_search_match.dart` + `QuranRepository.searchQuran`
  و`searchTopic`. أنماط الموضوعات في `topic_tree.dart` اتقاست على القاعدة؛ لو
  غيّرت نمط، قيسه تاني بـPython على `quran_local.db` واقرأ الكلمات اللي طلعت.
- **استبعاد الكلمات الشبيهة لازم يتعمل على الشكلين (strict وloose)** — غير كده
  «الصدقات» و«المنافقين» بيرجعوا.
- **الرقية مجموعة تحميل لوحدها** (`DownloadEngine.groupRuqyah`).
- **تنزيل التلاوات الكاملة في الـholding queue Native** بترتيب السور.
- **الإيموليتر بـ`-no-snapshot-save` بيرجع لحالة قديمة كل إقلاع.** مش bug.

## ما تعملهاش تاني

- `flutter build apk --release` بيقتل الإيموليتر (فخ ٢٤) — اقفله الأول.
- بعد البناء لازم `py -3 scripts/sign_release.py`.
- `Set-Content -Encoding utf8` في PowerShell 5.1 بيكتب BOM.
- heredoc في bash مع عربي بيفشل — اكتب السكربت بـWrite.
- `adb shell ls /sdcard/...` من Git Bash محتاج `MSYS_NO_PATHCONV=1`.
