اقرأ `CLAUDE.md` ثم `HANDOVER.md` (كتلة الحالة في أوله مكتوبة 2026-09-20، وهي
**التسليم الثالث** في نفس اليوم — الأول والتاني تحته).

المنشور الآن **v3.47.0** والتاج على `HEAD`. الشجرة نظيفة ومدفوعة.

## أول حاجة: صدّق ولا تثق

الجلسة اللي فاتت صلّحت **إحدى عشرة** حاجة رصدها المالك بنفسه، و**تلاتة بس
منهم اتشافوا على جهاز**. الباقي مكتوب ومختبَر (٤٥٣ اختبار) و**مافُتحش على
تليفون ولا محاكي**. افتحهم واحدة واحدة على `emulator-5554` قبل أي شغل جديد:

اللي **اتشاف**: صفحة الأذونات أول شاشة بعد السبلاش وتغيير لغتها يقلبها فورًا ·
شاشة «العرض» بتوري تحكّمات المصحف النصي في الوضع النصي وتحكّمات الورقي في
المصوّر · الخمس أزرار في شريط المصحف على سطر واحد.

اللي **لسه ماتشافش** — دي قايمة شغلك الأولى:

1. **المزامنة** — سجّل دخول بحسابين أو غيّر إعداد على جهاز وشوف إنه **مابيسافرش**
   دلوقتي. المفروض أربع حاجات بس تنتقل: `khatma_list_v1`، `ayah_notes_v1`،
   `quran_last_page`، `tasbeeh_custom_target` (`SyncService.syncedStateKeys`).
2. **ثيم RGB مع المصحف الورقي** — افتح الثيم وافتح المصحف المصوّر وشوف إن
   التدرّج المتحرّك مابقاش وراه.
3. **التكرار** — غيّر القارئ، شغّل «تكرار الآية»، وتأكد إنه **بصوت اللي اخترته**
   مش المنشاوي. وإن شريط «جارٍ تكرار الآية» فيه «إيقاف» **ظاهر** وبيختفي لما
   الصوت يقف.
4. **التنزيلات** — كل صف يفتح حاجة، و**الرجوع يرجّعك للتنزيلات** مش لتبويب تاني.
5. **أرقام علامات الآيات** في المصحف النصي — أصغر ومتوسّطة.
6. **قائمة المزيد** — التنزيلات بطاقة لوحدها، شرح الميزات جوّه الأدوات، الحساب
   والمزامنة جوّه الإعدادات.

## ثانيًا: اللي المالك طلبه وما اتعملش

* **خيارات التنزيل على صفحة الترحيب.** «والاوبشنز اللي ممكن يحملها المستخدم في
  نفس الصفحة بدل مايتفاجأ بيها جوه مش موجودة زي التفاسير مثلا». شاشة
  `onboarding_screen.dart` بتعرض المصحف بس (`MushafDownloadTile`). محتاجة كارت
  تاني على الأقل لحزمة **علوم القرآن** (`AppConfig.sciencesDbUrl`،
  **٣٣٬٢٣٩٬٥١١** بايت، `sciencesDbDownloadId`) بنفس شكل بوابة التنزيل اللي في
  `ayah_sciences_sheet.dart`.
* **ترتيب التشغيل الأول** اللي طلبه بالكامل: سبلاش ← الأذونات ← **التوتوريال** ←
  المزامنة و«مرحبًا بك» والخيارات. اللي اتعمل: سبلاش ← الأذونات ← الأونبوردنج.
  التوتوريال لسه بييجي في مكانه القديم.
* **«لما يجي الأذان وارفع الاشعار لفوق مفيش حاجة اوقفه منها»، و«الأذان أطفيه
  يرجع يتكرر».** **مش مشخّص.** الكود بيبني الإشعار بـ`setOngoing(true)` وزرار
  «إيقاف» وزرار «كتم» (`AdhanNotifications.kt:138`)، و`AdhanService` بيرجّع
  `START_NOT_STICKY`، و`rearmAfterFiring` بيحسب اليوم اللي بعده صح. يعني اللي
  بيشوفه مايطابقش الكود. **كرّره** بفخ #28: `settings put global auto_time 0`
  ثم `cmd alarm set-time <ms>` لحد قبل الأذان بدقيقة، وبعدين اقرأ
  `dumpsys notification --noredact | grep -A30 rafeeq` وشوف الإشعار المنشور
  فعلًا فيه إيه. **متغيّرش سطر قبل ما تشوف.**

## ثالثًا: المشروع الكبير — تصغير التطبيق

الـAPK **٢٥٩٬٧٤٨٬٢٨٦** بايت، كان **٣٣٩٬٩٢٠٬١٩٧** في v3.43.0 — نزل **٧٦.٥ MiB**.
الخريطة الكاملة بالأرقام المقيسة في `docs/size/`.

**قاعدة ثابتة من المالك:** «يشتغل مع اي نوع من انواع الاندرويد فوق سبعة ويشتغل
على اي نوع من معمارية». `minSdk` ٢٤ وتلات معماريات، **ولا تشيل واحدة**. جرّبتها
مرة ونزل APK **بيتركّب ويموت عند الفتح** — التفاصيل مكتوبة في `build.gradle.kts`.

الفاضل جوّه الـAPK (مقيس، مضغوط):
* الأذان ٤٣.٧٥ MiB (١٤ ملف) — النسخة المكرّرة في `res/raw` اتشالت خلاص
* `assets/data` ٢٣.٥٩ (فيها `hadith.zip` ٢١.٢) · HadeethEnc ١٦.٠٥ · الكتب
  المدمجة ٧.٢٠ · خلفيات الاقتباسات ٤.٤٤ · خطوط الواجهة ٣.٩٦ · الهوية ٣.٨٧

⚠️ **`hadith.zip` وHadeethEnc مدمجين بطلب صريح منه** («ادمجها مع التطبيق out of
box»). **ماتشيلهمش من غير ما يقول.**

## رابعًا: نضافة

`hifz_al_umr` في `book_catalog.dart` — اتأكد إن ليه ملف على البكت.

## قواعد لا تُكسر

* **لا تنشر إصدارًا إلا بأمره**، وابنِه بـ`build_github_release.bat` وحده (فخ #41).
* **مصحف ورقي واحد.** لا تقترح طبعة لا تملك إحداثياتها المنشورة.
* **بلّغ بالأرقام المقيسة**، وقل صراحةً ما لم تره بعينك.
* **التنقّل الأعمى على المحاكي بيضيّع توكنز.** استخدم
  `adb shell uiautomator dump /sdcard/ui.xml` واقرأ منه `content-desc` و`bounds`
  وخُد الإحداثيات منه — أسرع وأرخص بمراحل.
* **`uinput` بيشتغل على المحاكي** رغم إن `sendevent` مرفوض بـSELinux. لو احتجت
  لمسة بإصبعين، سجّل شاشة لمس افتراضية واحقن منها — اتعملت وشُوفت.
