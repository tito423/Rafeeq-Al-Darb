# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول، وبعدين `HANDOVER.md` (الجزء اللي فوق: «STATE AS OF
2026-09-15») و`NEXT_SESSION_PROMPT.md`. **واسأل المالك عن الكوتة قبل ما
تخطّط** — آخر قراءة كانت ٩٧ يوم ٢٠٢٦-٠٩-١٣، ويمكن الأسبوع اتجدّد.

**الحالة:** آخر ريليز منشور **v3.24.2** على `master` (`d7bba88`)، موقَّع
بـ`scripts/sign_release.py`. الكوميتات اللي بعده توثيق وتنظيف بس، مفيش كود
اتغيّر. التحليل نضيف و٢١٢ اختبار بيعدّوا.

**مهم تعرفه:** المالك شغّل وكيل تاني (جيميناي على Antigravity) في جزء من
الشغل. اللي بناه **حقيقي بس ماحدّش شافه شغال على جهاز**، وسبق وشحن ريليز
التطبيق فيه ما بيفتحش أصلًا. **أي حاجة عملها اعتبرها مش متأكَّد منها لحد
ما تشغّلها بنفسك.**

الردود على المالك بالمصري، ونصوص التطبيق **فصحى**. والريليز **بأمره هو**،
ولو الكوتة قرّبت تخلص والشغل مش منشور قوله سطر واحد.

---

## ١. المزامنة — ابدأ بيها، وهي مش خالصة

المالك بيجرّب زرار الدخول على تليفونه. **أول حاجة: اسأله الكارت عمل إيه**
(دلوقتي بيعرض سبب الخطأ لو فشل، بدل ما يسكت).

المعروف والمتقاس:
- جوجل للهوية بس (`email`/`profile`)، و`AppConfig.googleServerClientId` هو
  Client ID بتاع الويب. عميلَي الأندرويد متعملين.
- الخدمة على كلاودفلير في `sync_backend/`، شغالة على
  `https://rafeeq-sync-backend.int-vip00.workers.dev`، في حساب
  **`Int.vip00@gmail.com`** (مش إيميل المالك). `scripts/.env` فيه
  `CF_WORKERS_TOKEN` واتجرّب (Workers + D1).
- **اتقاس:** طلب من غير توكن ← **401**.

**اعمل ده وما تعتبرهاش خلصت غير بيه:**
1. سجّل دخول على `emulator-5554`، سبّح لرقم معروف وحرّك مكان الختمة.
2. امسح بيانات التطبيق (`adb shell pm clear com.tito.rafeeq_aldarb`) أو افتح
   AVD تاني، سجّل دخول، **وشوف نفس الأرقام بعينك**.
3. **الأمان اللي ماحدّش جرّبه:** توكن مزوّر لازم ياخد 401، وتوكن سليم عمره ما
   يرجّع بيانات `sub` تاني.
4. **الإرسال المكرر:** العدّادات بتتجمع (قرار المالك)، فلو الدفعة اتبعتت
   مرتين العدد هيزيد غلط. اتأكد إن فيه حماية.
5. المالك كان طالب عرض الدخول **مرة واحدة في أول تشغيل** مع «تخطّي» شغال —
   **ده مش موجود**، فيه كارت «المزيد» بس.

## ٢. تنزيل التلاوات آية بآية — اتبنى وما اتجرّبش خالص

«التنزيلات» ← «تلاوات الآيات». نزّل سورة كاملة، اقفل النت
(`adb shell cmd connectivity airplane-mode enable`)، شغّلها آية آية واسمعها،
وخد لقطة وبُصّ فيها. المالك قال عن المحاولة القديمة «فشل فشل ذريع». اقرأ الفخّ
٤٥ قبل أي callback.

## ٣. إصلاحين اتشحنوا من غير ما يتشافوا على جهاز

- **رفرفة تاب القرآن**: اتحط حارس `_appliedUiMode` في `quran_screen.dart`
  (كان `immersiveSticky` بيتطبّق في حلقة). قيسها زي الفخّ ٤٣:
  `dumpsys window | grep statusBars` و`dumpsys gfxinfo` قبل وبعد.
- **كارت الدخول** بقى بينشر النتيجة ويعرض الخطأ.

## ٤. مش مثبت

قفزة ثواني عدّاد إشعار الصلاة — ما اتكرّرتش على الإيمولاتور، فمش مثبت إنها
اتصلحت.

## ٥. مستنّي المالك

الـ١١ كليب تحت `adhan/video/` على R2 · عنوان المشكاة · هل نرجّع أول ~٢٠ صفحة
من مصحف حفص جوّه التطبيق عشان يفتح من غير نت.

---

## اللي خلص واتشاف على الجهاز

تعليم التجويد (٢٣ درس بمدى فقرات مقروء من الكتاب، والاستماع والإيقاف شغالين)
· «في مثل هذا اليوم» (٣٦٦ يوم لكل لغة، صفر فاضي) · شيل المصحف المضمّن
(٣٢٤٫٦ ← ٢٢٧٫٣ ميجا) ونسخة الـ٦٠٤ صفحة على R2 كاملة · ٢٨ كتاب شاملة
(الكتالوج ٢٧٥) · التطبيق بيفتح من أول تشغيل بعد إصلاح المزوّد اللي كان بيرمي
خطأ · قفل التصميم باختبار.
