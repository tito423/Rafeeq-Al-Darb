# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول، وبعدين أعلى `HANDOVER.md` («STATE AS OF 2026-09-15 —
evening»). **اسأل المالك عن الكوتة قبل ما تخطّط** — آخر قراءة ٣٣، ومعاه اشتراك
تاني. الردود بالمصري، ونصوص التطبيق فصحى. الريليز **بأمره هو**: آخر منشور لسه
**v3.24.2**، وكل اللي تحت متعمل commit ومش منشور.

**التجربة على تليفون المالك مش الإيموليتر** («جرب كل حاجة على تليفوني»):
الهونر `AB3S6R4C04016607` (تطبيقه في user 0)، والشاومي `BYKRKJPRC6O7FMHU`
(اتفصل في النص). بتثبّت نسخة **release موقّعة** بـ`scripts/sign_release.py`
فوق نسخته بـ`adb install -r` — بياناته بتفضل. **ماتثبّتش debug عليه أبدًا**
(توقيع مختلف = مسح). الإيموليتر TLS بتاعه بيتكسر بسبب Avast على الجهاز ده.

---

## ١. كمّل التجربة على التليفون من حيث وقفت

آخر نسخة مثبّتة على الهونر فيها قسم الحج؛ النسخة اللي فيها **إعادة تصميم
تلاوات الآيات** اتبنت في الخلفية — ثبّتها وجرّب:

1. **تلاوات آية بآية** (التنزيلات ← تلاوات الآيات): افتح قارئ، نزّل سورة قصيرة،
   اتأكد إن الحلقة بتتملي، شغّلها بزر «استماع»، واقفل النت وشغّلها تاني.
   **ماحدّش شافها شغالة لسه.**
2. **قسم الحج**: شفت الخريطة والأقسام ونص الخطوة ١ حرفيًا. **ما اتجرّبش**:
   عدّاد الطواف والسعي والجمرات بالضغط، ومسار العمرة.
3. **الجولة**: اتشاف أول ٣ محطات (الترحيب، الساعة بفريم ذهبي، العدّاد). **كمّل
   الـ٣١** وخصوصًا محطات القرآن (أزرار الشريط) والمحطات اللي بتنقل تبويب وتعمل
   scroll، واتأكد إن محطة ميزتها مش ظاهرة بتتخطّى.
4. **«مقولة اليوم»** — **اتجرّب على الهونر واتأكد**: عدّ
   `dumpsys alarm | grep -c ScheduledNotificationReceiver` = ١٠ وهي مقفولة
   (تذكيرات تانية)، ٣٤ على ١٥ دقيقة (+٢٤ بالظبط)، ٣٤ بعد التغيير لساعة (استبدال
   مش تراكم)، ١٠ بعد «إيقاف»، و**١٠ بعد ١٥ دقيقة ← إيقاف فورًا** (الحالة اللي
   كانت بتفضل تشتغل لوحدها). رجّعتها «موقوف» زي ما كانت. مش محتاجة تتعاد.
5. **«استمع» في التجويد**: الإصلاح (late `_root`) والحركة الجديدة ما اتسمعوش
   على التليفون.

## ٢. اللي اتصلح واتشاف على التليفون

- **الفليكر في القرآن**: السبب `OrientationBuilder` بياخد اتجاه جسم الشاشة؛
  اتكرر بالظبط على الإيموليتر بـ`wm density 600` واختفى بعد الإصلاح؛ واتشاف
  ثابت على الهونر والشاومي.
- **القنوات والمواقع**: إخفاء/تعديل/حذف/ترتيب/استعادة، وفتح داخل التطبيق
  (Custom Tab اتأكد بـ`topResumedActivity`) أو خارجي. رجّعت قائمة المالك زي ما
  كانت بعد التجربة.
- **الاسبلاش**: يشتغل بس بعد غياب ٣٠ دقيقة (اختيار المالك)، اتشاف على الإيموليتر.

## ٣. اتصلح بالاختبار بس

**عدّاد «٨٧٥٩ ساعة» للفجر** على تليفون ناس في الإمارات: `DateFormat('HH:mm')`
بيكتب أرقام هندية تحت `ar_EG`، والـregex كان بيرجّع سنة. اتصلح + ٤ اختبارات.
ما اتكرّرش على جهاز (محتاج لغة نظام ar_EG).

## ٤. قرارات المالك

- أقل أندرويد **٧ (API 24)** — Flutter 3.38 مايدعمش ٥.
- الجولة: **ميزة واحدة في كل محطة بفريم حواليها**، ممنوع تشاور على زرار
  التبويب (محفوظة في الذاكرة).

## ٥. لسه مستنّي المالك

الـ١١ كليب تحت `adhan/video/` على R2 · عنوان المشكاة · المزامنة بين جهازين
(ماحدّش كمّلها — شوف `NEXT_SESSION_PROMPT.md`).
