# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول (المنهج الملزم)، وبعده **`OWNER_FINDINGS.md`** و
**`WORK_QUEUE.md`** — دول ملاحظات صاحب التطبيق من استخدامه الفعلي وهما اللي
بتشتغل منهم — وبعدهم `HANDOVER.md`.

**قبل ما تخطّط: اقرأ الكوتة (§2.0) وقول لصاحب التطبيق الرقم.** لو الواجهة اللي
انت فيها مش بتوريه، اطلب منه يقراه من شاشته — ما تخمّنش.

---

## الحالة عند التوقف — كلها متحققة النهارده مش من الذاكرة

- المُصدَر: **v3.15.0** على `eb210cf`. التاج على `master` والـ SHA بتاعه =
  `HEAD`، وإصدار واحد بس في المستودع.
- الـ APK المرفوع **اتنزّل من جيت هب واتفحص**: `CN=Rafeeq Al-Darb` على
  أندرويد ٩ فما فوق.
- `flutter analyze lib test` نضيف · `flutter test` **١٤٧ ناجحة** ·
  `py -3 scripts/verify_hosted_content.py` **١٥ مسار، صفر فشل** · الشجرة نضيفة.
- نسخة الإصدار (بـR8) اتركّبت على إيموليتر بعد إعادة تشغيل نضيفة، اشتغلت
  بـ**صفر ANR**، ونزّلت كتاب مكانش عندها.

**التوقيع:** جرادل بيوقّع ديبج **بقصد**. بعد `flutter build apk --release`
لازم `py -3 scripts/sign_release.py` — فخ ٤١. المفتاح في `../Rafeeq-Keys/`
وصاحب التطبيق مسؤول عن نسخته الاحتياطية.

---

## أول حاجة: مستنيين رد صاحب التطبيق على تلاتة

قال إنه هيجرّب v3.15.0 ويرجع. **ما تبدأش شغل جديد قبل ما تشوف رده** — دي
تلاتة هو اللي بيتحقق منهم دلوقتي:

1. **تنبيهات قبل/بعد الصلاة على تليفونه.** اتأكد على الإيموليتر إنها بتسجّل
   منبّهات حقيقية (٣٩ ← ٤٦ منبّه أول ما اتضبطت على ١٠ دقايق). الأرجح إنها
   **مضبوطة على صفر = مطفية** عنده. لو قال لسه مش شغالة، الفجوة بين «اتسجّل»
   و«ظهر» — إدارة بطارية الشركة.
2. **سلوك قفل الشاشة.** اتصلحت أربع مسارات، بس الـ OEM بتاعه هو الحكم.
3. أي حاجة جديدة يشوفها.

---

## المفتوح، وليه — اتنين بس

### شروح الشاملة — مقفولة من المصدر، مش من عندنا

طلبها، وأنا قلتله صراحة إنها مش هتتعمل، والسبب **مقروء من الصفحة نفسها** مش
مفترض:

- فتح الباري على الشاملة **نثر مقسوم صفحات من غير أي علامة لرقم حديث** — لا
  `id`، لا `data-hadith`، لا «[ح ٤٥٤٣]». يعني **مفيش طريقة برمجية** تجيب بيها
  «شرح البخاري ٤٥٤٣».
- والحجم: فتح الباري **٧٩٩٦ صفحة**، شرح النووي ٤٠٨٦، عون المعبود ٤٦٦٥، تحفة
  الأحوذي ٤٨٧٤ — **٢١٬٦٢١ صفحة**.

الأرقام لو حد رجع ليها: **1673 / 1711 / 5760 / 21662**.

اللي ينفع بصدق: يتضافوا **ككتب في المكتبة** (تُقرأ وتُبحث) بالخط الموجود.
إنما ربطهم بكارت الحديث حديثًا بحديث **مش ممكن من المصدر ده** — والموسوعة
الحديثية هي اللي عندها شرح لكل حديث، وهي شغالة وبتتحمّل لوحدها.

### الـ ANR — اتفسّر واتصلح، بس افهم حدوده

٦٥ عملية تحقّق كلاسات بـ٩٬٧٢٤ ملّي ثانية بقت **صفر**، وصفر ANR. **بس صاحب
التطبيق عمره ما شاف الـ ANR ده.** الإيموليتر x86_64 من غير ملف تعريف AOT
فبيتحقّق من كل دالة؛ تليفونه ARM وبيتحقّق من جزء صغير. **ما تقولوش إنك صلّحت
حاجة هو اشتكى منها.**

---

## حاجات مصلّحة وعليها اختبارات بس **محدّش شافها على جهاز**

كلها في v3.15.0. لو فتحت أي واحدة فيهم، حدّث `WORK_QUEUE.md`:

- إشعار المقولة والتطبيق مقفول (يفتح المقولة على طول ويتخطّى الإسبلاش)
- البحث في المصحف بعد إصلاح السوابق و«ال»
- البحث جوّه قايمة السور والأجزاء
- الختمة: قطع الآية عند كلمة كاملة، والخطوة الواحدة، و«من أي سورة»
- اتساع طابور التنزيل، وزر «إصلاح التحميلات» بعد سدّ التقبين
- كارت اختيار القارئ ومصدر التشغيل
- سلايدر صوت المنبّه

---

## ما تعملهاش تاني

- `flutter build apk --release` بيقتل الإيموليتر (فخ ٢٤).
- **الـ heredoc بيبوّظ `\n` والـ quotes (فخ ١١)** — اتلسعت منه مرتين في
  السيشن دي. استخدم أداة Write لأي سكربت فيه نص.
- **فخ ٢٣ ضرب فعلًا**: مولّد كتب `value: 'j\$j'` في Dropdown — الباك سلاش قبل
  `$` **escape** مش interpolation، وكل التلاتين جزء كانوا هياخدوا نفس القيمة
  والـ Dropdown كان هيرمي. `flutter analyze` قال «مفيش مشاكل».
  `no_escaped_dollar_test.dart` بيكسر البناء على ده دلوقتي.
- `if (!snapshot.hasData)` **بيرسم الفشل على إنه تحميل**. استخدم `FutureView`.
- `X as int` على صف قاعدة بيانات **ادعاء عن البيانات** — شغّل
  `py -3 scripts/db_type_audit.py` بعد أي إعادة بناء لقاعدة.
- صفحة مدفوعة (`Navigator.push`) مش بتترسم تاني لما اللي تحتها تترسم — لازم
  `Consumer` جوّاها.
- `TextDirection` غامضة: `easy_localization` بيصدّر بتاعة `intl`. استخدم
  `ui.TextDirection`.
- `adb shell input keyevent KEYCODE_BACK` بيطلّعك من التبويب كله.
