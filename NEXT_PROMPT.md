# كمّل رفيق الدرب من حيث وقفنا (2026-09-23)

اقرأ `CLAUDE.md` كاملًا (إلزامي)، ثم سطر الحالة أعلى `HANDOVER.md`. ردّ
بالعربي. في أول رد: اقرأ الكوتة حيّة (§2.0) وتحقق من Remote Control
بـ `ListAgents` (§2.0b).

## أين وقفنا
`master` مدفوع، `flutter analyze` نظيف، **507 اختبارًا تمر**، المكتبة **230
كتابًا**. **لا إصدار منذ v3.55.0** و`pubspec` = `3.55.0+57`. تليفون المالك
(شاومي، arm64، أندرويد 16) عليه نسخة **debug** فيها التسميع شغّال.

## افعل بالترتيب

1. **أكمل زحف المرحلة ٢.** خمسة زحوف بدأت في الجلسة السابقة وقد تكون انتهت:
   الطبري، القرطبي، فتح الباري، المغني، المجموع. تحقق أولًا:
   `ls -l scripts/book_text_build/` وابحث عن
   `tafsir_al_tabari, tafsir_al_qurtubi, fath_al_bari, al_mughni_ibn_qudamah,
   al_majmu_sharh_al_muhadhdhab`. **أي ملف بحجم صفر أو ناقص = أعد بناءه**
   (`py -3 scripts/build_book_text.py <id>`). ثم ابدأ الثلاثة الباقية:
   `siyar_alam_al_nubala`, `tahdhib_al_kamal`, `lisan_al_arab`.

2. **ارفع وافهرس على دفعات**: `py -3 scripts/library_phase2.py --report`
   واقرأ كل بطاقة في `scripts/library_phase2_out.txt`، ثم شغّله بدون
   `--report`. الأداة تتخطى أي كتاب موجود في الكتالوج، فتشغيلها مرتين آمن.
   بعد كل دفعة: حدّث العددين في `test/editor_notes_removed_test.dart`
   (الكتالوج والمعلَّمة)، و`flutter analyze` + `flutter test`، **وافتح
   المكتبة على الجهاز وشاهد الكتاب الجديد** — البايتات على الباكت ليست
   ميزة تعمل (مصيدة #27).

3. **جرّب على هاتف الأونور** (سيوصّله المالك): ثبّت نسخة debug نفسها، وافتح
   كتابًا ضخمًا، وجرّب التسميع. القياس المرجعي من الشاومي: «البداية والنهاية»
   4,918 صفحة — تنزيل وفهرسة 7.5 ث، فتح 4.3 ث، قفز لآخر صفحة 5 ث، Java heap
   ثابتة 10 MB.

4. **اللقطات الثلاث الناقصة**: الأذان، الحديث، درس التجويد (الباقي أُعيد
   تصويره). ثم `build_store_screenshots.py` و`build_tutorial_shots.py`.

5. **الإصدار حين يطلبه المالك فقط** (طلب ألّا نُصدر الآن): ارفع `pubspec`،
   `build_github_release.bat` (مصيدة #41)، احذف الإصدار السابق ووسمه عدا
   v3.51.0، ملاحظات عربية صادقة، وتحقق أن الوسم = `HEAD`.

6. **مؤجَّل بأمره**: نموذج تصحيح التجويد — ابحث عنه على الإنترنت **بعد**
   انتهاء كل ما سبق. الخلاصة المقيسة: نموذجنا يسمع الكلمات لا الأحكام؛ تصحيح
   التجويد يحتاج نموذجًا صوتيًا مدرَّبًا على أخطاء مُعلَّمة، وهذا مشروع بيانات
   لا مشروع كود.

## ما يعمل الآن ولا تكسره
* التسميع: `whisper_flutter_new` + `asr/whisper-tiny-ar-quran/ggml-model.bin`
  (77,691,713 بايت). **لا تُدخل `onnxruntime` ثانيًا** — التطبيق يحمل نسخة
  لصوت قارئ الكتب، ونسختان تتصادمان (فشل sherpa على الهاتف بـ
  «cannot locate symbol OrtGetApiBase»).
* تحويل النموذج: `scripts/export_quran_asr_onnx.py` ثم محوّل whisper.cpp،
  **والأوزان تُحوَّل إلى fp16 أولًا** وإلا كُتب ملف فاسد موسوم f16.

## قواعد سارية
لا متون مجردة؛ كتب ابن القيم وابن الجوزي والحكيم الترمذي وابن أبي الدنيا
تبقى؛ لا شيء لابن باز وابن عثيمين وابن جبرين وابن عبد الوهاب والألباني
**إلا التخريج**؛ الأسماء المتشابهة لا تُمَس؛ «الإيضاح» فيه 38 صفحة نقاط
متروكة عمدًا لارتباطها بأرقام صفحات شاشة الحج.
