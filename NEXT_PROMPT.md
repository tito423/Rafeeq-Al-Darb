# رفيق الدرب — ابدأ من هنا

اقرأ `CLAUDE.md` أولاً — هو طريقة العمل الملزِمة على هذا المستودع، وخاصة
§1.1 (ممنوع أي شيء وهمي)، §1.2 (المحتوى الديني)، §1.3 (لا شيء «تمام» قبل أن
يُشغَّل على جهاز ويُرى)، §2.0 (اقرأ الكوتة قبل أن تَعِد بشيء) و§3 (المصائد
المدفوعة ثمنها). ثم `HANDOVER.md`. صاحب المشروع يكتب بالعامية المصرية —
**ردّ عليه بالعربية**، والكود والتعليقات ورسائل الكوميت بالإنجليزية.

## أين توقّفنا بالضبط

`master` على `f6b71416`، الشجرة نظيفة، ومدفوعة إلى `origin`.

**إصداران منشوران عن قصد** — `v3.52.0` (الأحدث) و`v3.51.0`. الأخير هو
**نقطة الرجوع** ولم يُحذف بناءً على طلب صريح: «ابني الرليز ومش تحذف الموجود
ممكن يكون فيه خطا ولا حاجة». لا تحذف أيًّا منهما ولا وسمه.

| | |
|---|---|
| النسخة | `pubspec.yaml` `3.52.0+54`، `AboutScreen.appVersion` `3.52.0` |
| الـ APK | `dist/RafeeqAlDarb-v3.52.0.apk` — 260,277,487 بايت، sha256 `0c85b6c2…` |
| المعماريات | `arm64-v8a` + `armeabi-v7a` + `x86_64`، `minSdk` 24 |
| نقطة الرجوع | الفرع `known-good/v3.51.0` والوسمان `backup-2026-09-21c` و`v3.51.0` — لا تحرّكها |
| وللإصدار الجديد | `known-good/v3.52.0` على `b0380411`، والوسم `backup-2026-09-21d` على `bfd2bd5c` |
| النسخ خارج جيت هب | `../Rafeeq-Backups/Rafeeq-Al-Darb-2026-09-21b.bundle` — 693,225,542 بايت، تاريخ كامل، `git bundle verify` نجح؛ والأقدم عند v3.51.0 باقية بجانبها. و`RESTORE.md` يشرح العودة |

تم التحقق منه في 2026-09-21 قبل تسليم هذا البرومبت مباشرة:
`flutter analyze lib test` → **No issues found** · `flutter test` →
**456 نجحت، 2 تخطّي** · **10 من 10** مسارات على R2 ردّت 206 بنوع المحتوى
الصحيح · 7 لغات × **1,684** مفتاحًا متطابقة.

---

## المهمة الأولى — قسم العمرة يجب أن يصير قسمًا مستقلًّا فعلًا

> «قسم العمرة بالكامل هو معلومات مكس من الحج، انا عاوز العمرة قسم منفصل
> في معلوماته تمام عن الحج.»

**هو محقّ، وهذا ليس ما أصلحته الجلسة السابقة.** الجلسة السابقة نقلت فصل
«العمرة» إلى أول المسار وأخرجت «حج الصبي» منه — وهذا كل شيء. أما التسعة
الباقية فهي **فصول الحج نفسها** تُعرض داخل تبويب العمرة.

الدليل من لقطته: تبويب العمرة ← الخطوة ٣ «المواقيت» يفتح على
«الباب الثاني / في الإحرام / فصل / **في ميقات الحج**».

في `rafeeq_app/lib/features/hajj/data/hajj_guide.dart`، هذه الفصول معلَّمة
`tracks: _both` وكلها من متن الحج:
`preparation`، `mawaqit`، `ihram`، `nusuk`، `prohibitions`، `tawaf`،
`sai`، `visitation`، `counsel`. مسار العمرة = فصل `umrah` (ص٣٧٨–٣٨٧) +
هذه التسعة.

### ما يجب فعله

1. **اقرأ الكتاب أولًا، لا الكود.** المصدر هو «الإيضاح في مناسك الحج
   والعمرة» للإمام النووي، معرّف الكتاب `al_idah_fi_manasik_al_hajj_wal_umrah`
   (الشاملة 96232). افتح صفحاته الحقيقية واعرف بالضبط ما فيه عن العمرة:
   «الباب الرابع في العمرة» ص٣٧٨–٣٨٧، وداخل الفصول المشتركة فقرات تخصّ
   المعتمر بعينه (ميقات العمرة داخل باب المواقيت مثلًا، وأحكام المتمتّع).
   §1.4: افحص الصفحات بيدك قبل أن تكتب أي شيء يقصّها.
2. **القصّ على مستوى الفقرة، لا الفصل.** `HajjStep` يحمل أصلًا
   `fromPage/fromPara/toPage/toPara` — فبإمكانك أن تعطي مسار العمرة شرائح
   من الفصول المشتركة تخصّ العمرة وحدها، بدل الفصل كاملًا. كل شريحة
   **تُقرأ بالعين** ويُثبت أنها عن العمرة قبل إدراجها.
3. **لو لم يكفِ الكتاب، قل ذلك ولا تخترع.** §1.1 و§1.2: لا تكتب متن عمرة
   من عندك ولا تعيد صياغة كلام النووي. إن كان الجواب الأمين هو «الإيضاح لا
   يحوي دليل عمرة مستقلًّا»، فاعرض على المالك مصدرًا ثانيًا موثّقًا
   (مؤلِّف، طبعة، رابط) ودَعْه يقرّر — ولا تُضِف كتابًا إلى الكتالوج قبل أن
   يستجيب محتواه على العنوان العام (§1.1).
4. **الترتيب**: ما يحتاجه المعتمر، بترتيبه، وليس بينه شيء من الحج.

### وحاجة ثالثة قالها ولم تُنفَّذ

> «اخر تابين حج الصبي ووصايا للحاج مش مناسبة»

خرج «حج الصبي» فقط. **«وصايا للحاج» (`counsel`) ما زالت على المسارين.**
اسأله: تخرج من العمرة أم تبقى؟ هي سطر واحد في `hajj_guide.dart`.
ولا تكتب في `HANDOVER.md` أنه «اختار أن تبقى» — لم يُسأل قط.

---

## المهمة الثانية — تدقيق المصحف النصّي، كلمة كلمة وعلامة علامة

> «فيه كوارث في المصحف النصي، علامات داخل في الايات، وحرف الـ ج متكرر اكتر
> من مرة. انا عاوزك تشيك على المصحف النصي كلمة كلمة، علامة علامة، اية اية.»

### ما قِسته بالفعل — ابنِ عليه ولا تعده

* **النصّ في قاعدة البيانات سليم.** `rafeeq_app/assets/data/quran_local.db`،
  جدول `ayahs`، عمود `text_uthmani`، **6,236** آية. آية الكرسي (٢:٢٥٥)
  تحمل ٥ × `U+06DA` (ARABIC SMALL HIGH JEEM)، وهي علامة وقف شرعية من رسم
  المصحف. **«ج» التي يراها ليست حرفًا زائدًا ولا خطأ في النصّ** — فلا
  تحذفها ولا «تنظّف» النصّ (§1.2: لا تُعدَّل آية).
* تعداد علامات الوقف في المصحف كله (`U+06D6`–`U+06ED`): `U+06ED` ٤٨٠٧،
  `U+06DF` ٣٩٨٨، `U+06E2` ٢٤٤٥، **`U+06DA` ١٩٧٢**، `U+06D6` ١٦٨٢،
  `U+06E5` ١٢٥٧، `U+06E6` ٩٩٥، `U+06D7` ٦٠٣، `U+06DE` ١٩٩، وبقيتها أقل.
* **الخط سليم وكامل.** `rafeeq_app/assets/fonts/AmiriQuran-Regular.ttf` هو
  Amiri Quran 1.003 الأصلي، 1,446 محرفًا، `GPOS` 19,006 بايت و`GSUB`
  15,958 بايت، و**كل الـ 21 علامة موجودة في الـ cmap**. فليست مشكلة خط
  ناقص.
* **السبب الأرجح، وهو ما عليك إثباته أو نفيه أولًا:** في نصّ تنزيل
  العثماني، علامة الوقف مكتوبة **كلمة مستقلّة بين مسافتين**
  (`ٱلْقَيُّومُ ۚ لَا`). قِسته: **4,578 علامة وقف قائمة بذاتها في 2,719 آية
  من 6,236** — ٤٤٪ من المصحف. والتصميم يضبط السطور (justify)، فتتباعد
  «الكلمات» وتقف العلامة وحدها في فراغ كأنها حرف مستقل — وهو تمامًا ما
  تُظهره لقطاته.

### ابدأ من هنا

1. **أثبت السبب قبل أن تُصلح.** الملفات:
   `rafeeq_app/lib/features/quran/presentation/widgets/mushaf_text_page.dart`
   (981 سطرًا، يستعمل `fontFamily: 'AmiriQuran'` في أربعة مواضع)،
   `mushaf_ayah_row.dart`، و`rafeeq_app/lib/features/quran/data/text_layout_provider.dart`
   (منطق ضبط السطور). افحص كيف تُقسَّم الكلمات وكيف تُوزَّع المسافات.
2. **الإصلاح الصحيح طباعي لا نصّي**: ألصِق علامة الوقف بالكلمة السابقة لها
   عند العرض (بلا مسافة قبلها)، وامنع كسر السطر قبلها، واترك
   `text_uthmani` في قاعدة البيانات **كما هو حرفًا بحرف**.
3. **ثم دقّق فعلًا، كما طلب: آية آية.** اكتب سكربتًا في `scripts/` يمرّ على
   الـ 6,236 آية ويُخرج تقريرًا إلى **ملف UTF-8** (ويندوز cp1256 لا يطبع
   العربية — مصيدة #10)، يرصد: كل محرف خارج المجموعة المتوقَّعة، كل علامة
   قائمة بذاتها، كل مسافة مزدوجة، كل آية يختلف طولها عن مرجع خارجي. وقارن
   عيّنة مقروءة بالعين بصفحات المصحف المطبوع.
4. **افتح الشاشة وانظر.** المصحف النصّي: `المصحف` ← `العرض` ← `وضع المصحف`
   ← النصّي. صفحات لقطاته: **٤٢** (آية الكرسي) و**٤٧**. وكبِّر بالإصبعين —
   مصيدة #48 لا تظهر إلا بالتكبير.
5. تحذير: `flutter analyze` و456 اختبارًا لم يروا أيًّا من هذا. اختبار يقارن
   نصًّا مُطبَّعًا بنصّ مُطبَّع ينجح وكلاهما فارغ — مصيدة #47.

---

## المهمة الثالثة — المقدمة الجزرية تحتاج شرحًا

> «الجزرية دي محتاجة شرح، خليه يجيب حاجة فري تشرحها موثقة.»

المستوى الثاني في التجويد يعرض **متن** المقدمة الجزرية شِعرًا مجرّدًا:
«٢٧ - وَالأَخْذُ بِالتَّجْوِيدِ حَتْمٌ لَازِمُ … مَنْ لَمْ يُصَحِّحْ القُرَانَ آثِمُ».
هذا نظم لا يُفهم بلا شرح، وقد حُذف منه زرّ «استماع» في 3.52.0 فصار المتن
وحده على الشاشة. المطلوب: **شرح مجاني وموثَّق** يُعرض بجانب كل بيت.

### القيود — كلها إلزامية

* **مجاني الحقوق فعلًا.** مؤلِّف متوفّى منذ زمن بعيد، أو رخصة صريحة.
  مؤلِّف معاصر = مشكلة حقوق، حتى لو كان الملف متاحًا. راجع
  `CONTENT-LICENSES.md` — هو سجلّ المصدر والحقوق في هذا المستودع، وكل
  إضافة تُقيَّد فيه.
* **مُسمّى**: اسم الشارح وسنة وفاته والطبعة والرابط، ويظهر ذلك في شاشة
  المصادر (§1.2).
* **لا شيء مخترَع.** ممنوع أن تكتب شرحًا من عندك أو تُعيد صياغته «بأسلوب
  أسهل». إن لم تجد مصدرًا تتحقّق منه، **قل ذلك وقف** (§1.1).
* **لا تُدرِج في الكتالوج قبل أن يستجيب المحتوى** على العنوان العام
  بطلب `HEAD` أو مدى بايتات (§1.1).

### مرشّحان حقيقيان وجدتهما في الفهرس المحلي

`py -3 scripts/shamela_index.py find "الجزرية"` (8,598 كتابًا؛ ولا تستعمل
بحث الشاملة نفسه — يبحث **داخل** الكتب لا في عناوينها، مصيدة #17):

| المعرِّف | العنوان | التصنيف |
|---|---|---|
| **21580** | فتح رب البرية شرح المقدمة الجزرية في علم التجويد | التجويد والقراءات |
| **17065** | الروضة الندية شرح متن الجزرية | التجويد والقراءات |

**لم أتحقّق من مؤلِّف أيٍّ منهما ولا من سنة وفاته ولا من حقوقه** — هذه أول
خطوة عندك، قبل أي سطر كود. وهناك شروح كلاسيكية مشهورة خارج الفهرس تستحقّ
البحث على archive.org (مصيدة #9: استعلاماته تحتاج إنجليزية) مثل «المنح
الفكرية» لملا علي القاري و«الحواشي الأزهرية» لخالد الأزهري — كلاهما ليس في
الفهرس المحلي.

### وبعد اختيار المصدر

1. اقرأ صفحات حقيقية منه قبل كتابة أي مُحلِّل (§1.4)، وانتبه لمصيدة #34
   (حواشي المحقّق تُكتب داخل متن الشاملة) ومصيدة #35 (الآية بين `{ }`
   والحديث بين `(( ))`).
2. الشرح يُربط **ببيت المتن**، لا بالصفحة — فالشاشة تعرض أبياتًا مرقّمة.
3. اعرضه بحيث يبقى المتن هو الأصل والشرح تحته أو خلف زرّ، واسأل المالك عن
   الشكل قبل أن تبنيه كاملًا.
4. `flutter test test/lesson_heading_bare_test.dart` وأخواته موجودة لأن
   مُطبِّعًا عربيًّا أعاد سلسلة فارغة فأكل الدروس كلها (مصيدة #47). أي
   مُطبِّع جديد **يجب أن يُثبَت أنه يُرجع شيئًا** قبل أن يُقارَن به.

---

## ما زال مفتوحًا من قبل — لا تنسَه

* **الأذان: «أرفع الإشعار لفوق مفيش حاجة توقفه»، و«أطفيه يرجع يتكرر».**
  **لم يُشخَّص إطلاقًا.** أُبلِغ عنه أكثر من مرة ولم يُفتح الملف بعد. ابدأ
  من `lib/core/services/` (خدمة الأذان والإشعارات) ومن `NotificationRouter`
  (مصيدة #31: مُعالِج نقر واحد للتطبيق كله). أعِد إنتاجه على الجهاز
  بـ `adb shell cmd alarm set-time` (مصيدة #28) بدل انتظار الفجر.
* **التوتوريال** ما زال خارج سلسلة التشغيل الأول؛ مكانه تبويب «المزيد» ←
  «الأدوات».

## قبل أن تقول «خلصت» (§2.2)

1. `cd rafeeq_app && flutter analyze lib test` → No issues found
2. `flutter test` → الكل ينجح (456 الآن، 2 تخطّي)
3. شغِّله على `emulator-5554` أو هاتف و**شاهد** كل تغيير بعينك
4. `HANDOVER.md` يعكسه
5. مُودَع ومدفوع — و`.\cp.bat "<ماذا فعلت>"` نقطة حفظ في خطوة واحدة
   (بلا نقاط ولا أقواس في الرسالة؛ `cmd.exe` يختنق بها)

## ملاحظات تشغيلية من هذه الجلسة

* **الكوتة**: عند التسليم كان **97٪ من الحد الأسبوعي مستهلكًا**، ويتجدّد
  2026-09-21 الساعة 22:00 UTC. اقرأ الرقم حيًّا في أول ردّ (§2.0) ولا
  تبدأ مرحلة طويلة لا تكفيها.
* **Remote Control**: افحصه بـ `ListAgents` في أول ردّ (§2.0b). لا يُفعَّل
  إلا عند بدء الجلسة بـ `claude rc`.
* الإيموليتور مات مرة أثناء التثبيت وعاد وسجلّ الحزمة موجود بلا نشاط
  (`pm path` فارغ). **«Success» من `adb install` قبل انهيار الجهاز ليست
  تثبيتًا** — ألغِ التثبيت وثبِّت من جديد واقرأ الخرج كاملًا.
* `adb` مع مسار يبدأ بـ `/` داخل Git Bash يحتاج `MSYS_NO_PATHCONV=1`
  (مصيدة #46) — وهو ما يلزم لـ `uiautomator dump /sdcard/ui.xml`، وهو
  الطريقة الصحيحة لمعرفة إحداثيات أي زر بدل التخمين.
