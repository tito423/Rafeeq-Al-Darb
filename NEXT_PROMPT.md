# كمّل رفيق الدرب من حيث وقفنا

اقرأ بالترتيب: `CLAUDE.md` كاملًا (خصوصًا §1.2 الجديد: لا تساهل في أي شيء يمس القرآن، وتصويب كلمات القرآن
المقتبسة بعد رؤية صفحة المصحف بالعين)، ثم `TASK_FOLLOWUP.md` (قسم «Next step» وسطور الـ Log من 2026-09-26)،
ثم `NEXT_SESSION_PROMPT.md`، ثم `TRAPS.md` للمنطقة التي ستعمل فيها.
ردّ بالعربي. في أول رد: الكوتة حيّة (`get_usage`) فقط — لا تتكلم عن Remote Control.

## أين وقفنا (2026-09-26 ~02:10 دبي)
- المنشور ما زال **v3.63.7**. `master` متقدم عليه بكثير، ولا شيء من عمل 25 مساءً و26 منشور. لا تنشر إلا بطلب المالك.
- analyze نظيف، **587** اختبار ناجح، المستضاف 10/10 = 206 (منها حزمة العلوم الجديدة `sciences/v2/quran_sciences.zip`
  32,146,611 بايت + نسختها على GitHub content-mirror، والقديمة `sciences/quran_sciences.zip` باقية لنسخة 3.63.7).
- **الإعراب انتهى في الكود:** الدعاس بدل تسميات Corpus، 27 مقطعًا من المطبوع، 6 تصويبات لكلمات قرآنية + 3 تصويبات
  لشرح الكتاب من الجدول/درويش/الإعراب الميسر (مُعلَّمة في النص)، و215 إحالة «سبق إعرابها» تعرض الإعراب السابق في إطار.
  رُئي على المحاكي (بناء قبل تصويبات الشرح): البقرة 5، البقرة 31 ← 23، شاشة المصادر.

## الخطوة التالية بالضبط
1. **هاتف المالك Xiaomi** (`adb -s BYKRKJPRC6O7FMHU`، 23078PND5G، Android 16): عليه البناء الموقّع (01:58) مثبّت نظيف
   02:02:48 بأمره، **لم يُفتح بعده**. الهاتف حساس للصوت ليلًا: `cmd media_session volume --stream 3 --set 0` قبل الفتح.
2. **مشكلة الاتجاه (المالك: «هتلاقي فيه العجب في الأورينتيشن … بص على الاسبلاش الأول»):** سجّل فيديو للإقلاع البارد من
   PowerShell (لا Git Bash — فخ 46): `adb shell screenrecord --time-limit 12 /sdcard/x.mp4` ثم افتح التطبيق، اسحب الملف،
   واستخرج الإطارات بـ ffmpeg (فخ 14: موجود على الجهاز — ابحث عن مساره). المرصود حتى الآن: التدوير التلقائي مفعّل،
   التطبيق يطلب UNSPECIFIED ويتبع الحساس (ROTATION_90) بينما لانشر شاومي ثابت طولي؛ الاسبلاش ظهر بالعرض في أول فتح.
   لم يُعرف السبب بعد — لا تُصلح قبل رؤية الإطارات.
3. بعد الاتجاه: على الهاتف تأكد من خط مجمع الملك فهد + ترتيب سطور البقرة ص2 + الإعراب (نزّل الحزمة 32.1 MB، البقرة 5،
   البقرة 31 ← 23، النور 21 فيه «[تصويب عن …]»).
4. **الإحالات الـ222 غير المثبتة** (`scripts/irab_daas_refs_for_owner.md`): بقاعدة المالك لا تُرفع له قبل ملاحقتها —
   ابحث في كتب الإعراب الأخرى على tafsir.app (`get.php?src=aljadwal|iraab-aldarweesh|aliraab-almuyassar&s=&a=&ver=1`)
   عن الآية التي يشير إليها «مثلها»، وأثبتها بنص، ثم أعد `resolve_irab_daas_refs.py` → `build_irab_daas_table.py` →
   رفع الحزمة (`upload_sciences_pack.py` + `github_content_mirror.py --only content-mirror`) وحدّث `sciencesDbBytes` واختباره.
5. تجميلي: فواصل أسطر الشاملة داخل الجملة في بعض المقاطع (مثل 2:23 «فعل ماض / ناقص»).
6. لم يُفحص: Avast على هاتف المالك أظهر `MalwareFoundActivity` في آخر المهام — اسأل المالك قبل أي شيء.
