# كمّل رفيق الدرب من حيث وقفنا

اقرأ بالترتيب: `CLAUDE.md` كاملًا، ثم `TASK_FOLLOWUP.md` (قسم «Next step» — أعلاه أحدث)، ثم `ADAPTIVE_PLAN.md`
و`SHAMELA_IMPORT_PLAN.md` و`DORAR_ISLAMQA_NOTES.md`، ثم `TRAPS.md` للمنطقة التي ستعمل فيها. ردّ بالعربي.
في أول رد: الكوتة حيّة (`get_usage`) فقط — لا تتكلم عن Remote Control. المالك قال «كمّل ومتشغلش بالك بالكوتة».

## أين وقفنا (2026-09-26 ~20:30 دبي)
- المنشور **v3.65.1** (التاج `e0d4066a` = HEAD وقتها): الشاملة داخل التطبيق + علامة «موجود في مكتبتك».
- analyze نظيف، **612** اختبارًا ناجحًا.
- **كل مرة تنشر: أعطِ المالك رابط صفحة الإصدار + رابط الـ APK المباشر** (ذاكرة release-link-always).

## غير منتهٍ — يُعاد ولا يُصدَّق (بالترتيب)
1. **نقل بيئة التطوير إلى `E:\DevEnv`** (المالك وافق؛ القرص C كان ممتلئًا). تم: متغيرات المستخدم مضبوطة
   (`ANDROID_HOME`/`ANDROID_SDK_ROOT`=`E:\DevEnv\Android\Sdk`، `GRADLE_USER_HOME`=`E:\DevEnv\gradle`،
   `PUB_CACHE`=`E:\DevEnv\pub-cache`، `ANDROID_AVD_HOME`=`E:\DevEnv\avd`)، المحاكيات **نُقلت** (بيانات الهاتف مُسحت)،
   والـ SDK وGradle وPub **نُسخت** — ونسخ C ما زالت موجودة. الباقي: `rafeeq_app/android/local.properties`
   (`sdk.dir`) → E، `flutter config --android-sdk E:\DevEnv\Android\Sdk`، `flutter pub get`، ثم تحقق: `flutter doctor`،
   `build_github_release.bat`، المحاكي يقلع من E ويتثبت عليه التطبيق. **بعدها فقط** احذف نسخ C
   (`%LOCALAPPDATA%\Android\Sdk`، `%USERPROFILE%\.gradle`، `%LOCALAPPDATA%\Pub\Cache`) وأبلغه بالمساحة المحررة.
2. **مركز «الدرر السنية»** مكتمل في الكود (تخريج الأحاديث + 11 موسوعة بفهرس شجري وقارئ أقسام وحواشٍ) ومختبر على
   صفحات حقيقية، **لم يُبنَ ولم يُرَ**: ابنِه وشاهد: المكتبة ← أيقونة الدرر ← الموسوعة العقدية ← الشجرة ← القسم 10،
   وقسمًا فقهيًا، وبحث التخريج.
3. **التحقق من أي حديث في أي مكان بالدرر** (موافَق عليه): إجراء موحّد «تخريج من الدرر» على أحاديث كتب الحديث
   وحديث اليوم والموسوعة الحديثية والأذكار، **وعلى أي نص يُحدَّد داخل كتب المكتبة**؛ ورقة سفلية بالأحكام (المحدّث بالاسم)،
   استخراج المتن (بلا إسناد ولا تشكيل)، وحفظ النتيجة لكل حديث.
4. **الإسلام سؤال وجواب**: بحث وقراءة داخل التطبيق عند الطلب (لا API؛ الـ sitemap؛ «جميع الحقوق محفوظة» → لا استيراد جماعي).
5. **إصدار 3.66.0** بعد انتهاء الكل، مع الرابطين.

## باقٍ من قبل (لا يسبق ما فوقه)
- جولة الشاشات بالطول والعرض على كل المقاسات + مشي الريموت على Google TV (`ADAPTIVE_PLAN.md`، المرحلة C).
- الشاملة: استيراد كتاب كبير، الاستكمال بعد قتل التطبيق، إشعار الاستيراد، رف «من الشاملة» — لم تُرَ.
- الإحالات الـ222 غير المثبتة في الإعراب؛ منتقي الوقت اللاتيني (ليس كودنا)؛ تنبيه Avast على الشاومي.
