اقرأ `CLAUDE.md` ثم `HANDOVER.md` (كتلة الحالة في أوله مكتوبة اليوم 2026-09-20،
وهي **التسليم الثاني** في نفس اليوم — الأول تحتها).

## ابدأ من هنا بالضبط

### 0) قبل أي شيء: اسأل المالك عمّا لم يكتمل

أوقف الجلسة السابقة بناء الإصدار **v3.44.0** في منتصفه بقوله:

> «قبل ما تنشر وترفع فيه حاجة لسه مخلصتش»

ولم يقل ما هي. **اسأله أولًا**، ولا تبنِ إصدارًا قبل أن يجيب. الشجرة نظيفة
والعمل كله مكمَّت، فلا شيء ضائع — لكن كوميتين لم يُدفعا بعد إن لم يكن قد
دُفعا: `740d0302` و`13be4f06`. تحقّق بـ `git log --oneline origin/master..HEAD`
وادفعهما إن لزم.

### 1) نشر الباك-إند — معطّل على المالك وحده

موقع المراجعة أصبح يرفع ملاحظات المراجعين تلقائيًا إلى نقطة نهاية جديدة في
الـWorker (`sync_backend/src/index.ts`، المسارات `/review`، بلا تسجيل دخول
Google عمدًا). الكود مكتوب و**مُختبَر محليًا** بـ `npx wrangler dev`: رُفعت
ملاحظة بالعربي وسُحبت سليمة عبر `POST /review`، `GET /review?reviewer=`،
`GET /review/reviewers`، وعمل الـCORS preflight.

**لكنه لم يُنشر**، لأن `npx wrangler whoami` يقول «not authenticated» وتسجيل
الدخول من حق المالك وحده. الأمران:

```
cd "E:\My Projects\Rafiq-Al-Darb\sync_backend" && npx wrangler login
cd "E:\My Projects\Rafiq-Al-Darb\sync_backend" && npx wrangler deploy
```

بعد النشر **تحقّق بنفسك**، لا تصدّق أنه اشتغل: افتح
https://tito423.github.io/rafeeq-review/ ، اكتب اسمًا، ضع ملاحظة على أي نص،
ثم افتح تبويب «المراجعون» وتأكد أنها ظهرت باسمك. إلى أن يحدث ذلك، المراجع
يرى «تعذّر الحفظ — سيُعاد الإرسال» وتبقى ملاحظته على جهازه فقط.

### 2) ثلاث حاجات لم تُجرَّب على جهاز — أعد تجربتها ولا تصدّقها

كلها في إصدار v3.43.0 المنشور، ومكتوبة في ملاحظاته صراحة:

* **تظليل البسملة أثناء التلاوة المستمرة.** افتح سورة (ليست الفاتحة ولا
  التوبة) وشغّل التلاوة من الآية الأولى: المفروض أن يقع التظليل على **سطر
  البسملة وحده** ثم ينتقل للآية. لم يُشاهَد لأن التلاوة تأتي من الإنترنت وكل
  تحميل مستضاف يفشل على المحاكي (فخ #13).
* **التكبير بإصبعين في المصحف النصي** والرجوع بضغطة. المحاكي يرفض
  `sendevent` على `/dev/input` فلا يمكن حقن لمسة بإصبعين.
* **شريط المصحف في الوضع النصي**: السورة يمينًا، الجزء شمالًا، ورقم الصفحة
  أسفل الشاشة. شوهد في المصحف المصوّر فقط؛ الوضع النصي لم يُفتح.

### 3) المشروع الكبير: تصغير التطبيق من ٣٤٠ إلى ~١٠٠ ميجا

هذا ما طلبه المالك ووُضعت أرقامه ولم يبدأ. الشكل الذي وصفه:

> سبلاش ← اختيار اللغة ← الأذونات ← التوتوريال السريع ← **شاشة تنزيل المواد**،
> المستخدم ينزّل ما يريد. وتُحذف شاشة «اختر مصحفك» من التطبيق كله.

القياسات (كلها مقيسة لا مقدَّرة):

| | ميجا |
|---|---|
| أرضية ثابتة (محرّك فلاتر + كود + مكتبات) | **٧٧.٨** |
| يبقى مدمجًا (نص القرآن ٥.٣٨، خلفيات الاقتباسات ٤.٤٤، الهوية ٣.٨٧، خطوط القرآن ٢.٢، أذان واحد، الأذكار مفصولة، الترجمات، الكتالوجات) | **~٢١.٥** |
| **الإجمالي المتوقَّع** | **~١٠٠** |

ينتقل إلى شاشة التنزيل: `quran_sciences.db` **١٣١.٧** (سبعة تفاسير ٥٦.٥ ميجا
نصًّا، وترجمات، وإعراب، ومعاني كلمات — **والأذكار بداخلها، ٤٠ كيلو فقط،
يجب فصلها في ملف مستقل**) · ١٣ أذانًا **٤٢.٨** · `hadith.zip` **٢١.٢** ·
موسوعة الحديث **١٦.١** · الكتب المدمجة **٧.٢** · ١٥ خط واجهة **٦.٨** ·
المصحف الورقي **٧٠.٩**.

**حذار:** هذا ثمانية مسارات تنزيل جديدة، وكل مسار يجب أن يُشاهَد شغّالًا على
جهاز — فخ #27 في `CLAUDE.md`: حزم HadeethEnc نزلت ونجح فكّها ومرّت ٦٦ اختبارًا
وظلّ الزر مكانه للأبد. قسّمه مراحل تنتهي كل واحدة على كوميت شغّال.

### 4) نضافة معلّقة

* **مساحة ميتة على R2**: مصحف الـSVG القديم **٢٨٦ ميجا** وصفحات المصاحف
  الأربعة الممسوحة، لم يعد شيء منها مستخدمًا. حذفها قرار المالك ولا رجعة فيه،
  فلا تحذف قبل أن يقول.
* كتب المكتبة على موقع المراجعة تُقرأ مباشرة من R2؛ ضُبطت سياسة CORS للبكت
  بـ `scripts/r2_set_cors_for_review_site.py` (GET/HEAD من
  `tito423.github.io` و`localhost:8765` فقط). لو تغيّر عنوان الموقع فلا بد من
  تحديثها.

## قواعد لا تُكسر

* **لا تسلّم شيئًا ناقصًا.** «بربك أبدًا أبدًا متعملش شي ناقص، بكره حاجة زي دي
  وبتفاجئ» — إن تعذّر جزء فقل ما هو ولماذا، ولا تُنهِ عند ٨٠٪ وتقول خلص.
* **المالك يتابع الكوتة بنفسه.** «امشي ورا اللي بقولهولك وانا هتابع الكوته» —
  لا توقف العمل لتتفاوض على الميزانية، واستمر في `.\cp.bat` بعد كل خطوة.
* **لا تنشر إصدارًا إلا بأمره**، وابنِه بـ `build_github_release.bat` وحده
  (يوقّع بالمفتاح الحقيقي — فخ #41).
* **مصحف ورقي واحد، وتظليله مضبوط.** لا تقترح طبعة لا تملك إحداثياتها
  المنشورة، ولا تُعِد تشغيل خط أنابيب القياس البصري إلا بطلبه.
* بلّغ بالأرقام المقيسة لا المقدَّرة، وقل صراحةً ما لم تره بعينك.
