اقرأ `CLAUDE.md` ثم `HANDOVER.md` (كتلة الحالة في أوله مكتوبة 2026-09-21).

# 🔴 قبل أي شيء: فيه نقطة رجوع، احترمها

المنشور **v3.51.0**، وهو **نقطة الرجوع المعتمدة** بطلب صريح من المالك قبل أن
يمرّ على التطبيق عميل آخر (غالبًا Codex) بمراجعة كاملة.

| | |
|---|---|
| فرع ثابت | `known-good/v3.51.0` — **لا يُحرَّك ولا يُحذف مهما حصل** |
| وسم احتياطي | `backup-2026-09-21c` — نفس الكوميت |
| وسم الإصدار | `v3.51.0` — نفس الكوميت (`6d3389d2`) |
| حزمة كاملة خارج GitHub | `../Rafeeq-Backups/Rafeeq-Al-Darb-2026-09-21.bundle` — **693,190,329** بايت، كل التاريخ والفروع والوسوم، `git bundle verify` يقول «complete history» |
| الـAPK | `dist/RafeeqAlDarb-v3.51.0.apk` (**260,277,487** بايت) وعلى Releases |
| التعليمات | `RESTORE.md` في جذر المستودع |

**لو خرّبت حاجة:**
```
git switch -c rescue-<date> known-good/v3.51.0      # راجع أولًا، بلا مسح
git reset --hard known-good/v3.51.0                  # لو قررت
git push --force-with-lease origin master
adb install -r dist/RafeeqAlDarb-v3.51.0.apk         # بلا إلغاء تثبيت
```

**⚠️ الشيئان اللذان لا يعوّضهما شيء:** حزمة النسخ الاحتياطي أعلاه، ومفتاح
التوقيع في `../Rafeeq-Keys/` — كلاهما **خارج** المستودع. لو ضاع المفتاح فلن
يُثبَّت أي تحديث فوق التطبيق إلا بإلغاء تثبيت يمحو مئات الميجابايت.

# أولًا: صدّق ولا تثق

جلسة 2026-09-20/21 صلّحت **تسعة عشر** شيئًا رصدها المالك بنفسه. **ما شُوهد على
جهاز**: التشغيل الأول كاملًا (السبلاش، صفحة الأذونات وعلامات الصح فيها، الانتقال
إلى «اختر مصحفك» وعليها علوم القرآن)، ومعرض الصور في «شرح ميزات التطبيق»،
وقائمة «المزيد»، وشاشة «العرض» في الوضعين، وشريط المصحف.

**ما لم يُفتح على جهاز بعد — افتحه أولًا قبل أي شغل جديد:**

1. **المزامنة** — المفروض أربعة مفاتيح فقط تسافر (`SyncService.syncedStateKeys`:
   `khatma_list_v1`، `ayah_notes_v1`، `quran_last_page`،
   `tasbeeh_custom_target`). غيّر إعدادًا على جهاز وتأكد أنه **لا يسافر**.
2. **ثيم RGB مع المصحف الورقي** — التدرّج المتحرّك يجب ألا يظهر خلف الصفحة.
3. **التكرار** — غيّر القارئ ثم «تكرار الآية»: يجب أن يقرأ **بصوت من اخترت**،
   وشريط «جارٍ تكرار الآية» فيه «إيقاف» **ظاهر** ويختفي بانتهاء الصوت.
4. **التنزيلات** — كل صف يفتح شيئًا، والرجوع يعيدك إليها، وصفّ «علوم القرآن»
   يفتح `SciencesPackScreen` بالأرقام المحسوبة.
5. **أرقام علامات الآيات** في المصحف النصي، و**تبويبات المكتبة** الأربعة.
6. **إذن شاشة الأذان الكاملة** (الصف السادس في صفحة الأذونات).

# ثانيًا: المفتوح

### 1. إشعار الأذان — بلاغ متكرر، غير مشخّص

«لما يجي الأذان وارفع الاشعار لفوق مفيش حاجة اوقفه منها» و«الأذان بيشتغل
أطفيه يرجع يتكرر».

الكود يقول العكس: `AdhanNotifications.kt` يبني الإشعار بـ`setOngoing(true)`
وزرّي «إيقاف» و«كتم»، و`AdhanService.onStartCommand` يرجّع `START_NOT_STICKY`،
و`rearmAfterFiring` يحسب اليوم التالي صحيحًا. **فما يراه لا يطابق الكود.**

**كرّره قبل أن تغيّر سطرًا** (فخ #28):
```
adb shell settings put global auto_time 0
adb shell cmd alarm set-time <epoch-ms قبل الأذان بدقيقة>
adb shell dumpsys notification --noredact | grep -A40 rafeeq
```

### 2. التوتوريال

طلبه: سبلاش ← الأذونات ← **التوتوريال** ← المزامنة و«مرحبًا بك» والخيارات.
المنفَّذ: سبلاش ← الأذونات ← الأونبوردنج. التوتوريال ما زال داخل التطبيق.

### 3. التصغير

APK **260,277,487** بايت، كان **339,920,197** في v3.43.0. الخريطة المقيسة في
`docs/size/`.

**قاعدة ثابتة:** `minSdk` 24 وثلاث معماريات، **ولا تشيل واحدة** — جُرِّب ونزل
APK **يُثبَّت ويموت عند الفتح**؛ التفاصيل في `build.gradle.kts`.
و`hadith.zip` وHadeethEnc مدمجان **بطلب صريح منه**.

# قواعد لا تُكسر

* **لا تنشر إصدارًا إلا بأمره**، وابنِه بـ`build_github_release.bat` وحده (فخ #41).
* **بلّغ بالأرقام المقيسة**، وقل صراحةً ما لم تره بعينك.
* **`adb install -r` يفشل بصمت** إذا كان على الجهاز بناء موقّع بمفتاح مختلف —
  `INSTALL_FAILED_UPDATE_INCOMPATIBLE` يذهب إلى stderr ويبتلعه أي `| tail`.
  كلّفني فحصين كاذبين في جلسة واحدة. **أزل التثبيت أولًا، واقرأ خرج التثبيت.**
* **التنقّل الأعمى على المحاكي يضيّع التوكنز.** استخدم
  `adb shell uiautomator dump /sdcard/ui.xml` وخذ الإحداثيات من `bounds`.
* **`uinput` يعمل على المحاكي** رغم أن `sendevent` مرفوض بـSELinux — للمس بإصبعين.
* **مصحف ورقي واحد.** لا تقترح طبعة لا تملك إحداثياتها المنشورة.
* **مفتاح ترجمة غير موجود يُطبع كما هو** (فخ #8). حدث مرتين اليوم؛
  `literal_translation_keys_test` يمسكه — شغّله.
