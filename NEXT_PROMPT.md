# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول (المنهج الملزم)، وبعده `OWNER_FINDINGS.md` — ده ملف
ملاحظات صاحب التطبيق من استخدامه الفعلي، وهو اللي بتشتغل منه، وبعده
`HANDOVER.md`.

**قبل ما تخطّط: اقرأ الكوتة (§2.0) وقول لصاحب التطبيق الرقم.** لو الواجهة
مش بتوريه، اطلب منه يقراه من شاشته.

## الحالة عند التوقف

- المُصدَر على جيت هب لسه **v3.12.0**. كل الإصلاحات اللي بعده **مدفوعة على
  `master` ومش متصدّرة** — لسه محتاجة إصدار جديد.
- `flutter analyze lib test` نضيف · `flutter test` **١٠٩ ناجحة** · الشجرة نضيفة.
- **التوقيع:** جرادل بيوقّع ديبج بقصد. بعد `flutter build apk --release` لازم
  `py -3 scripts/sign_release.py` — فخ ٤١. المفتاح في `../Rafeeq-Keys/`.

## أول حاجة: حاجات مصلّحة ومحدّش شافها على جهاز

اتعملت واتأكدت بالتحليل والاختبارات بس **ما اتفتحتش على جهاز**. افتحها
واتأكد بنفسك، ما تصدّقش الكوميت:

1. **اسم السورة** — روح صفحة ٢٣٥ في المصحف النصي. المفروض الهيدر يكتب
   «هود · يوسف» مش «يوسف» لوحدها. جرّب صفحة ٢٢١ كمان (المفروض «يونس · هود»).
2. **قائمة السور اتقفلت** على الشمرلي والهندي والنستعليق. افتح واحد منهم
   واتأكد إن أيقونتي «السور» و«الجزء» مش ظاهرين، وإن باقي الأدوات شغالة.
3. **إشعار المقولة والتطبيق مقفول** — اقفل التطبيق تمامًا، استنى إشعار مقولة،
   اضغط عليه: المفروض يفتح المقولة على طول من غير إسبلاش.
4. **اختيار الأذان** — افتح إعدادات الأذان → الأذان الافتراضي، اختار واحد،
   والعلامة لازم **تتحرك عليه فورًا**. دي كانت أكبر شكوى وسببها إن الصفحة
   المدفوعة مكانتش بترسم تاني.

## بعدها — المفتوح، بالترتيب

5. **الـ ANR.** logcat سجّل `Waited 5007ms for MotionEvent` بعد التشغيل بدقيقة،
   على نسخة **debug**. شغّل `flutter run --profile` واقرأ DevTools timeline لأول
   ١٠ ثواني. **ما تقولش إن التطبيق سلس قبل ما يبقى في رقم.**
6. **الرجوع في كارت الحديث.** السحب لقدّام شغال على الجهاز، والرجوع **لأ**.
   السهمين اللي في الكارت بيوروا الحالة — بص على السهم الشمال: منوّر ولا مطفي؟
   لو مطفي يبقى `hasPrevious` بترجع false والمشكلة في `_history` في
   `daily_hadith_provider.dart`. لو منوّر يبقى المشكلة في تنفيذ `previous()`.
   **اعمل اختبار يثبت الغلط قبل ما تصلّحه.**
7. **الأذان.** حاجتين منفصلتين:
   - النسبة: الملفات مطابقة بالبايت لمصدرها على archive.org (اتأكدت)، بس
     تسمية المصدر نفسها محدّش تحقق منها، وصاحب التطبيق **بيقول بأذنه إنها
     غلط**. أذنه دليل أقوى من اسم ملف. يا إما تسمية موثّقة من مصدر مسمّى، يا
     إما الاسم يتشال (§1.1/§1.2).
   - الجودة: **٦ من ١٠ متشفّرة ١٦ kb/s** (اتقاست بـ ffmpeg). ده لوحده كفاية
     يخلي صوت معروف مش متعرّف عليه.
8. **زر تجربة الأذان بيفتح الإعدادات** — أعد إنتاجها، غالبًا إذن الـ exact
   alarm أو الـ full-screen intent.
9. **تنبيهات قبل/بعد الصلاة مش بتشتغل على تليفونه هو** (شغالة على الإيموليتر
   بدليل `dumpsys alarm`). الفجوة بين «المنبّه اتسجّل» و«الإشعار ظهر» — غالبًا
   إدارة البطارية بتاعة الشركة. محتاج قياس على تليفونه، مش على الإيموليتر.
10. **شاشة سنن النسائي طلعت فاضية** في سكرين شوت بتاعه — أعد إنتاجها.
11. **شرح الحديث** — HadeethEnc فيها شروح والتطبيق شايلها أصلًا.
12. **تجميل شريط أدوات المصحف** — شكله الأفقي خلص، التجميل لأ.

## ما تعملهاش تاني

- `flutter build apk --release` بيقتل الإيموليتر (فخ ٢٤).
- الكونسول cp1256 مش بيطبع عربي (فخ ١٠)؛ الـ heredoc بيبوّظ `\n` (فخ ١١).
- `TextDirection` غامضة: `easy_localization` بيصدّر بتاعة `intl`. استخدم
  `ui.TextDirection`.
- صفحة مدفوعة (`Navigator.push`) **مش** بتترسم تاني لما الشاشة اللي تحتها
  تترسم. لو محتاجة تقرا provider لازم `Consumer` جوّاها.
