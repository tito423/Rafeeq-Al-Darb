# كمّل رفيق الدرب من حيث وقفنا (2026-09-24)

اقرأ `CLAUDE.md` كاملًا (إلزامي)، ثم `PLAN.md` (الخطة المرحلية)، ثم قسم WIP وأول جدول في `HANDOVER.md`، ثم `docs/AUDIT_2026-09-24.md`. ردّ بالعربي. في أول رد: اقرأ الكوتة حيّة (§2.0) وتحقق من Remote Control بـ `ListAgents` (§2.0b).

## أين وقفنا
- آخر إصدار **v3.60.0** (الوسم `3b4247e1`). بعده على `master` تعديلات **غير مُصدَرة**: تسجيل الخروج يمسح بيانات الحساب فقط (D1)، حماية `/sync` في الـ Worker (منشورة، B1+B5)، خلفيات الأذكار ودليل المسلم على R2 وGitHub ثم Unsplash (C1).
- `flutter analyze` نظيف، **568 اختبارًا تمر**.
- نسخة احتياطية لكل محتوى R2 على GitHub Releases (`content-mirror` 348، `content-mushaf` 604، `content-surah` 228) — لا تُحذف أبدًا مع تنظيف الإصدارات. التحقق: `py -3 scripts/github_content_mirror.py --verify`.
- قاعدة المالك الدائمة: خمسة مصادر لكل ما يُنزَّل، ولا تخطٍّ لآية أبدًا (ذاكرة `five-backups-never-skip`).

## افعل بالترتيب — «خلّص أول حاجة طلبتها»
0. **لم يُرَ على جهاز بعد — افتحه أولًا على emulator-5554:** خلفيات بطاقات الأذكار ودليل المسلم الجديد تظهر (C1). ابنِ ثم شغّل المحاكي (مصيدة #24؛ وإن سقط المحاكي بخطأ `UpdateLayeredWindowIndirect` فهو من الجهاز المضيف — أعد تشغيله).
1. **المرحلة 1 في `PLAN.md` — صفحة «التحميلات المبدئية»** (الصفحة بعد الأذونات، اليوم «اختر مصحفك»): الاسم، النبذة (تجعل التطبيق يعمل بلا إنترنت، ويُنصح بالواي فاي)، **إصلاح اهتزاز صف «علوم القرآن» مع تغيّر النسبة** (أبلغ عنه المالك سابقًا ولم يُصلح — تحقق بـ `screenrecord` لا بلقطة ثابتة)، صفوف: المصحف الورقي، علوم القرآن، تلاوة آية بآية (قارئ من أسرع خادم يُقاس عند فتح الصفحة، مع ترشيح أصغر حجم)، التلاوة الكاملة (ترشيح أصغر حجم)، نموذج التسميع، صوت قارئ الكتب (المالك لديه ملاحظة عليه — اسأله)، وأي شيء آخر يلزم للعمل بلا إنترنت، ومجموع المساحة من الأحجام الحقيقية. جرّبها على **تثبيت جديد** (`adb uninstall` ثم install). ثم إصدار.
2. **المرحلة 2 — أكمل المراجعة** من الأسهل للأصعب: A1 أسماء الأزرار لقارئ الشاشة، DB1، B2 (التحقق من توكن Google محليًا)، B3، B4.
3. المرحلة 3 (نسخة Play بلا كتب كاملة، والمتاجر) والمرحلة 4 (تسميع لحظي مثل Tarteel) — **لا تبدأهما قبل أن يطلب المالك**.

## على المالك
- **غيّر مفتاح R2** من Cloudflare (موجود في تاريخ الـ repo العام) وضع الجديد في `scripts/.env`.
- الدومين حين يكون جاهزًا.

## تذكير بالطريقة
بعد كل خطوة: `flutter analyze lib test` + `flutter test` (**تحقق من كود الخروج**) ثم المحاكي ثم commit + HANDOVER. الإصدار: `build_github_release.bat` فقط، احذف الإصدار السابق ووسمه، أبقِ v3.51.0 وإصدارات `content-*`.
