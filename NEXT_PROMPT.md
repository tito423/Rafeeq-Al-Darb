# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول (ده المنهج الملزم)، وبعده `HANDOVER.md` من فوق،
وبعده `NEXT_SESSION_PROMPT.md`.

**قبل ما تخطّط أي حاجة: اقرأ الكوتة المتبقية دلوقتي (§2.0) وقول لصاحب التطبيق
الرقم.** لو الواجهة اللي انت فيها مش بتوريه، اطلب منه يقراه من شاشته — ما
تخمّنش.

## الحالة عند التوقف

- المُصدَر: **v3.12.0** على `17a5d9b` — التاج على `master` والـ SHA بتاعه =
  `HEAD` (اتأكد). إصدار واحد بس في المستودع.
- `flutter analyze lib test` نضيف · `flutter test` **١٠٠ ناجحة** · الشجرة
  نضيفة ومدفوعة.
- كل اللي في v3.12.0 اتشاف على `emulator-5554` — مفيش حاجة مطبّقة وغير مجرّبة.

## أول حاجة تعملها: قرار مفتاح التوقيع

**ده مستني قرار صاحب التطبيق، مش شغل ناقص.** الـ APK بتاع v3.12.0 موقّع
بمفتاح الديبج بتاع أندرويد — اتقاس على الملف المرفوع نفسه:

```
Signer #1 certificate DN: C=US, O=Android, CN=Android Debug
SHA-256: 6ce659534f0f68978cb32e7d14162fda4b6780684b4b7f6e4a0f299d856632ce
```

و`rafeeq_app/android/app/build.gradle.kts` لسه فيه سطر فلاتر
`// TODO: Add your own signing config`.

المعنى: مفتاح الديبج مش سر — أي حد يقدر يبني APK أندرويد يقبله كتحديث لده.
لتطبيق سايدلود صاحبه بينزّله من ريليزاته بنفسه، الخطر العملي صغير.

**التحويل لمفتاح حقيقي بيتطلب إلغاء تثبيت التطبيق الأول** (أندرويد بيرفض
تحديث موقّع بمفتاح مختلف)، يعني بيروح معاه ~٣٤٨ MB صفحات مصحف منزّلة وكل
الإعدادات المحلية.

- **لو قال آه:** اعمل keystore، حطه بره المستودع، ضيفه لـ `.gitignore`،
  اربطه في `signingConfigs` عن طريق `key.properties` مش في الـ gradle مباشرة،
  ابني، و**أكّد بـ `apksigner verify --print-certs` إن الـ DN اتغيّر** قبل ما
  تقول إنها خلصت. وقول له صريح إنه لازم يعمل uninstall قبل التنصيب.
- **لو قال لأ:** سيبها، وشيل الـ TODO من `build.gradle.kts` وحطّ مكانه تعليق
  بيقول إن ده قرار مقصود، عشان محدش يفتكرها سهو.

## بعد كده — الحاجات المفتوحة، بالترتيب

1. **السلاسة اتقاس بجد.** الجلسة دي **فشلت** تقيس الـ jank:
   `dumpsys gfxinfo` بيرجّع صفر إطارات لتطبيق فلاتر، و
   `dumpsys SurfaceFlinger --latency` رجّع صفر صفوف على أندرويد ١٦.
   **ما تعتمدش على أي كلام عن السلاسة في `HANDOVER.md` — مفيش رقم.**
   شغّل `flutter run --profile` وافتح DevTools، وخد رقم حقيقي من الـ frame
   chart وانت بتقلّب في المصحف وفي شاشة المكتبة (أتقل شاشتين).
2. **الملفات الكبيرة.** `library_screen.dart` ١٬٥٨٢ سطر و
   `ayah_sciences_sheet.dart` ١٬٥٤٨ — اتقسّموا لما يبقى في سبب حقيقي تلمسهم،
   مش قبل كده. `book_catalog.dart` ٤٬٠٦٣ سطر بيانات، سيبه.
3. **`app_config.dart:13`** — الـ TODO بتاع GitHub raw. اتقاس النهارده:
   الصفحات ٠٠١/٠٥٠/٢٠٠/٤٠٠/٦٠٤ كلها بترجع 200 في ٠٫٤–١٫٠ ثانية بحجم
   ١٩٦–٦٢٦ KB. شغّال. الملاحظة تحذير من التوسّع مش عيب — القرار لصاحب التطبيق
   لو عايز يعمل ميرور على الباكت.

## حاجات اتحرقت فيها وقت — ما تعملهاش تاني

- `scripts/code_health.py` بيبلّغ عن `DownloadCategoryX` كاسم مش مستخدم.
  **مستخدم في ٥ أماكن.** أسماء الـ extension في Dart عمرها ما بتتكتب عند
  الاستدعاء. اعمل `grep` على أعضاء أي حاجة قبل ما تمسحها.
- `flutter build apk --release` بيقتل الإيموليتر (فخ ٢٤). خلّص أي تجريب على
  الجهاز الأول.
- الكونسول cp1256 ومش بيطبع عربي (فخ ١٠)، والـ heredoc بيبوّظ `\n` (فخ ١١).
