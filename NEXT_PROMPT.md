# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول، وبعدين ابدأ من هنا بالظبط.

---

كمّل تنظيم الكود من **المرحلة ٤** في `REFACTOR.md`. المراحل ١ و٢ و٣ خلصت
واتأكدت على `emulator-5554` واتعملها commit وpush، والإصدار **v3.20.0**
اترفع على GitHub (التاج بيشاور على `e8dd7d8` = HEAD، الـAPK متوقّع
بـ`scripts/sign_release.py` مش خارج من `flutter build` على طول).

## اللي اتعمل واتشاف على الجهاز

| | من | لـ | اتشاف |
|---|---|---|---|
| `library_screen.dart` | ١٬٦٢٥ سطر / ٢٥ كلاس | ٩٩ سطر + ٤ تبويبات + `data/islamic_websites.dart` + `widgets/book_card.dart` | ✅ التبويبات الخمسة كلها |
| `ayah_sciences_sheet.dart` | ١٬٥٤٣ سطر / ٢٠ كلاس | ١٨٣ سطر + ٧ ملفات في `widgets/ayah_sciences/` | ✅ الثلاث تبويبات على الآية ٢:٧ |
| `quran_screen.dart` | ١٬٧٠٠ سطر | ١٬٠٤٧ سطر + ٦ ملفات في `widgets/mushaf/` | ✅ الشريط بالاتناشر أكشن |

`flutter analyze lib test` نضيف و**١٩٩** تست بيعدّوا.

## ابدأ من هنا

1. **المرحلة ٤** — طلّع الداتا من `presentation` في التمن أماكن اللي
   `REFACTOR.md` سمّاها. نفس الطريقة اللي اشتغلت تلات مرات: انقل البلوك،
   خد بلوك الـimports من الملف الأصلي وزوّده `../` حسب العمق، بعدين شغّل
   `scripts/strip_unused_imports.py` (لو مش موجود، هو ٣٧ سطر: بيقرا
   `unused_import` / `unnecessary_import` من تقرير `flutter analyze`
   ويمسح السطور دي بالظبط ويعيد لحد ما التقرير يفضى — أكتب تاني، متخمّنش
   بالعين).
2. **المرحلة ٥** — التمن method channels، خمسة منهم جوّه
   `configureFlutterEngine` اللي طوله ٣٠٠ سطر في `MainActivity.kt`.
3. **المرحلة ٦** — تست حارس: سقف لطول الملف، ومنع أي ليستة داتا جوّه
   `presentation`. **حدّد السقف على الحقيقة المقاسة** — أطول ملف دلوقتي
   `quran_screen.dart` بـ١٬٠٤٧ سطر، والباقي تحت ٦٠٠.

## حاجات ناقصة ولازم تتعمل تاني — متثقش فيها

- **`quran_screen.dart` لسه ١٬٠٤٧ سطر**، وكلهم تقريبًا `_QuranScreenState`
  لوحده. تقسيمه أكتر مش نقل ميكانيكي — محتاج تطلّع الحالة المحفوظة
  (font scale، سرعة التمرير التلقائي، ملء الشاشة، الوضع، آخر صفحة) في
  controller. ده **مرحلة ٣ب** ولسه ماتعملتش.
- **توجيه إشعار تحميل المصحف لسه ماتجربش على الجهاز.** لازم تبدأ تحميل
  مصحف حقيقي وتضغط الإشعار نفسه وتشوف بيروح فين. الترويسة الجروپ للتلات
  جروپات التانية (`files` / `recitations` / `ruqyah`) **مستحيلة من
  الباكدج نفسه** ومكتوبة كده في `test/download_notification_routing_test.dart`.
- **الاسكرول بار ماظهرش تاني عند التمرير** لما اتجرب — السحب الصناعي ماحركش
  الصفحة أصلًا، فالقياس باطل. جرّبه بإيدك.

## أسئلة المالك لسه من غير رد (اتسألت تلات مرات)

- الـ١١ أوبچكت اللي تحت `adhan/video/` على R2 (٧٤٬٠٨٠٬١٥٤ بايت) — يتمسحوا؟
  الكليبات نفسها اتشالت من التطبيق خلاص.
- عنوان شغّال لـ**موقع المشكاة**؟
- سؤاله عن فتح **الشاملة** جوّه التطبيق وفهرسة كتبها في المكتبة.
