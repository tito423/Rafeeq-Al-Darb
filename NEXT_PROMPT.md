# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول (المنهج الملزم)، وبعده **`WORK_QUEUE.md`** (أول قسمين:
التاسعة I1–I13 والتامنة G1–G9)، وبعدها `HANDOVER.md`.

**قبل ما تخطّط: اسأله عن الكوتة (§2.0).** مقدرتش أقراها من الواجهة.

---

## الحالة عند التوقف

- المُصدَر: **v3.18.0** على `master`. أول حاجة:
  `gh release view v3.18.0 --json tagName,targetCommitish,assets` وتأكد إن SHA
  التاج = `git rev-parse HEAD` وإن فيه إصدار واحد بس.
- `flutter analyze lib test` نضيف · `flutter test` **١٨٦ ناجحة**.
- الـAPK متوقّع بـ`scripts/sign_release.py`.

---

## أول حاجة: اللي اتعمل وماحدش شافه — اعمله تاني

1. **«وضع المصاحف» مش شغال نهائي (G1)** — ماتكررش عندي. اطلب منه سكرين شوت
   أو فيديو قصير للي بيحصل قبل ما تغيّر أي حاجة.
2. **تشغيل فيديوهات الأذان ورا بعض (G5)** — السويتش في شاشة إعدادات الأذان تحت
   اختيار الفيديو (بيظهر بس في وضع الفيديو). لا السويتش اتشاف ولا الانتقال بين
   المقاطع. نزّل مقطعين وشغّل معاينة الأذان.
3. **توقيت نص الأذان (G6)** — اتقاس بـffmpeg في ٩ تسجيلات من ١٤، بس ماحدش تفرّج
   عليه قدام أذان حقيقي. `adb shell cmd alarm set-time` (فخ ٢٨) لقبل الأذان بدقيقة.
4. **التلاوة المستمرة من الآية المختارة في الوضع العرضي (I5)** — الطولي اتشاف.
5. **تنزيل تلاوة كاملة لآخرها (١١٠ سورة)** — اتشاف أول ١٠ بالترتيب وفي الخلفية،
   مش لحد الآخر، ولا الإيقاف والاستكمال من نص سورة.
6. **سحب شريط التمرير الجانبي (I13)** — الأسهم اتشافت، السحب لأ.
7. **نافذة اختيار مجلد في المشغّل (I9)** — واجهة النظام، ماتفتحتش.
8. **كل ده على تليفونه** — الإيموليتر x86.

---

## حاجات لازم تعرفها

- **تنزيل التلاوات الكاملة في الـholding queue بتاع background_downloader
  (Native).** بيرتّب بالأولوية وبعدين `creationTime` — عشان كده كل مهمة ليها
  وقت إنشاء متباعد مللي ثانية بترتيب السور (`_enqueue` في
  `quran_audio_library.dart`). ماترجعش لطابور Dart — مابيتحركش والتطبيق في
  الخلفية.
- **الحالة `enqueued` = «في الانتظار»**، مش تنزيل ٠٪.
- **التلاوة المستمرة للمصحف النصي بس** (تظليل التجويد صفحة ٧٧ نازل سطر).
- **الضغط على آية = تحديد + كارت.** التلاوة من «تلاوة الآية» أو «التلاوة المستمرة».
- **الإيموليتر بـ`-no-snapshot-save` بيرجع لحالة 3.15.0 كل إقلاع** — أي تحميل
  أو إذن أو ملف اتحط بيضيع. مش bug.
- مشغّل واحد بس في التطبيق (`AyahAudioService.player` / `claimForMusic`).

## ما تعملهاش تاني

- `flutter build apk --release` بيقتل الإيموليتر (فخ ٢٤) — اقفله الأول.
- `Set-Content -Encoding utf8` في PowerShell 5.1 بيكتب BOM.
- heredoc في bash مع عربي وعلامات تنصيص بيفشل — اكتب السكربت بـWrite.
- `adb shell input text` مابيكتبش في حقول Flutter هنا.
- `adb shell ls /sdcard/...` من Git Bash بيتحوّل لمسار ويندوز — حط
  `MSYS_NO_PATHCONV=1`.
- بعد `flutter build apk --release` لازم `py -3 scripts/sign_release.py`.
