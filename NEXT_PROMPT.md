# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول، واقرأ الكوتة المتبقّية قبل ما تخطّط أي حاجة — لو
الواجهة اللي أنت فيها ما تعرضهاش، اسأل المالك يقرأها من شاشته.

**الحالة:** آخر ريليز منشور **v3.23.0** ومطابق لـ`HEAD` (`18be905`) وموقَّع
بالمفتاح الحقيقي عبر `scripts/sign_release.py`. الشجرة نظيفة ومدفوعة على
`master`. مافيش شغل خالص مستنّي النشر.

نصوص التطبيق العربية كلها **فصحى** مش عامية — ده طلب صريح ويسري على أي نص جديد.
وممنوع تنشر ناتج `flutter build apk` مباشرة؛ لازم يعدّي على `sign_release.py`.

---

## ١. تنزيل التلاوات آية بآية — من أول وجديد (أهم بند مفتوح)

المالك: «عملناه قبل كده وفشل فشل ذريع… عايزك تعمله احترافي وتضبط الداونلودر
بتاعه علشان ما يحصلش موضوع التهنيج واللخبطة اللي كانت بتحصل قبل كده».

المطلوب: أسرع وأوثق مصدر، تنزيل على الهاتف، **كارت خاص في «التنزيلات»**،
وقراءة وسماع آية بآية **بدون إنترنت**.

ابدأ من:

- `AppConfig.quranAudioBackupBase` = everyayah.com، وهو المصدر الأساسي وبيرد على
  **Range requests** — يعني التنزيل المقطوع يكمّل من مكانه بدل ما يبدأ من أول،
  وده بالظبط اللي كان ناقص في المحاولة القديمة.
- `RecitationSource.hasVerifiedMirror` بيقول مين من القرّاء متأكَّد إن له نسخة
  هناك. **ما تضيفش قارئ للكتالوج قبل ما أول ملف له يردّ فعلًا** (§١.١).
- `background_downloader` بنفس مجموعات التنزيل الموجودة، و**اقرأ الفخ ٤٥** قبل
  ما تسجّل أي callback: `registerCallbacks` بيحوّل الـupdates stream للحالة
  والتقدّم بس، و`taskNotificationTapCallback` لوحده آمن.
- الملفات القديمة اتشالت في 3.17.0 وفيه `purgeLegacyAyahFiles` ليها.

**ما تعتبرش الشغل خلص إلا لما تنزّل سورة كاملة على الإيمولاتور، تقفل الإنترنت،
وتسمعها آية آية.** «البايتات موجودة على الجهاز» مش معناها إن الميزة شغّالة —
ده فخ ٢٧ بالحرف.

## ٢. تسجيل الدخول بجوجل + مزامنة على درايف

المالك **اختار الطريقة دي** (مش Firestore): البيانات في `appDataFolder` بتاع
حساب المستخدم نفسه — مجانية مهما زاد عدد المستخدمين، وإحنا مش ماسكين بيانات حد.

**مستنّي منه** (وهو سأل ازاي يعملها، والخطوات اتبعتتله في الشات):

1. مشروع على `console.cloud.google.com` → تفعيل **Google Drive API**.
2. شاشة الموافقة (OAuth consent screen): نوع **External**، وضع **Testing**،
   وإضافة إيميله في **Test users**. النطاق المطلوب واحد بس:
   `https://www.googleapis.com/auth/drive.appdata`.
3. **Credentials → Create credentials → OAuth client ID → Android**:
   - Package name: `com.tito.rafeeq_aldarb`
   - SHA-1 الديبج: `BE:6D:45:79:0B:95:BA:8F:5E:03:B2:4F:DA:30:03:EC:85:D0:8A:40`
   - SHA-1 الإصدار: من `../Rafeeq-Keys/rafeeq-release.jks` (الباسورد في
     `KEYSTORE-PASSWORD.txt` جنبه) —
     `keytool -list -v -keystore ../Rafeeq-Keys/rafeeq-release.jks`
   - **يعمل عميلين**، واحد لكل بصمة، أو يضيف البصمتين.

العميل الأندرويدي مالوش secret، فمافيش حاجة سرّية تتكتب في الريبو — بس أي ملف
`google-services.json` يفضل بره الجيت زي `scripts/.env`.

المطلوب بعدها: كارت في «المزيد» يقلب بعد الدخول ويعرض الإيميل وتقدّم الاستخدام
ديناميكيًا، بلون الثيم وأنيمتد. والمزامنة: الإعدادات + الختمة + تقدّم القراءة +
عدّادات الأذكار والمسبحة.

## ٣. كتب الشاملة — مبنيّة ومرفوعة ومش متحطّة في الكتالوج

تلات دفعات جاهزة على R2 ومالهاش مدخل في `book_catalog.dart`:

| الملف | المؤلف | العدد |
|---|---|---|
| `scripts/shamela_added_bulugh_al_maram.json` | ابن حجر | ١٠ |
| `scripts/shamela_added_hilyat_al_awliya.json` | أبو نعيم | ٩ |
| `scripts/shamela_added_sahih_al_adab_al_mufrad.json` | الألباني | ١٠ |

في كل ملف: الحجم المقاس، الكارت الأصلي، و`printMatches`. **الباقي**: تغيير
الـslugs لأسماء مقروءة (دلوقتي كلها منقحرة وحشة زي
`thdhyr_alsajd_mn_atkhadh_alqbwr_msajd` — شوف الـRENAME في سجلّ الـgit)، مسح
الأوبچكتات القديمة من R2 بعد النقل، مدخل في `book_catalog.dart`، ووصف بالسبع
لغات.

**٤ مؤلفين مستحيل يوصلوا ١٠** لأن الشاملة مالهاش عندهم حاجة تانية: التبريزي،
ابن قدامة (مختصر منهاج القاصدين)، الخضري، خالد محمد خالد. المالك قال «اللي مش
متوفر مش تتعب نفسك».

## ٤. حاجات مفتوحة، ومنها حاجة مش مثبتة

- **قفزة ثواني العدّاد في الإشعار**: الأساس بقى بيتصحّح مرتين لكل صلاة بدل
  مرة، بس **القفزة نفسها ما اتكرّرتش على الإيمولاتور** — يعني **مش مثبت إنها
  اتصلحت**. لو المالك لسه شايفها، دي أول حاجة تتفتح تاني.
- **١٣ كتاب من الكتب المضافة ما اتفتحوش على الجهاز** غير «فضائل القرآن».
  افتح واحدًا من كل دفعة قبل ما تعتبرها خلصت.
- الـ١١ أوبچكت تحت `adhan/video/` على R2 (~٧٤ ميجا) — المالك لسه ما قالش
  يتمسحوا.
- عنوان شغّال لموقع المشكاة.

---

## اللي خلص واتأكد على الجهاز في السيشن اللي فاتت

- **تعليم التجويد**: ٢٣ درسًا، نصّها من «تيسير أحكام التجويد» بمدى صريح
  (صفحة:فقرة → صفحة:فقرة) مقروء من النصّ الحقيقي فقرةً فقرة، وزرّ «استمع»
  بيشغّل الآية مجوَّدة و«إيقاف» بيوقفها فعلًا. التقدّم متخزّن باسم الدرس مش
  برقمه.
- **«في مثل هذا اليوم»**: ٣٦٦ يومًا لكل لغة، **صفر يوم فاضي**، ٤٣٨٨ حدثًا
  بالعربية و٤٣٩٢ بالإنجليزية، والبطاقة اتفتحت من التاريخين الاتنين.
- وضع التركيز بتلات وجهات · الإشعار الذكي (تصاعدي ساعة ثم تنازلي) · الاسم
  المفضّل بزخرفة · التوتوريال بالسبوت لايت · زرّ «وضع المصحف» · رفرفة
  الشاشة · تنظيم الكود ٦ مراحل · فحص أمني (zip-slip + فلتر الروابط).
