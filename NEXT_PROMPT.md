# كمّل رفيق الدرب من حيث وقفنا (2026-09-23 مساءً)

اقرأ `CLAUDE.md` كاملًا (إلزامي)، ثم سطر «HANDOVER 2026-09-23 (evening)» وقسم
«NEXT CHANGES» في أول `HANDOVER.md`. ردّ بالعربي. في أول رد: اقرأ الكوتة
حيّة (§2.0) وتحقق من Remote Control بـ `ListAgents` (§2.0b).

## أين وقفنا
`master` مدفوع، `flutter analyze` نظيف، **529 اختبارًا تمر**. آخر إصدار
**v3.57.0** (الوسم `19330ebd`)، و`HEAD` بعده بخمسة commits: عمل الحفظ غير
المُصدَر وملاحظات HANDOVER. `pubspec` = `3.57.0+59`. المكتبة **239 كتابًا**.
**المالك أكّد على هاتفه: التسميع يعمل** بعد إصلاح النموذج العام.

## افعل بالترتيب — «صلح كل ده اللي فات»

0. **لم يُرَ قط وهو يعمل — افتحه أولًا على emulator-5554** (commit
   `a51a47c5`): في جلسة حفظ، (أ) زر «اختر السورة والآية» في شريط العنوان يفتح
   قائمة، و«ابدأ من هنا» يعيد الجلسة من السورة/الآية المختارة بكل الآيات بعدها؛
   (ب) شريحة القارئ فوق «استمع» تفتح قائمة القرّاء (المنزّل أولًا «على
   الجهاز»)، و«استمع» يشغّل ذلك القارئ (كان المنشاوي دائمًا). ابنِ debug
   **قبل** تشغيل المحاكي (مصيدة #24).

1. **«استمع» يبقى على «إيقاف» على هاتف المالك** (v3.57.0، بلا أي تلاوة
   منزّلة). غير مفسَّر: حدود الـ20 ثانية في 3.57.0 يُفترض أن تنهي الانتظار.
   **لا تخمّن:** اطلب منه توصيل الهاتف بـ USB واسحب
   `adb logcat | grep -iE "AyahAudioService|ExoPlayer|just_audio"` أثناء
   الضغط على «استمع».

2. **اختيار الثيم في أول شاشة بعد الإسبلاش** — «اختر مصحفك»
   (`onboarding_screen.dart`) تظهر داكنة على تثبيت جديد. اعرف لماذا أولًا، ثم
   أضف اختيار الثيم كما يُعرض اختيار اللغة في شاشة الأذونات. جرّبه على **تثبيت
   جديد** (`adb uninstall` ثم install).

3. **ثبّت سطر «٠ / ٦٠٤ صفحة محفوظة»** بجانب نسبة التحميل في نفس الشاشة؛ يطلع
   وينزل مع الرقم. أرقام بعرض ثابت وارتفاع ثابت، وتحقق بتسجيل
   (`screenrecord` + ffmpeg) أثناء تنزيل حقيقي — لقطة ثابتة لا تُظهر القفز.

4. **غيّر «اختر مصحفك»** و«اختر الرواية المفضلة لديك» (`onboarding.title`
   و`onboarding.subtitle`) في اللغات السبع: مصحف واحد ورقي، رواية واحدة.

5. بعد كل بند: `flutter analyze lib test` + `flutter test` (**تحقق من كود
   الخروج، لا تمرّره عبر `| tail`** — مرّ commit باختبار فاشل هكذا اليوم)،
   ثم شاهده على المحاكي، ثم commit.

6. **الإصدار حين يطلبه المالك فقط**: ارفع `pubspec` و`AboutScreen.appVersion`،
   شغّل الاختبارات بعد الرفع، ثم `build_github_release.bat`، احذف v3.57.0
   ووسمها (أبقِ v3.51.0)، ملاحظات عربية صادقة، وتحقق أن الوسم = `HEAD`.

7. **مؤجَّل بأمره**: نموذج تصحيح التجويد — ابحث عنه **بعد** كل ما سبق.

## ما يعمل الآن ولا تكسره
* **التسميع**: نموذج القرآن يُحفظ باسم `ggml-tiny.bin` (الاسم الذي تفتحه
  whisper_flutter_new) ويُتحقَّق منه بـ sha256 `5a9a479f…`؛ النموذج العام
  بالحجم نفسه تمامًا (بصمته `be07e048…`). لا تغيّر اسم الملف.
* **دليل الحج** يقرأ باب «الحج والعمرة» من «الفقه المنهجي» (شاملة 6369،
  `scripts/build_hajj_guide_book.py`)، والجزيري تحت كل خطوة، وبطاقة «المواقيت
  اليوم» من haj.gov.sa. **لا أرقام صفحات ولا «من كتاب كذا» تحت النصوص** (أمر
  المالك)؛ المصادر في شاشة المصادر. اختبار التغطية يمنع أي فقرة بلا خطوة.
* أي كتاب في قسم «الحديث» يُضمَّن في الـ APK إلا ما في `hostedHadithBooks`.

## قواعد سارية
الكتب الحديثة مرحَّب بها، **لكن الخمسة مستبعدون**: ابن باز، ابن عثيمين، ابن
جبرين، ابن عبد الوهاب، الألباني (إلا التخريج). ذكرهم في كتاب غيرهم يبقى، ونصّهم
داخل كتاب يُحذف. لا متون مجردة. كتب ابن القيم وابن الجوزي والحكيم الترمذي وابن
أبي الدنيا تبقى. «الإيضاح» فيه 38 صفحة نقاط متروكة عمدًا.
