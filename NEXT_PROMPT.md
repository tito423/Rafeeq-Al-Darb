# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول، وبعدين ابدأ من هنا بالظبط.

---

آخر كوميت `47b0a05` على `master`، والشجرة نضيفة و`analyze` نضيف و**٢٠٧** تست
بيعدّوا. آخر ريليز مرفوع هو **v3.21.0**، وهو **أقدم من الشغل اللي بعده** —
يعني كل اللي تحت عنوان «اتعمل واتشاف» مش موجود في الـAPK اللي على GitHub لسه.

## أول حاجة تعملها: ريليز v3.22.0

لأن اللي بينزّل الـAPK دلوقتي بياخد نسخة من غير وضع التركيز المعدّل ولا
التوتوريال الجديد ولا إصلاحات المالك الأخيرة.

1. `pubspec.yaml` → `3.22.0+21` و`about_screen.dart` → `appVersion = '3.22.0'`
   (فيه تست بيربط الاتنين: `test/about_version_test.dart`).
2. `flutter build apk --release` وبعدها **`py -3 scripts/sign_release.py`** —
   ممنوع ترفع خرج `flutter build` مباشرة، هو موقّع بمفتاح الديبج (فخ ٤١).
3. امسح الريليز القديم وتاجه، وبعدين
   `gh release create v3.22.0 --target master …` وتأكد إن SHA التاج =
   `git rev-parse HEAD`.

---

## اللي المالك طلبه ولسه **ماتعملش**

### ١. «وضع المصحف» — قال إنه مش شغال، وأنا مقدرتش أكرّره
على `emulator-5554` الزرار بيشتغل: ضغطته واتحول لوضع الصورة فعلًا واتصوّر.
عنده مش شغال، وصورته بتقول إن **مصحف التجويد ٧٩٪ والمصحف المذهّب ٧٣٪** —
يعني لسه بيتحمّلوا. **الفرضية:** لما المصحف المختار صفحاته مش متحمّلة، وضع
الصورة بيفتح على لا شيء فيبان كأن الزرار ميّت.
**اعمل الآتي:** في `_buildViewer`'s image branch شوف بيرسم إيه لما الصفحة
مش موجودة، وخلّيه **يقول** («الصفحة لسه بتتحمّل — ٧٩٪») بدل ما يسيب فراغ.
واختبرها بإدخال مصحف نصّ صفحاته ناقصة.

### ٢. ١٠ كتب لكل مؤلف من الشاملة
فيه مؤلفين في المكتبة ليهم **كتاب واحد** بس. المطلوب: لكل واحد فيهم أحسن
١٠ كتب من الشاملة. ده شغل محتوى حقيقي، مش كود:
- استخدم `py -3 scripts/shamela_index.py find "<اسم الكتاب>"` — **مش** بحث
  الشاملة نفسه (فخ ١٧: بحثهم جوّه متون الكتب مش في عناوينها، وده اللي
  خلّى سيشن قديمة تفهرس الكتاب الغلط).
- كل كتاب لازم يتجزب ويتحقق ويترفع على R2 بنفس مسار `books/text/` وبنفس
  سياسة الـgzip (فخ ٦)، و**يتأكد إنه بيفتح في التطبيق فعلًا** قبل ما
  يتحط في `book_catalog.dart` (فخ ٢٣ — ١١ كتاب اتحطوا وماتفتحوش أبدًا).
- خد بالك من فخ ٣٤ و٣٥ في الفلترة.

### ٣. «خلفيات شاشة الأذان» — عشر خلفيات إسلامية متحركة
خيار جديد في إعدادات الأذان بالاسم ده، فيه **١٠ مشاهد مرسومة** (مش فيديو —
الكليبات اتشالت لسبب، شوف فخ ٣٦ و٣٧)، كل واحدة `CustomPainter` متحركة
وشكلها إسلامي. `lib/features/adhan/presentation/widgets/adhan_scene.dart`
هو النموذج: سماء لكل صلاة، مسجد بقبة ومئذنتين، وحلقات صوت متزامنة مع
عبارات الأذان من `adhan_phrase_timings.json`. المطلوب توسّعها لعشر خلفيات
يختار منها المستخدم.

### ٤. كارت «مقولة اليوم» في الشاشة الرئيسية
- يظهر **فوق كارت حديث اليوم**، ولو الخيار مفعّل من الإعدادات بس.
- كارت **صغير** فيه نسخة مصغّرة من شكل المقولة.
- التنقل للمقولة اللي بعدها بالسحب بالإصبع **أو** بأسهم.
- الضغط عليه يكبّره **ملء الشاشة**، وفيه زرار **dismiss**.
- المقولات موجودة أصلًا: `quoteLibraryProvider` و`quote_card_screen.dart`
  و`quotes/data/quote_palettes.dart`. ولما تعمله، **لفّه في
  `TutorialAnchor(id: TourAnchor.quoteCard, …)`** وضيف له فصل في
  `tutorial_chapters.dart` — الـanchor معرّف ومستنّي.

---

## اللي اتعمل واتشاف على الجهاز في السيشن دي

- **تنظيم الكود، المراحل ١–٦ كلها خلصت.** `library_screen` من ١٬٦٢٥ لـ٩٩،
  `ayah_sciences_sheet` من ١٬٥٤٣ لـ١٨٣، `quran_screen` من ١٬٧٠٠ لـ١٬٠٤٧،
  سبع كتالوجات طلعوا من `presentation` لـ`data/`، و`MainActivity` من ٤٢١
  لـ١٥١ بملف لكل قناة. وفيه تستات حارسة: سقف طول الملف، منع الداتا في
  `presentation`، وتطابق أسماء الـmethod channels بين كوتلن ودارت.
- **أمان:** zip-slip في فك الأرشيف (كان ممكن يكتب فوق قاعدة بيانات مبنيّة)،
  وكل اللينكات الخارجة بقت بتعدّي على فلتر http/https واحد.
- **وضع التركيز** بكارت في «المزيد»، ومخرجين اتجربوا.
- **اسم المدينة في الإشعار** بقى بلغة التطبيق — **اتأكد في حالتك بالظبط**:
  جهاز إنجليزي وتطبيق عربي، والإشعار طلع «المغرب · ٢٠:٠٤ / ١ ربيع الآخر
  ١٤٤٨ هـ · ميدان التحرير».
- **التوتوريال** اتعمل من أول: مفيش مؤقت، سبوت لايت بيفضي مكان الفيتشر
  ويعتّم اللي حواليه، وفقاعة بسهم بيشاور عليه، ١٧ محطة منهم حديث اليوم.
- **القارئ الغريب** `ar.oimaoqataris` اتشال، وأيقونات البرق/السحابة بقى
  ليها شرح مكتوب.
- **الأذان اتشال من التنزيلات**، و**كارت التنزيلات** بقى بلون الثيم،
  و**كارت الذكر الطويل** في المسبحة بقى بيتظبط على طول الشاشة.

## حاجة مقيسة وغير موثوقة — متبنيش عليها

بعتّ ١٧٦ طلب على `cdn.islamic.network` أختبر تلاوات القرّاء، فرجّع
403/502/000 لحوالي ١٦٠ منهم بينما أول طلب لوحده رجّع 206. **ده throttling
مش غياب محتوى** (فخ ٣٨)، والتطبيق أصلًا بيستخدم `everyayah.com` كمصدر أساسي.
لو عايز تتأكد إن القرّاء شغالين، اختبرهم على `everyayah.com` وبفواصل زمنية.

## أسئلة المالك لسه من غير رد (اتسألت أربع مرات)

- الـ١١ أوبچكت تحت `adhan/video/` على R2 (٧٤٬٠٨٠٬١٥٤ بايت) — يتمسحوا؟
- عنوان شغّال لـ**موقع المشكاة**؟
- سؤاله عن فتح **الشاملة** جوّه التطبيق وفهرسة كتبها في المكتبة.
