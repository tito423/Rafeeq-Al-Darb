اقرأ `CLAUDE.md` ثم `HANDOVER.md` (كتلة الحالة في أوله مكتوبة 2026-09-20).

## 🔴 اقرأ ده الأول: فيه نقطة رجوع، احترمها

المنشور **v3.50.0**، وهو **نقطة الرجوع المعتمدة** بطلب صريح من المالك قبل ما
يعمل أوديت كامل للتطبيق بعميل تاني:

| | |
|---|---|
| فرع ثابت | `known-good/v3.50.0` — **لا يُحرَّك ولا يُحذف مهما حصل** |
| وسم احتياطي | `backup-2026-09-21b` — نفس الكوميت |
| وسم الإصدار | `v3.50.0` — نفس الكوميت |
| التعليمات | `RESTORE.md` في جذر المستودع |
| الـAPK | `dist/RafeeqAlDarb-v3.50.0.apk` (259,748,286 بايت) وعلى Releases |

**لو خرّبت حاجة:** `git switch -c rescue-<date> known-good/v3.50.0`، راجع،
وبعدين `git reset --hard known-good/v3.50.0` و`push --force-with-lease`.
وعلى التليفون: `adb install -r dist/RafeeqAlDarb-v3.50.0.apk` (التوقيع فيه
lineage فمفيش إلغاء تثبيت ومفيش محتوى بيضيع).

**⚠️ مفتاح التوقيع في `../Rafeeq-Keys/` خارج المستودع ومش مغطّى بأي نسخة
احتياطية هنا.** لو ضاع، مفيش تحديث هيتركّب فوق التطبيق تاني إلا بإلغاء تثبيت
بيمسح مئات الميجابايت. **خد نسخة منه.**

## اللي لسه مفتوح — بالترتيب

### 1. إشعار الأذان — مش مشخّص، والمالك بلّغ عنه مرتين

«لما يجي الأذان وارفع الاشعار لفوق مفيش حاجة اوقفه منها» و«الأذان بيشتغل
أطفيه يرجع يتكرر».

الكود بيقول العكس: `AdhanNotifications.kt` بيبني الإشعار بـ`setOngoing(true)`
وزرار «إيقاف» وزرار «كتم»، و`AdhanService.onStartCommand` بيرجّع
`START_NOT_STICKY` وبيعمل `stopSelf` على intent فاضي، و`rearmAfterFiring`
بيحسب اليوم اللي بعده صح. **يعني اللي بيشوفه مايطابقش الكود.**

**كرّره قبل ما تغيّر سطر** (فخ #28):
```
adb shell settings put global auto_time 0
adb shell cmd alarm set-time <epoch-ms قبل الأذان بدقيقة>
adb shell dumpsys notification --noredact | grep -A40 rafeeq
```
وشوف الإشعار المنشور فعلًا فيه إيه، وسجّل اللي شفته.

### 2. ترتيب التشغيل الأول — نصّه اتعمل

طلبه: سبلاش ← الأذونات ← **التوتوريال** ← المزامنة و«مرحبًا بك» والخيارات.
اللي اتعمل: سبلاش ← صفحة الأذونات (شغّالة ومتشافة) ← الأونبوردنج (فيه المصحف
**وعلوم القرآن**). **التوتوريال لسه في مكانه القديم** جوّه التطبيق مش في
السلسلة.

### 3. صور شاشات المتجر جوّه «شرح ميزات واستخدام التطبيق»

طلبها مرتين وما اتعملتش. الصور جاهزة: `store/google_play/screenshots/phone/`
— **٨** صور 1080×1920، **٦.٧ ميجا** إجمالي. «شرح ميزات التطبيق» دلوقتي جولة
overlay (`tutorial_overlay.dart`) من غير شاشة محتوى، فمحتاجة **معرض** جديد.
⚠️ ٦.٧ ميجا على الـAPK قرار — الأفضل تصغيرها أو ترفعها على R2.

### 4. اللي اتصلّح ومااتشافش على جهاز

اتشاف بالكامل: **التشغيل الأول من أوله لآخره** — السبلاش بيشتغل، صفحة
الأذونات، «السماح بها» بتمشي على الخمسة، الموقع والمنبّهات بيتحطّ عليهم صح،
**ونافذة البطارية بتظهر** (ماكانتش تقدر قبل كده)، وبعد آخر إذن التطبيق بيفتح
لوحده على «اختر مصحفك» وفيها **علوم القرآن جنب المصحف** · وشاشة «العرض» في
الوضعين · وشريط المصحف على سطر واحد.

**الباقي مكتوب ومختبَر ومافُتحش على جهاز**:
المزامنة (القايمة البيضا)، ثيم RGB مع المصحف الورقي، التكرار بالقارئ المختار،
شريط «جارٍ تكرار الآية» وزر إيقافه، تنقّل التنزيلات والرجوع منها، أرقام علامات
الآيات، قائمة المزيد، وعلوم القرآن في صفحة البداية.

### 5. موقع المراجعة — جاهز بالكامل

https://tito423.github.io/rafeeq-review/ — الأذكار والاقتباسات والنصوص
والمصادر والحج والتجويد والكتب، و**التسعة كتب حديث** (`data/hadith/index.json`،
٩ كتب و١٤٨٢ باب). والباك-إند بيسجّل ملاحظات المراجعين فعلًا (اتجرّب POST/GET
بالعربي على الـWorker الحيّ).

### 6. التصغير

APK **٢٥٩٬٨١٣٬٨٢٢** بايت، كان **٣٣٩٬٩٢٠٬١٩٧** في v3.43.0. الخريطة في
`docs/size/`. **قاعدة ثابتة:** `minSdk` ٢٤ وتلات معماريات، **ولا تشيل واحدة** —
جرّبتها ونزل APK بيتركّب ويموت عند الفتح. و`hadith.zip` وHadeethEnc مدمجين
**بطلب صريح منه**.

## قواعد لا تُكسر

* **لا تنشر إصدارًا إلا بأمره**، وابنِه بـ`build_github_release.bat` وحده (فخ #41).
* **بلّغ بالأرقام المقيسة**، وقل صراحةً ما لم تره بعينك.
* **التنقّل الأعمى على المحاكي بيضيّع توكنز.** استخدم
  `adb shell uiautomator dump /sdcard/ui.xml` وخُد الإحداثيات من `bounds`.
* **`uinput` بيشتغل على المحاكي** رغم إن `sendevent` مرفوض بـSELinux — لو
  احتجت لمسة بإصبعين.
* **مصحف ورقي واحد.** لا تقترح طبعة لا تملك إحداثياتها المنشورة.
