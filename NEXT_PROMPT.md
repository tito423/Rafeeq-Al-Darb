# رفيق الدرب — كمل من هنا

اقرأ `CLAUDE.md` الأول، ده إلزامي ومش اختياري. `HANDOVER.md` بلوك الحالة في
أوله بيوصف **2026-09-18** بالظبط. البرومبت ده فيه كل حاجة — مش محتاج تفتح
ملف تاني عشان تبدأ.

---

## 0. الحالة المتحقَّق منها (2026-09-18) — أرقام متقاسة مش متفكّرة

| | |
|---|---|
| الإصدار | **v3.36.1** (`3.36.1+38`) — التاج والريليز الاتنين على `HEAD` |
| الشجرة | نضيفة، كل حاجة مدفوعة على `master` |
| analyze | `flutter analyze lib test integration_test` → **No issues found** |
| الاختبارات | **366** بتعدّي · **3** اختبارات على الجهاز (`integration_test`) |
| المحتوى المستضاف | **12/12** · صور الكروت **13/13** · أحجام الكتالوج **213/213** بصفر اختلاف |
| المكتبة | **213** كتاب في 9 أرفف · **43** فيهم `editorNotesRemoved` |
| القراءة الصوتية | **83 من 213** كتاب (البوابة: تشكيل ≥ 80%) |
| اللغات | **7 × 1,529** مفتاح، متطابقين |
| المقولات | **57** من 4 كتب · قنوات الدعوة **9** |
| الـAPK | **217.6 م.ب** — موقّع بالمفتاح الحقيقي، `sign_release.py` طبع `OK: rotated` |

الأرفف: tazkiyah 107 · hadith 29 · adab 22 · talibIlm 14 · tarikh 12 ·
fiqh 10 · aqidah 8 · seerah 8 · tafsir 3.

**لينك الريليز:** https://github.com/tito423/Rafeeq-Al-Darb/releases/tag/v3.36.1

---

## 1. أول حاجة: اسأله عن تليفونه — ولا تكمل قبل ما تسمع منه

المالك نزّل **v3.36.1** على الـ Honor بتاعه. ده **أول build يشتغل على جهازه
من v3.25.0** — يعني إحدى عشر إصدار كلهم متشافين على إيميوليتور بس.

اسأله الأربع أسئلة دي بالاسم، وماتفترضش إجابة أي واحدة فيهم:

1. **الأذان ضرب في الخلفية؟** ده وعد التطبيق الأساسي. Honor من أشرس الأجهزة
   في قتل التطبيقات في الخلفية. نجاحه على الإيميوليتور **لا يقول أي حاجة**
   عن جهازه. لو مضربش — دي أهم مشكلة في المشروع كله، وابدأ منها.
2. **شكل الإشعار** على واجهة Honor — هو طلبه يبقى زي «صلاتك».
3. **البوصلة** — الإيميوليتور **مفيهوش حساس مغناطيسي**، فاتجاه القبلة
   **غير متحقَّق منه إطلاقًا**.
4. **القراءة الصوتية** — يفتح كتاب مشكّل ويدوس «استماع» ويسمع بودنه. أنا
   أثبتّ إن المحرك بيقرا الحركات وإنه بيشتغل أوفلاين، لكن الحكم إنها
   «متقنة» حكمه هو.

---

## 2. حاجة واحدة ناقصة من طلبه: رأي Gemini

طلب أسأل **Gemini وChatGPT** عن تصميم القراءة الصوتية. **ChatGPT اتسأل**
وإجابته متسجلة في كوميت `421c1c1`. **Gemini مااتسألش** — طلب تسجيل دخول
(`gemini.google.com` و`aistudio.google.com`) ومادخلتش بحسابه.

لو فتحه هو وبعتلك رأيه، ضيفه. **متسجلش دخول بحسابه.**

---

## 3. حاجات نصّ خلصانة — متعاملش كأنها متحققة

- **زرار «استماع» اتشاف شغال** على كتاب واحد بس (`إخبار أهل الرسوخ`، مشكّل
  83.5%، والصوت اللي اشتغل `ar-xa-x-arz-seanet-embedded` — صوت مدمج، يعني
  اشتغل من غير نت). **مااتجربش على كتاب تحت الحد** — يعني رسالة «لا يُقرأ
  هذا الكتاب صوتيًا» **مااتشافتش على شاشة ولا مرة**. دي أول حاجة تجربها.
- **`AppConfig.donationUrl` فاضي** ولسه. شاشة الدعم بتخفي الزرار وده صح، بس
  لو المالك عنده لينك، يتحط بـ `--dart-define`.
- **الـ24-hour** للإسباني والفرنسي والبرتغالي والروسي: CLDR بيفضّل ساعة ٢٤
  للغات دي وإحنا فارضين ١٢. المالك قال «**تمام مش مشكلة**» — سيبها، بس فيه
  إعداد «نظام ١٢ ساعة» في الإعدادات لو حد سأل.

---

## 4. الشغل اللي بعده، لو هو موافق

**تحسين نموذج التشكيل على كتبنا.** ده اللي ممكن يفتح الـ**130** كتاب الباقيين
للقراءة الصوتية.

الرقم اللي المشروع واقف عنده، متقاس على كتبنا إحنا مش على بنشمارك:

```
CATT encoder-only، ١٠ من كتبنا المشكّلة، ٣٬٠٤٤ كلمة:
  vowel-WER      15.6%   كلمة من كل ٦ بحركة غلط
  shadda         16.5%   فرق شدّة بس
  حركة الإعراب   15.4%   وده مكان المعنى
```

`scripts/evaluate_catt_on_our_books.py` هو الهارنس — **شغّله تاني، متجادلش
في الرقم.** الأخطاء اللي فاضلة بالظبط هي النوع الخطر: `الْبَابِ` بترجع
`الْبَابُ`، و`مُدَبَّرَةً` بترجع `مُدَبِّرَةً` — مبني للمجهول بقى مبني
للمعلوم، المعنى اتقلب.

**متهيألي الطريق:** fine-tune لـCATT على الـ83 كتاب المشكّلة عندنا (الداتاسيت
وسكربت التدريب منشورين في الريبو بتاعه، Apache-2.0)، **ونموذج بيعرف يقول
«مش عارف»** فيسيب الكلمة بدون تشكيل بدل ما يخمّن. الدقة أهم من التغطية هنا،
لأن تكلفة الخطأ الواثق هي **نسبة كلام غلط لعالم**.

**حاجتين لازم تعرفهم قبل ما تبدأ:**
- **Tashkeela رخصته GPLv2** (75.6 مليون كلمة) — لو هتدرّب عليه، ده قرار
  ترخيص حقيقي مش تفصيلة.
- **CATT عنده تصدير ONNX جاهز** (`export_to_onnx.py`) ورخصته **Apache-2.0**،
  و`max_seq_len = 1024` فلازم تقطّع الفقرات قبلها. نسخة encoder-only هي
  الوحيدة اللي ممكن تشتغل على تليفون؛ نسخة encoder-decoder **مخلصتش بعد
  ٢٥ دقيقة** على CPU لعشر كتب — ده مش نتيجة عن دقتها، ده حقيقة عن سرعتها.

---

## 5. اللي اتعمل في سيشن 2026-09-17/18 — عشان تعرف انت واقف فين

1. **اتجاه الأرقام (bidi).** «a / b» كانت بتتقلب في **16** موضع، منهم شريط
   رقم صفحة المصحف وعدّاد المسبحة. **الاتنين كانوا "اتصلحوا" قبل كده**
   بتقسيم الرقمين لتلات `Text` جوّه `Row` — والكلام عن bidi كان صح والنتيجة
   غلط، لأن الـ`Row` تحت RTL بيحطّ أول عيل على **اليمين**. `ratio()` في
   `lib/core/utils/byte_formatter.dart` هو الإصلاح، ووراه اختبار **رندرة**
   بيقرا مواضع المؤشر تحت RTL حقيقي.
2. **الأرقام العربية.** ~**70** موضع كان بيكتب أرقام لاتينية جوّه جُمل عربية.
   اتصلحت **من مكان واحد** (`trn()` / `pluralN()` / `uiLanguageCode` في
   `lib/core/utils/digits.dart`) بعد ما 7 شاشات اتصلحت بالإيد والثامنة فضلت
   تطلع أكتر.
3. **حاجات كانت بتقول معلومة غلط.** تنزيل الحديث «~١٦ م.ب» والحقيقي **22.2**
   · الرقية «خمس تلاوات» وتحتها **ستة** · والتسجيل السادس **مش من
   archive.org** أصلًا · وصورتين في الأذكار **404**.
4. **القراءة الصوتية** اتعملت من الصفر: 83 من 213 كتاب، القرار متقاس مش
   مفترض، والقرآن **مابيتقرأش** بالمحرك ده إطلاقًا.
5. **السبع لغات** اتفتحوا على جهاز، خمسة منهم لأول مرة. و**كل الـ18 شاشة**
   اتفتحت.
6. **بحث التشكيل** — الإجابة «لسه لأ» ووراها رقم، مش رأي.

---

## 6. الملفات اللي تبدأ منها

| | |
|---|---|
| `lib/features/library/data/book_speaker.dart` | محرك القراءة + `pageSpeechText` اللي بيشيل القرآن |
| `lib/features/library/presentation/widgets/book_listen_action.dart` | زرار «استماع» والبوابة |
| `integration_test/tts_harakat_test.dart` | الدليل اللي الميزة كلها واقفة عليه (`-d emulator-5554`) |
| `scripts/measure_diacritisation.py` + `apply_diacritisation.py` | بيولّدوا `diacritisedPct` في الكتالوج |
| `scripts/evaluate_catt_on_our_books.py` | بيقيس هل نقدر نقرا الـ130 الباقيين |
| `lib/core/utils/digits.dart` | `trn` / `pluralN` / `uiLanguageCode` |
| `lib/core/utils/byte_formatter.dart` | `ratio()` و`formatBytes` |

---

## 7. قواعد شغل لازم تلتزم بيها

- **رد بالعربي.** الكود والكومتات والكومت-مسجات بالإنجليزي.
- **مفيش حاجة «خلصت» غير لما تشتغل على `emulator-5554` وتشوفها بعينك.**
  `flutter analyze` نضيف معناه إن الكود بيتكمبل، مش إن الميزة شغالة.
- **الاختبار اللي بيعدّي على كود مكسور أوحش من مفيش اختبار.** في السيشن
  اللي فات حصل **تلات مرات**: حارس بيفحص سطر سطر والكود حاطط الفاصل في
  السطر اللي بعده، واختبار اتكتب على `tr()` مش متاح فكان بيقيس الـfallback،
  وسكربت عدّى وهو فاشل عشان `\b` اتحوّل لبايت backspace. **جرّب إن الاختبار
  بيفشل على الكود المكسور قبل ما تثق فيه.**
- **متضغطش الأرقام، قِسها.** «~١٦ م.ب» كانت **٢٢.٢** — ناقص 39%.
- **الأسرار:** بيانات R2 في `scripts/.env` وهو gitignored — **متكتبهاش في
  كوميت ولا تطبعها ولا تحطها في رسالة**، و**متستخدمش `verify=False`** أبدًا،
  استخدم `r2_client()` من `scripts/r2_common.py`.
- **مفتاح التوقيع في `../Rafeeq-Keys/`** برّه الريبو، وهو مسؤولية المالك.
  **متنشرش ناتج `flutter build apk` على طول** — ده موقّع بمفتاح الديباج؛
  لازم `scripts/sign_release.py` يطبع `OK: rotated`.
- **رسايل الكوميت اكتبها في ملف و`git commit -F`** — الباك-تيك و`$` جوّه
  `bash -m` بيتمسخوا (حصل ٨ مرات).
- **الكونسول cp1256** فمش بيطبع عربي: `PYTHONIOENCODING=utf-8 PYTHONUTF8=1`
  أو اكتب في ملف UTF-8 و`cat`.
- **ملفات `_*.txt` و`_*.json` في الروت** (`_diacritisation.json`،
  `_catt_eval.txt`، `_catalog_sizes.txt`) نواتج قياس، مش كود.
- **`hw.ramSize` بتاع الإيميوليتور اترفع 2048 → 4096.** لو رجع 2 جيجا التطبيق
  هيعمل ANR على التشغيل وهتفتكرها باج في التطبيق. النسخة القديمة محفوظة جنبه.
- **الـbuild بيقتل الإيميوليتور** على الجهاز ده — ابني الأول، وبعدين شغّل
  الإيميوليتور.
- **اسأله عن الكوته في أول رد**، وقول له Remote Control شغّال ولا لأ
  (`ListAgents` هو الفحص).
- **متكتبش البرومبت ده تاني** غير لما يقول «**جهّز الدنيا**».
