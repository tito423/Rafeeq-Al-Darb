# البرومبت الجاهز للسيشن الجاية

اقرأ `CLAUDE.md` الأول (المنهج الملزم)، وبعده **`WORK_QUEUE.md`** (أول قسم:
الدفعة الخامسة D1–D6)، وبعدها `HANDOVER.md`.

**قبل ما تخطّط: اسأله عن الكوتة (§2.0).** آخر رقم قاله «١٥» في نص السيشن اللي
فاتت — ده رقم قديم، اسأل تاني.

---

## الحالة عند التوقف — متحققة

- المُصدَر: **v3.17.0** على `master`. قبل ما تعمل أي حاجة: 
  `gh release view v3.17.0 --json tagName,targetCommitish,assets` وتأكد إن SHA
  التاج = `git rev-parse HEAD`، وإن فيه إصدار واحد بس.
- `flutter analyze lib test` نضيف · `flutter test` **١٧٦ ناجحة**.
- الـAPK اتوقّع بـ`scripts/sign_release.py` (المفتاح الجديد على ٩+، القديم تحته).
  الحجم ٣٤٧٬١٧٣٬١٧٠ بايت.

---

## أول حاجة: اللي اتعمل ومحدّش شافه على جهاز — اعمله تاني وماتثقش فيه

1. **تحميل تلاوة كاملة لآخرها.** اتشاف تحميل الفاتحة بس. نزّل تلاوة قارئ كاملة
   (١١٤ سورة) وجرّب «إيقاف التنزيل» ثم «استكمال»، واقفل التطبيق بـ
   `am force-stop` في النص وافتحه وشوف بيكمّل لوحده ولا لأ
   (`QuranAudioLibrary.repair` بيتنادى من `ensureReady`).
2. **مؤقت الإيقاف** (بالدقائق وبعد نهاية السورة) وإضافة ملفات من الجهاز —
   ماتجرّبوش خالص.
3. **زرار «إصلاح التحميلات» على تحميل مصحف واقف فعلًا** — اللي اتشاف هو
   الاستكمال التلقائي بعد إعادة الفتح، مش الزرار. وكمان الكارت في «التنزيلات»
   وهو بيرجع يتصل بتحميل استُكمل بعد الفتح.
4. **«وضع المصحف مش بيشغّل أول مصحف مصوّر»** — ماتكرّرش على الإيموليتر (التجويد
   فتح صفحة 249). مستني منه لقطة. ماتخترعش سبب.
5. **كل ده على تليفونه** مش على إيموليتر x86.

---

## حاجات لازم تعرفها

- **مشغّل واحد بس في التطبيق.** `just_audio_background` بيرفض مشغّل تاني.
  المشغّل الجديد بياخد `AyahAudioService.player` عن طريق `claimForMusic()`،
  وأي تشغيل تاني بيحط `musicOwnsPlayer = false`. ماتعملش `AudioPlayer()` جديد.
- **الموسوعة مدمجة** في `assets/data/hadeethenc/` (٧ ملفات zip). لو اتحدّثت
  الحزم: غيّر الملفات + `hadeethenc.json` + `AppConfig.hadeethEncVersion` مع بعض —
  `hadeethenc_pack_test` بيقارن الحجم.
- **رأس الصفحة** بيقرا أول وآخر صفحة لكل سورة من `quran_local.db`.
- **تحميلات المصاحف المطلوبة** محفوظة في `mushaf.wanted_downloads_v1` وبتتكمّل
  بعد ٥ ثواني من فتح التطبيق.
- Urdu في API الـmp3quran بيرجّع أسماء مليانة كلمات SEO — عشان كده بيستخدم
  القائمة العربي.

## ما تعملهاش تاني

- `flutter build apk --release` بيقتل الإيموليتر (فخ ٢٤). ابنِ الأول.
- **`Set-Content -Encoding utf8` في PowerShell 5.1 بيكتب BOM** — حصل في
  `pubspec.yaml` واتشال. استخدم Write أو Python.
- **heredoc في bash مع نص عربي فيه علامات تنصيص فشل بـ `unexpected EOF`** — اكتب
  السكربت بأداة Write وشغّله (فخ ١١).
- `adb shell input text` مابيكتبش في حقول Flutter هنا — دوس على أزرار الكيبورد.
- التوقيع: بعد `flutter build apk --release` لازم `py -3 scripts/sign_release.py`.
