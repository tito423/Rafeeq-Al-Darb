# كمّل رفيق الدرب من حيث وقفنا (2026-09-22)

اقرأ `CLAUDE.md` كاملًا (إلزامي)، ثم أعلى `HANDOVER.md`: جدول الحالة وكتلة
«SESSION 2026-09-22» و«LATER THE SAME DAY». ردّ بالعربي. في أول رد: اقرأ الكوتة
حيّة (§2.0) وتحقق من Remote Control بـ `ListAgents` (§2.0b).

## أين وقفنا بالضبط
- `master` مدفوع. **لا إصدار منذ v3.55.0**. كل شغل 2026-09-22 رُئي على
  `emulator-5554` فقط و**لم يصل لهاتفي**.
- **مكتبة المرحلة ١ نصف منجزة — لا تثق بها، أكملها:** 19 كتابًا مُعرَّفة في
  `BOOKS` داخل `scripts/build_book_text.py` (قسم «library «المرحلة ١»»). كانت
  16 مبنيّة في `scripts/book_text_build/` (خارج git، على القرص فقط) و3 ما زالت
  تُزحف: `ihkam_al_ahkam`، `al_rawd_al_murbi`، `al_iqna_fi_hall_alfaz_abi_shuja`.
  **لم يُرفع ولم يُضف للكتالوج أي كتاب منها.**

## افعل بالترتيب
1. تحقق أي ملفات المرحلة ١ موجودة؛ ابنِ الناقص:
   `py -3 scripts/build_book_text.py <id>`.
2. **أخرج** `tawdih_al_maqasid_sharh_al_nuniyyah` من `PLAN` في
   `scripts/library_phase1.py` (مؤلفه نجدي؛ قاعدتي: لا متشددين).
3. `py -3 scripts/library_phase1.py --report` واقرأ كل بطاقة في
   `scripts/library_phase1_out.txt`. **بطاقة `al_rawd_al_murbi` تحديدًا**: لو فيها
   العثيمين أو السعدي في «معه/حاشية/تعليق» ارفضه أو اختر طبعة أخرى برقمها.
4. شغّله بدون `--report`: يرفع لـR2 بلا `Content-Encoding`، يتحقق من الرابط
   العام، يحذف `al_waraqat` و`al_ajurrumiyyah` (متون مجردة)، ويضيف الكتب.
   `flutter analyze` + `flutter test` ثم افتح المكتبة على المحاكي وشاهد.
5. **تنظيف حواشي المحققين** — قاعدتي: «شيل أي حاجة لابن باز وابن عثيمين وابن
   جبرين وابن عبد الوهاب والألباني إلا التخريج، وأي حد معروف بالتشدد؛ لو
   شيلها يضر الكتاب سيبها، مش هيضر شيلها». قيس أولًا (الملاحظات في HANDOVER):
   لا كتاب في الكتالوج من تأليفهم؛ المطابقات في حواشي المحققين (أوضحها مقدمة
   مشهور حسن لـ«الموافقات»). **لا تمس نص المؤلف** ولا الأسماء المتشابهة
   (عبد الرحمن بن سليمان العثيمين المؤرخ، رواة اسمهم محمد بن عبد الوهاب).
   ارفع المعدَّل واقرأه بعينك.
6. قِس كتابًا كبيرًا على هاتف (مصيدة #4) قبل موسوعات المرحلة ٢.
7. لا متون مجردة في المكتبة؛ كتب ابن القيم وابن الجوزي والحكيم الترمذي
   وابن أبي الدنيا والزهد والرقائق **تبقى كلها** (ومتن النونية يبقى).
8. إصدار حين أطلب: ارفع `pubspec` (ما زال 3.55.0+57)،
   `build_github_release.bat`، احذف الإصدار السابق ووسمه (عدا v3.51.0)،
   ملاحظات عربية صادقة، وتحقق أن الوسم = `HEAD`.

## ما لم يُحسم
- نسخة البلاي ستور بدون مكتبة (قرار مبدئي، لم يُبنَ).
- أرقام عدّاد إشعار الصلاة لاتينية (Android يستخدم لغة النظام).
